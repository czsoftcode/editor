---
phase: 09-core-sandbox-logic-settings-removal
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - src/app/sandbox.rs
  - src/app/mod.rs
  - src/app/ui/workspace/state/mod.rs
  - src/app/ui/workspace/state/init.rs
  - src/app/ui/panels.rs
  - src/app/ui/ai_panel.rs
  - src/app/ui/terminal/ai_chat/render.rs
  - src/app/ui/workspace/modal_dialogs/ai.rs
autonomous: true
requirements: [CORE-01, CORE-02]
gap_closure: true

must_haves:
  truths:
    - "Soubor src/app/sandbox.rs neexistuje"
    - "pub mod sandbox deklarace neexistuje v src/app/mod.rs"
    - "WorkspaceState neobsahuje sandbox ani file_tree_in_sandbox fieldy"
    - "Zadny soubor v src/ neobsahuje struct Sandbox ani struct SyncPlan"
    - "Projekt se kompiluje pres cargo check (warnings povoleny)"
    - "Vsechny testy prochazi (cargo test --lib)"
  artifacts:
    - path: "src/app/sandbox.rs"
      provides: "NEEXISTUJE — smazany"
    - path: "src/app/ui/workspace/state/mod.rs"
      provides: "WorkspaceState bez sandbox a file_tree_in_sandbox fieldu"
    - path: "src/app/ui/workspace/state/init.rs"
      provides: "init_workspace bez Sandbox::new a sandbox fieldu v konstruktoru"
  key_links:
    - from: "src/app/ui/panels.rs"
      to: "src/app/ui/workspace/state/mod.rs"
      via: "panels pouzivaji ws.root_path misto ws.sandbox.root"
      pattern: "ws\\.root_path"
    - from: "src/app/ui/ai_panel.rs"
      to: "src/app/ui/workspace/state/mod.rs"
      via: "AI panel start agent primo bez sync plan"
      pattern: "start_response\\.clicked"
---

<objective>
Smazat src/app/sandbox.rs, odstranit sandbox a file_tree_in_sandbox fieldy z WorkspaceState, a opravit vsechny remaining reference na ws.sandbox.* v celem codebase. Toto uzavira gapy CORE-01 a CORE-02 z verifikace.

Purpose: Dokoncit Phase 9 — sandbox modul a jeho datove struktury musi kompletne zmizet z codebase.
Output: sandbox.rs smazan, vsechny reference opraveny, projekt se kompiluje a testy prochazi.
</objective>

<execution_context>
@/home/stkremen/.claude/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-core-sandbox-logic-settings-removal/09-CONTEXT.md
@.planning/phases/09-core-sandbox-logic-settings-removal/09-01-SUMMARY.md
@.planning/phases/09-core-sandbox-logic-settings-removal/09-02-SUMMARY.md
@.planning/phases/09-core-sandbox-logic-settings-removal/09-VERIFICATION.md

@src/app/sandbox.rs
@src/app/mod.rs
@src/app/ui/workspace/state/mod.rs
@src/app/ui/workspace/state/init.rs
@src/app/ui/panels.rs
@src/app/ui/ai_panel.rs
@src/app/ui/terminal/ai_chat/render.rs
@src/app/ui/workspace/modal_dialogs/ai.rs

<interfaces>
<!-- Aktualni stav WorkspaceState — fieldy k odstraneni -->
From src/app/ui/workspace/state/mod.rs (radky 133, 136):
```rust
pub file_tree_in_sandbox: bool,          // radek 133 — SMAZAT
pub sandbox: crate::app::sandbox::Sandbox, // radek 136 — SMAZAT
```

From src/app/ui/workspace/state/init.rs (radky 23, 28, 193, 196):
```rust
let sandbox = crate::app::sandbox::Sandbox::new(&root_path);  // radek 23 — SMAZAT
project_watcher.add_path(&sandbox.root);                        // radek 28 — SMAZAT
file_tree_in_sandbox: false,                                    // radek 193 — SMAZAT
sandbox,                                                        // radek 196 — SMAZAT
```

From src/app/ui/file_tree/mod.rs (radek 92-97):
```rust
pub fn ui(&mut self, ui: &mut Ui, i18n: &I18n, is_sandbox: bool) -> FileTreeResult
```
Tato funkce prijima is_sandbox parametr — Phase 10 ji upravi. Ted prochazime false.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smazat sandbox.rs, odstranit sandbox fieldy z WorkspaceState a init</name>
  <files>src/app/sandbox.rs, src/app/mod.rs, src/app/ui/workspace/state/mod.rs, src/app/ui/workspace/state/init.rs</files>
  <action>
1. **SMAZAT src/app/sandbox.rs** — celý soubor (rm).

2. **src/app/mod.rs** — radek 14: odstranit `pub mod sandbox;`. Zkontrolovat ze zadny dalsi radek neimportuje z sandbox modulu.

3. **src/app/ui/workspace/state/mod.rs** — odstranit 2 fieldy z WorkspaceState struct:
   - Radek 133: `pub file_tree_in_sandbox: bool,` — SMAZAT
   - Radek 136: `pub sandbox: crate::app::sandbox::Sandbox,` — SMAZAT
   - Overit ze zadny import `crate::app::sandbox` nezustal v souboru

4. **src/app/ui/workspace/state/init.rs** — upravit init_workspace funkci:
   - Radek 23: SMAZAT `let sandbox = crate::app::sandbox::Sandbox::new(&root_path);`
   - Radek 28: SMAZAT `project_watcher.add_path(&sandbox.root);` (project_watcher jiz watchuje root_path pres ProjectWatcher::new, sandbox dir neni potreba)
   - Radek 193: SMAZAT `file_tree_in_sandbox: false,` z WorkspaceState konstruktoru
   - Radek 196: SMAZAT `sandbox,` z WorkspaceState konstruktoru

Po tomto tasku projekt NEBUDE kompilovat — zbyvajici reference na ws.sandbox.* a ws.file_tree_in_sandbox opravuje Task 2.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor && test ! -f src/app/sandbox.rs && echo "sandbox.rs deleted" && ! grep -q 'pub mod sandbox' src/app/mod.rs && echo "mod removed" && ! grep -q 'file_tree_in_sandbox' src/app/ui/workspace/state/mod.rs && echo "field removed" && ! grep -q 'crate::app::sandbox' src/app/ui/workspace/state/init.rs && echo "init cleaned"</automated>
  </verify>
  <done>
    - src/app/sandbox.rs neexistuje
    - pub mod sandbox; odstraneno z app/mod.rs
    - file_tree_in_sandbox a sandbox fieldy odstraneny z WorkspaceState
    - init_workspace nevytvari Sandbox instanci
  </done>
</task>

<task type="auto">
  <name>Task 2: Opravit vsechny remaining sandbox reference a zajistit kompilaci</name>
  <files>src/app/ui/panels.rs, src/app/ui/ai_panel.rs, src/app/ui/terminal/ai_chat/render.rs, src/app/ui/workspace/modal_dialogs/ai.rs</files>
  <action>
1. **src/app/ui/panels.rs** — radky 42-88: Cely sandbox toggle blok v left panelu nahradit jednoduchym nadpisem.
   Konkretne:
   - Radek 42-48: Nahradit if/else `file_tree_in_sandbox` za prosty nadpis: `ui.heading(egui::RichText::new(i18n.get("panel-files")).strong());`
   - Radky 51-79: SMAZAT cely blok `ui.with_layout(right_to_left)` s Project/Sandbox selectable_label toggle buttons a reload logikou — to je UI Phase 10 funkcionalita ktera uz nema smysl
   - Radek 88: Zmenit `ws.file_tree.ui(ui, i18n, ws.file_tree_in_sandbox)` na `ws.file_tree.ui(ui, i18n, false)` — vzdy project mode

2. **src/app/ui/ai_panel.rs** — radky 161-177: Zjednodusit start agent logiku.
   Nahradit cely blok s `ws.sandbox.get_sync_plan()`:
   ```rust
   if start_response.clicked()
       && let Some(agent) = current_agent
   {
       // Start agent immediately (no sandbox sync needed)
       let cmd = agent.command.clone();
       let active = ws.claude_active_tab;
       let context = format_context_for_terminal(&AiManager::generate_context(ws));
       if let Some(terminal) = ws.claude_tabs.get_mut(active) {
           terminal.send_command(&cmd);
           if agent.context_aware {
               terminal.send_command(&context);
           }
       }
   }
   ```
   Cely blok s `plan.is_empty()` / `sync_confirmation` odebrat. Uz neni ws.sync_confirmation field.

3. **src/app/ui/terminal/ai_chat/render.rs** — radek 346: Nahradit `ws.sandbox.root.to_string_lossy()` za `ws.root_path.to_string_lossy()`.

4. **src/app/ui/workspace/modal_dialogs/ai.rs** — radky 88-98 (promotion_success dialog): Tento dialog referencuje `ws.promotion_success` ktery byl odstranen v Plan 02. Pokud blok stale existuje, SMAZAT cely blok (radky 79-98).
   - Radky 100-202 (sync confirmation dialog): SMAZAT cely blok — referencuje `ws.sync_confirmation`, `ws.sandbox.root`, `ws.sandbox.promote_file`, `ws.sandbox_staged_dirty`, `ws.pending_agent_id`. Vsechny tyto fieldy uz neexistuji na WorkspaceState. Agent se nyni startuje primo z ai_panel.rs bez sync dialogu.
   - Radky 205-284 (sandbox staged files dialog): SMAZAT cely blok — referencuje `ws.show_sandbox_staged`, `ws.sandbox_staged_files`, `ws.sandbox.root`. Staged files uz neexistuji.
   - POZOR: Nesmaz funkci `render_ai_dialogs` samotnou — ponechej ji, ale bez sandbox dialogu. Funkce muze obsahovat i jine AI dialogy (model picker, error dialog atp.) — ponechej vse co neni sandbox.

5. **Iterativni oprava kompilace:**
   - Spustit `cargo check 2>&1 | head -50` po kazdé vétsi skupine zmen
   - Pokud se objevi dalsi sandbox reference ktere nebyly v planu — opravit analogicky (nahradit ws.sandbox.root za ws.root_path, smazat sandbox-only bloky, nahradit sandbox booly za false)
   - Pokud se objevi unused import warnings pro crate::app::sandbox — SMAZAT import
   - Cil: `cargo check` projde (warnings OK, errors NE)
   - Pote: `cargo test --lib 2>&1` — overit ze vsechny testy prochazi
   - Nakonec: `grep -r 'crate::app::sandbox' src/` — musi byt prazdny vysledek (krome modal_dialogs/sandbox.rs ktery je #[cfg(never)])
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor && cargo check 2>&1 | tail -5 && echo "---" && ! grep -rq 'struct Sandbox' src/ && echo "No Sandbox struct" && ! grep -rq 'struct SyncPlan' src/ && echo "No SyncPlan struct" && ! grep -rq 'ws\.sandbox\.' src/ && echo "No ws.sandbox refs" && ! grep -rq 'file_tree_in_sandbox' src/ && echo "No file_tree_in_sandbox refs" && cargo test --lib 2>&1 | tail -5</automated>
  </verify>
  <done>
    - cargo check projde bez errors (warnings povoleny)
    - Zadne struct Sandbox/SyncPlan v src/ (krome #[cfg(never)] modal_dialogs/sandbox.rs)
    - Zadne ws.sandbox.* reference v src/
    - Zadne file_tree_in_sandbox reference v src/
    - cargo test --lib projde
  </done>
</task>

</tasks>

<verification>
- `test ! -f src/app/sandbox.rs` — soubor neexistuje
- `! grep -q 'pub mod sandbox' src/app/mod.rs` — mod deklarace odstranena
- `! grep -r 'crate::app::sandbox::Sandbox' src/app/ui/workspace/state/` — zadne reference ve state
- `! grep -r 'ws\.sandbox\.' src/` — zadne ws.sandbox reference
- `! grep -r 'file_tree_in_sandbox' src/` — zadne file_tree_in_sandbox reference
- `cargo check` — kompilace bez errors
- `cargo test --lib` — vsechny testy prochazi
</verification>

<success_criteria>
sandbox.rs smazan, pub mod sandbox; odstraneno, WorkspaceState bez sandbox fieldu, vsechny reference opraveny, projekt se kompiluje a testy prochazi. CORE-01 a CORE-02 jsou splneny.
</success_criteria>

<output>
After completion, create `.planning/phases/09-core-sandbox-logic-settings-removal/09-03-SUMMARY.md`
</output>
