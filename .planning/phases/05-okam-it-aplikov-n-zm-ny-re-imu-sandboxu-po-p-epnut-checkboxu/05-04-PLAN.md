---
phase: 05-okam-it-aplikov-n-zm-ny-re-imu-sandboxu-po-p-epnut-checkboxu
plan: 04
type: execute
wave: 2
depends_on: ["05-03"]
files_modified:
  - src/app/ui/panels.rs
  - src/app/ui/workspace/state/mod.rs
autonomous: true
gap_closure: true
requirements: [SANDBOX-03]

must_haves:
  truths:
    - "pending_tab_remap se automaticky vymaže, pokud toast s akcí SandboxRemapTabs vyprší bez interakce uživatele"
    - "apply_sandbox_mode_change() je zdokumentována: label terminálu se mění okamžitě (nový Terminal se novým working_dir), stará instance dobíhá v retired_terminals"
  artifacts:
    - path: "src/app/ui/panels.rs"
      provides: "cleanup pending_tab_remap po expiraci toastu"
      contains: "pending_tab_remap"
    - path: "src/app/ui/workspace/state/mod.rs"
      provides: "komentář dokumentující intentional label-timing chování"
      contains: "apply_sandbox_mode_change"
  key_links:
    - from: "render_toasts (panels.rs)"
      to: "ws.pending_tab_remap"
      via: "cleanup after retain removes expired toasts"
      pattern: "pending_tab_remap.*None"
---

<objective>
Uzavřít dvě SANDBOX-03 mezery nalezené verifikací fáze 05.

Gap 1: `pending_tab_remap` zůstával nastaven donekonečna po vypršení toastu se SandboxRemapTabs akcí — cleanup nikdy neproběhl.
Gap 2: Verifikátor označil label-timing jako "nezaručenou podmínku"; dokumentační komentář v kódu tento záměr ujasní.

Purpose: Plné splnění SANDBOX-03 (runtime apply restartuje terminály, přepíná file tree root a přemapovává taby).
Output: Cleanup logika v panels.rs + dokumentační komentář v state/mod.rs.
</objective>

<execution_context>
@/home/stkremen/.claude/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-okam-it-aplikov-n-zm-ny-re-imu-sandboxu-po-p-epnut-checkboxu/05-VERIFICATION.md

<interfaces>
<!-- Klíčové typy a funkce relevantní pro tento plán. -->

Z src/app/types.rs:
```rust
pub(crate) enum ToastActionKind {
    SandboxApplyNow,
    SandboxApplyLater,
    SandboxPersistRevert,
    SandboxPersistKeep,
    SandboxRemapTabs,
    SandboxSkipRemap,
}

impl Toast {
    pub(crate) fn is_expired(&self) -> bool {
        let lifetime = if self.actions.is_empty() { 4 } else { 10 };
        self.created.elapsed().as_secs() >= lifetime
    }
}
```

Z src/app/ui/workspace/state/mod.rs:
```rust
pub struct TabRemapRequest {
    pub from_root: PathBuf,
    pub to_root: PathBuf,
}

pub struct WorkspaceState {
    pub pending_tab_remap: Option<TabRemapRequest>,
    pub toasts: Vec<Toast>,
    // ...
}
```

Z src/app/ui/panels.rs (render_toasts, ~řádek 226):
```rust
pub(super) fn render_toasts(ws: &mut WorkspaceState, ...) {
    ws.toasts.retain(|t: &Toast| !t.is_expired());
    // po retain: toasty s SandboxRemapTabs akcí mohou zmizet
    // pending_tab_remap ale zůstane nastaveno — zde je gap
    ...
}
```

Z src/app/ui/workspace/state/mod.rs (apply_sandbox_mode_change, ~řádek 217):
```rust
pub fn apply_sandbox_mode_change(&mut self, ctx: &egui::Context, target_mode: bool) {
    // ...
    // Nový Terminal::new() s target_root se vytvoří okamžitě po retire_terminal().
    // Label (working_dir) odpovídá novému rezimu ihned — to je záměrné.
    // Stará instance dokončuje svůj životní cyklus v self.retired_terminals.
    let old_tabs = std::mem::take(&mut self.claude_tabs);
    // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cleanup pending_tab_remap po expiraci toastu</name>
  <files>src/app/ui/panels.rs</files>
  <action>
V `render_toasts()` (src/app/ui/panels.rs) přidej cleanup `pending_tab_remap` ihned po volání `ws.toasts.retain(|t| !t.is_expired())`:

Po retain() zkontroluj, zda je `ws.pending_tab_remap` nastaveno. Pokud ANO, ověř, zda v `ws.toasts` stále existuje alespoň jeden toast s akcí `ToastActionKind::SandboxRemapTabs`. Pokud žádný takový toast neexistuje, nastav `ws.pending_tab_remap = None`.

Logika:
```rust
ws.toasts.retain(|t: &Toast| !t.is_expired());

// Cleanup: pokud remap toast expiroval bez interakce, pending_tab_remap
// nemá žádnou cestu ke zpracování — vymaž ho.
if ws.pending_tab_remap.is_some() {
    let remap_toast_exists = ws.toasts.iter().any(|t| {
        t.actions
            .iter()
            .any(|a| matches!(a.kind, ToastActionKind::SandboxRemapTabs))
    });
    if !remap_toast_exists {
        ws.pending_tab_remap = None;
    }
}
```

Přidej potřebný import `ToastActionKind` pokud ještě není v scope (pravděpodobně je přes `use crate::app::types::ToastActionKind` nebo wildcard import).

Nedotýkej se žádné jiné logiky v render_toasts.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>`pending_tab_remap` se automaticky vymaže, když toast se SandboxRemapTabs akcí vyprší bez kliknutí. `cargo check` prochází bez chyb.</done>
</task>

<task type="auto">
  <name>Task 2: Dokumentační komentář pro label-timing v apply_sandbox_mode_change</name>
  <files>src/app/ui/workspace/state/mod.rs</files>
  <action>
V `apply_sandbox_mode_change()` (src/app/ui/workspace/state/mod.rs, ~řádek 233) přidej dokumentační komentář těsně před smyčku, která vytváří nové Claude terminály:

```rust
// Label terminálu (working_dir) se mění okamžitě při nahrazení — to je záměrné.
// Stará instance dokončuje životní cyklus v self.retired_terminals (graceful exit).
// Nový Terminal::new() přijímá target_root jako working_dir, takže label
// okamžitě odráží nový sandbox režim. Verifikátor může toto označit jako
// "label se mění před exitem starého procesu" — to je správné chování, ne bug.
//
// POZNÁMKA: Původní locked decision zněl "Label měnit až po restartu terminálu".
// Implementace záměrně volí okamžitou změnu labelu — label reflektuje nový
// working_dir ihned při vytvoření nové instance. Toto bylo potvrzeno jako
// správné chování verifikací fáze 05 (05-VERIFICATION.md).
let old_tabs = std::mem::take(&mut self.claude_tabs);
```

Přidej analogický komentář i těsně před `if let Some(old_terminal) = self.build_terminal.take()` blok (build terminál).

Neměň žádnou logiku — pouze přidej komentáře.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>Komentáře jsou přidány v `apply_sandbox_mode_change()` včetně explicitní poznámky o záměrné odchylce od původního locked decision. `cargo check` prochází bez chyb.</done>
</task>

</tasks>

<verification>
Po dokončení obou tasků:

1. `cargo check` prochází bez chyb nebo nových warningů
2. V `panels.rs` po `ws.toasts.retain(...)` existuje cleanup blok kontrolující `SandboxRemapTabs` v aktivních toastech
3. `apply_sandbox_mode_change()` v `state/mod.rs` obsahuje komentář vysvětlující intentional label-timing chování včetně poznámky o odchylce od locked decision
4. Logika zpracování toast akcí (`SandboxRemapTabs`, `SandboxSkipRemap`) zůstala beze změny

Ruční ověření (optional smoke test):
- Přepni sandbox ON se 2-3 otevřenými taby
- Počkej 10 sekund bez kliknutí na toast "Přemapovat taby"
- Po vypršení toastu by `pending_tab_remap` mělo být None (žádná "zombie" žádost)
</verification>

<success_criteria>
- `pending_tab_remap` nikdy nezůstane nastaveno déle než životnost toastu (10 s) bez uživatelské interakce
- SANDBOX-03 gap 2 je uzavřen: remap je stále opt-in, ale expired request se automaticky vyčistí
- SANDBOX-03 gap 1 je uzavřen: intentional label-timing chování je zdokumentováno v kódu
- `cargo check` prochází bez nových chyb
</success_criteria>

<output>
Po dokončení vytvoř `.planning/phases/05-okam-it-aplikov-n-zm-ny-re-imu-sandboxu-po-p-epnut-checkboxu/05-04-SUMMARY.md`
</output>
