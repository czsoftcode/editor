---
phase: 03-light-varianty-settings-ui
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/settings.rs
autonomous: true
requirements: [LITE-01, LITE-02, LITE-03, LITE-04]

must_haves:
  truths:
    - "`Settings::to_egui_visuals()` mapuje vsechny tri light varianty na explicitni palety: WarmIvory (255,252,240), CoolGray (242,242,242), Sepia (240,230,210)."
    - "Kazda light varianta nastavuje `panel_fill`, `window_fill` i `faint_bg_color`; `faint_bg_color` neni shodny s `panel_fill` a panely se vizualne oddeli."
    - "Dark branch zustava funkcne kompatibilni (`Visuals::dark()`), bez regresi do jiz hotovych THEME/TERM/TREE casti."
    - "Mapovani variant je testovatelne unit testy v `src/settings.rs` a kryje LITE-01..LITE-04 bez UI zavislosti."
  artifacts:
    - path: "src/settings.rs"
      provides: "Per-variant light visuals mapovani + unit testy kontrastu/palet."
      exports: ["Settings::to_egui_visuals"]
      contains: "match self.light_variant"
  key_links:
    - from: "Settings::to_egui_visuals()"
      to: "LightVariant"
      via: "match vetve WarmIvory/CoolGray/Sepia"
      pattern: "LightVariant::(WarmIvory|CoolGray|Sepia)"
    - from: "Light variant palette"
      to: "egui::Visuals"
      via: "panel_fill + window_fill + faint_bg_color"
      pattern: "(panel_fill|window_fill|faint_bg_color)"
---

<objective>
Dodat konecne mapovani tri light variant do `Settings::to_egui_visuals()` tak, aby kazda varianta mela vlastni citelnou paletu a splnila LITE-01..LITE-04.

Purpose: Uzavrit modelovou cast Phase 03 pred UI integraci; vsechny vizualni konstanty drzet centralne v `settings.rs`.
Output: `src/settings.rs` s explicitnim mapovanim variant a unit testy overujicimi barvy + oddeleni panelu.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-light-varianty-settings-ui/03-CONTEXT.md
@.planning/phases/03-light-varianty-settings-ui/03-RESEARCH.md
@.planning/phases/03-light-varianty-settings-ui/03-VALIDATION.md
@src/settings.rs

<interfaces>
```rust
// src/settings.rs (aktualne)
pub fn to_egui_visuals(&self) -> eframe::egui::Visuals {
    if self.dark_theme {
        eframe::egui::Visuals::dark()
    } else {
        eframe::egui::Visuals::light()
    }
}

// cil: explicitni mapovani per light variant,
// dark branch zustava beze zmeny.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementovat per-variant light palety v Settings modelu</name>
  <files>src/settings.rs</files>
  <action>
    1. V `Settings::to_egui_visuals()` zachovej dark branch (`Visuals::dark()`).
    2. Pro light branch vyjdi z `Visuals::light()` a pro kazdou `LightVariant` vetve nastav:
       - `panel_fill` (LITE-01/02/03 presne dle requirementu),
       - `window_fill` (jemne odlisene od panelu),
       - `faint_bg_color` (citelne odliseni panelu).
    3. Nemen `syntect_theme_name()` ani ostatni fields Settings; scope drzet ciste na visuals mapovani.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor/.polycredo/sandbox && RUSTC_WRAPPER= cargo check</automated>
  </verify>
  <done>
    `to_egui_visuals()` vraci tri odlisne light varianty a dark branch zustava kompatibilni.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit testy pro LITE-01..LITE-04</name>
  <files>src/settings.rs</files>
  <action>
    1. Pridej testy pro kazdou light variantu (`WarmIvory`, `CoolGray`, `Sepia`) s asserty na cilove `panel_fill` RGB.
    2. Pridej testy, ze `faint_bg_color != panel_fill` a hodnoty `faint_bg_color` jsou pairwise odlisne mezi variantami.
    3. Pridej regresni test, ze dark mode stale vraci `visuals.dark_mode == true`.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor/.polycredo/sandbox && RUSTC_WRAPPER= cargo test settings::tests -- --nocapture</automated>
  </verify>
  <done>
    Testy deterministicky potvrzuji mapovani LITE-01..LITE-04 a nejsou zavisle na GUI runu.
  </done>
</task>

</tasks>

<verification>
- `RUSTC_WRAPPER= cargo test settings::tests -- --nocapture` je zeleny.
- `RUSTC_WRAPPER= cargo check` je zeleny.
- `rg -n "LightVariant::(WarmIvory|CoolGray|Sepia)" src/settings.rs` vrati vsechny tri vetve.
- `rg -n "panel_fill|window_fill|faint_bg_color" src/settings.rs` potvrdi explicitni mapovani.
</verification>

<success_criteria>
- `LITE-01`: WarmIvory ma `panel_fill` ~ `rgb(255,252,240)`.
- `LITE-02`: CoolGray ma `panel_fill` ~ `rgb(242,242,242)`.
- `LITE-03`: Sepia ma `panel_fill` ~ `rgb(240,230,210)`.
- `LITE-04`: Kazda varianta ma funkcni `faint_bg_color` pro odliseni panelu.
- Zmena je izolovana na `settings.rs` bez side efektu mimo theme model.
</success_criteria>

<output>
Po dokonceni vytvor `.planning/phases/03-light-varianty-settings-ui/03-01-SUMMARY.md`.
</output>
