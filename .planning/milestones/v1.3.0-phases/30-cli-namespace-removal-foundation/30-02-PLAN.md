---
phase: 30-cli-namespace-removal-foundation
plan: 02
type: execute
wave: 3
depends_on:
  - 30-01
  - 30-04
files_modified:
  - src/app/cli
  - src/app/mod.rs
  - src/app/ui/ai_panel.rs
autonomous: true
requirements:
  - CLI-01

must_haves:
  truths:
    - "`src/app/cli/*` je po tomto plánu fyzicky odstraněno z repozitáře."
    - "Hard removal proběhne až po green stavu migračních plánů (30-01, 30-04)."
    - "Nezůstanou orphan odkazy na CLI filesystémovou vrstvu v modulech a include cestách."
  artifacts:
    - path: "src/app/mod.rs"
      provides: "Absence legacy `mod cli` napojení"
      contains: "pub mod ai_core"
    - path: "src/app/ui/ai_panel.rs"
      provides: "UI entrypoint bez závislosti na legacy CLI manageru"
      contains: "use crate::app::ai_core"
  key_links:
    - from: "Hard removal pass"
      to: "CLI-01 acceptance"
      via: "Delete `src/app/cli` + compile gate"
      pattern: "cargo check"
---

<objective>
Provést řízené fyzické odstranění `src/app/cli/*` po úspěšné migraci importů,
aby repozitář splnil CLI-01 bez neplatných modulových vazeb.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-cli-namespace-removal-foundation/30-CONTEXT.md
@.planning/phases/30-cli-namespace-removal-foundation/30-RESEARCH.md
@.planning/phases/30-cli-namespace-removal-foundation/30-VALIDATION.md
@./CLAUDE.md
@.planning/phases/30-cli-namespace-removal-foundation/30-01-PLAN.md
@.planning/phases/30-cli-namespace-removal-foundation/30-04-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pre-removal readiness gate</name>
  <files>src/app/mod.rs, src/app/ui/ai_panel.rs</files>
  <action>
    1. Potvrď, že po 30-01 a 30-04 je build zelený a kritické call-site neodkazují na `app::cli`.
    2. Doplň poslední nutné přepojení, které blokuje fyzické smazání CLI stromu.
    3. Pokud readiness gate selže, oprav v rámci tohoto plánu před hard removal.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>
    Projekt je technicky připravený na smazání `src/app/cli/*`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hard removal `src/app/cli/*`</name>
  <files>src/app/cli, src/app/mod.rs</files>
  <action>
    1. Smaž celý strom `src/app/cli/*` včetně `mod.rs` a dílčích modulů.
    2. Odstraň include/reference body, které by po smazání způsobily kompilaci na neexistující soubory.
    3. Zkontroluj, že po odstranění neexistují fallback aliasy vracející starý namespace.
  </action>
  <verify>
    <automated>test ! -d src/app/cli</automated>
  </verify>
  <done>
    Legacy CLI strom je fyzicky pryč a repozitář plní CLI-01.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build gate po hard removal</name>
  <files>src/</files>
  <action>
    1. Spusť `cargo check` bez `src/app/cli/*` a oprav případné orphan reference.
    2. Spusť grep na přímé odkazování legacy namespace a souborů (`app::cli`, `src/app/cli`).
    3. Zapiš důkaz splnění CLI-01 do phase summary/verification artefaktů.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>
    Hard removal je build-stable a průkazně dokončený.
  </done>
</task>

</tasks>

<verification>
test ! -d src/app/cli
cargo check
rg -n "app::cli|src/app/cli" src
</verification>

<success_criteria>
- `src/app/cli/*` neexistuje v pracovním stromu
- Projekt kompiluje po fyzickém odstranění legacy CLI vrstvy
- Nejsou orphan reference na smazanou složku/moduly
- CLI-01 je splněno průkazně (filesystem + build)
</success_criteria>
