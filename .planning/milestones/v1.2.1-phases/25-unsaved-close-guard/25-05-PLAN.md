---
phase: 25-unsaved-close-guard
plan: 05
type: execute
wave: 5
depends_on:
  - 25-04
files_modified:
  - src/app/ui/workspace/tests/unsaved_close_guard.rs
autonomous: true
requirements:
  - GUARD-01
  - GUARD-02
  - GUARD-03
  - GUARD-04

must_haves:
  truths:
    - "Testy pokrývají root close flow při dirty tabs a sekvenční průchod více workspace front."
    - "`Cancel` v guard flow zastaví celou close operaci bez uzavření dalších položek queue."
    - "Save-fail branch má explicitní coverage, která potvrzuje zachování otevřeného flow na stejném tabu."
  artifacts:
    - path: "src/app/ui/workspace/tests/unsaved_close_guard.rs"
      provides: "Scénáře root flow + cancel stop + save-fail guard větve"
      contains: "unsaved_close_guard_root_flow"
  key_links:
    - from: "Root guard orchestrace"
      to: "automatizované regresní testy"
      via: "unsaved_close_guard test suite"
      pattern: "GUARD-01..04"
---

<objective>
Dodat cílené regresní testy, které potvrzují konzistenci unsaved close guard chování
po napojení root close orchestrace.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-unsaved-close-guard/25-CONTEXT.md
@.planning/phases/25-unsaved-close-guard/25-RESEARCH.md
@.planning/phases/25-unsaved-close-guard/25-VALIDATION.md
@./CLAUDE.md
@src/app/ui/workspace/tests/unsaved_close_guard.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Root flow + cancel stop test coverage</name>
  <files>src/app/ui/workspace/tests/unsaved_close_guard.rs</files>
  <action>
    1. Doplň/aktualizuj test scénáře pro `Quit`, `Close Project` a window close trigger při dirty stavech.
    2. Ověř sekvenční průchod více workspace queue a stop celé operace při `Cancel`.
    3. Zachovej čitelné fixture setupy, aby testy byly stabilní a deterministické.
  </action>
  <verify>
    <automated>cargo test unsaved_close_guard_root_flow -- --nocapture</automated>
  </verify>
  <done>
    Root close guard scénáře mají spolehlivou automatizovanou coverage.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Save-fail branch regression test</name>
  <files>src/app/ui/workspace/tests/unsaved_close_guard.rs</files>
  <action>
    1. Přidej test, který při save failu během close flow ověří zachování stejné queue položky.
    2. Potvrď, že po save failu je možné pokračovat volbou `Discard` nebo ukončit flow přes `Cancel`.
    3. Ověř, že save fail nevynutí tiché uzavření žádného dirty tabu.
  </action>
  <verify>
    <automated>cargo test unsaved_close_guard_save_fail -- --nocapture</automated>
  </verify>
  <done>
    Save-fail guard větev je krytá regresním testem.
  </done>
</task>

</tasks>

<verification>
cargo check
cargo test unsaved_close_guard -- --nocapture
</verification>

<success_criteria>
- Existují testy pro root close flow, sekvenční queue a `Cancel` stop
- Save-fail branch má explicitní regresní coverage
- Test suite validuje konzistenci GUARD-01..04 po root integraci
</success_criteria>
