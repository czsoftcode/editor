---
phase: 25-unsaved-close-guard
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/ui/workspace/mod.rs
  - src/app/ui/editor/render/normal.rs
autonomous: true
gap_closure: true
requirements:
  - GUARD-01
  - GUARD-03

must_haves:
  truths:
    - "Ctrl+W nad editorem neprovádí editaci textu a pouze spouští close guard flow."
    - "Při aktivním unsaved guard dialogu editor nepřijímá write shortcuty ani edit input."
    - "Close guard flow se nespouští re-entrantně z duplicitních shortcut eventů."
  artifacts:
    - path: "src/app/ui/workspace/mod.rs"
      provides: "Spotřebování Ctrl+W shortcutu a guard-aware dialog/input lock"
      contains: "consume_shortcut/consume_key + dialog_open_base guard"
    - path: "src/app/ui/editor/render/normal.rs"
      provides: "Respektování input locku editoru při aktivním guard flow"
      contains: "podmínka blokující editor input"
  key_links:
    - from: "workspace shortcut handler"
      to: "editor TextEdit"
      via: "consumed keyboard event"
      pattern: "Ctrl+W"
---

<objective>
Opravit regresi, kdy Ctrl+W během editace maže text, a zajistit, že při otevřeném
unsaved close guard dialogu není možné dále editovat obsah souboru.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/25-unsaved-close-guard/25-UAT.md
@.planning/debug/ctrl-w-unsaved-guard-mutates-content.md
@src/app/ui/workspace/mod.rs
@src/app/ui/editor/render/normal.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Spotřebování Ctrl+W shortcutu ve workspace vrstvě</name>
  <files>src/app/ui/workspace/mod.rs</files>
  <action>
    1. V shortcut handleru nahraď pasivní čtení kláves za `consume_shortcut`/`consume_key` pro Ctrl+W.
    2. Ujisti se, že close request proběhne pouze jednou na stisk.
    3. Zachovej existující větvení pro clean tab (okamžité zavření bez dialogu).
  </action>
  <verify>
    <automated>cargo test unsaved_close_guard_ctrl_w_consumes_shortcut -- --nocapture</automated>
  </verify>
  <done>
    Ctrl+W již nepropadá do editoru a nespouští nechtěné mazání znaků.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Globální input lock editoru během aktivního guard flow</name>
  <files>src/app/ui/workspace/mod.rs, src/app/ui/editor/render/normal.rs</files>
  <action>
    1. Rozšiř `dialog_open_base` (nebo ekvivalent) o stav aktivního unsaved guard flow.
    2. V renderu editoru respektuj tento lock a nepouštěj edit input, dokud je guard otevřený.
    3. Ověř, že lock neblokuje čtení stavu potřebné pro vykreslení a že nezasekne close flow.
  </action>
  <verify>
    <automated>cargo test unsaved_close_guard_input_lock -- --nocapture</automated>
  </verify>
  <done>
    Během guard dialogu editor pouze zobrazuje obsah, ale nereaguje na edit klávesy.
  </done>
</task>

</tasks>

<verification>
cargo check
cargo test unsaved_close_guard -- --nocapture
./check.sh
</verification>

<success_criteria>
- Ctrl+W v aktivním editoru nespustí editaci textu
- Guard flow je vyvolaný přes jeden konzistentní workspace vstup
- Při aktivním guard dialogu je editor input locknutý
</success_criteria>
