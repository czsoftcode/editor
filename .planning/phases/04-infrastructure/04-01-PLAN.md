---
phase: 04-infrastructure
plan: 01
type: feature
wave: 1
depends_on: []
files_modified:
  - src/settings.rs
  - src/app/ui/background.rs
  - src/app/ui/workspace/mod.rs
  - src/app/ui/editor/files.rs
  - src/app/ui/editor/ui.rs
  - src/app/ui/workspace/menubar/mod.rs
  - src/app/ui/workspace/modal_dialogs/conflict.rs
  - src/app/ui/workspace/modal_dialogs/settings.rs
  - src/app/ui/workspace/state/init.rs
  - src/app/ui/workspace/state/mod.rs
  - src/app/mod.rs
  - src/app/ui/terminal/mod.rs
  - src/app/ui/terminal/instance/mod.rs
  - src/app/ui/terminal/bottom/build_bar.rs
  - locales/cs/ui.ftl
  - locales/en/ui.ftl
autonomous: false
requirements: [SETT-01, SETT-02, SETT-03, SETT-04, SETT-05, TERM-01, TERM-02, TERM-03]

must_haves:
  truths:
    - "Settings obsahují nový boolean pro sandbox režim a je persistován do settings.toml."
    - "Přepínač v Settings > Projekt respektuje Save/Cancel semantiku a změna se projeví až po znovuotevření projektu."
    - "Při startu projektu se build_in_sandbox a file_tree_in_sandbox nastaví z uloženého sandbox režimu."
    - "Terminály používají správný cwd podle sandbox režimu a label odpovídá (ON: Sandbox, OFF: Terminal + cesta)."
    - "Runtime UI nečte `settings.sandbox_mode` přímo; změna se uplatní jen přes init (apply on reopen)."
  artifacts:
    - path: "src/settings.rs"
      provides: "Pole a persistování sandbox režimu (settings.toml) + testy persistence."
      contains: "sandbox"
    - path: "src/app/ui/workspace/modal_dialogs/settings.rs"
      provides: "UI přepínač + tooltip + inline info o reopen + toasty + save handler." 
      contains: "Režim sandboxu"
    - path: "src/app/ui/workspace/state/init.rs"
      provides: "Aplikace flagů build_in_sandbox / file_tree_in_sandbox při initu."
      contains: "build_in_sandbox"
    - path: "src/app/ui/terminal/mod.rs"
      provides: "CWD a label terminálů podle sandbox režimu."
      contains: "Terminal"
  key_links:
    - from: "Settings Save"
      to: "settings.toml"
      via: "persist sandbox_mode"
      pattern: "settings.toml"
    - from: "Workspace init"
      to: "build_in_sandbox / file_tree_in_sandbox"
      via: "settings.sandbox"
      pattern: "build_in_sandbox|file_tree_in_sandbox"
    - from: "sandbox OFF"
      to: "terminal cwd"
      via: "root project path"
      pattern: "current_dir|cwd|root"
---

<objective>
Zavést sandbox režim do Settings (persistovaný do settings.toml), přidat přepínač v Settings > Projekt s tooltipem a inline poznámkou o reopen, a při startu projektu nastavovat build_in_sandbox / file_tree_in_sandbox podle uloženého režimu. Terminály musí respektovat režim pro cwd i label.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-infrastructure/04-CONTEXT.md
@.planning/phases/04-infrastructure/04-RESEARCH.md
@.planning/phases/04-infrastructure/04-VALIDATION.md
@src/settings.rs
@src/app/ui/workspace/modal_dialogs/settings.rs
@src/app/ui/workspace/state/init.rs
@src/app/mod.rs
@src/app/ui/terminal/mod.rs
@src/app/ui/terminal/bottom/build_bar.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Settings model + persistování sandbox režimu + nahrazení Safe Mode</name>
  <files>src/settings.rs, src/app/ui/background.rs, src/app/ui/workspace/mod.rs, src/app/ui/editor/files.rs, src/app/ui/editor/ui.rs, src/app/ui/workspace/menubar/mod.rs, src/app/ui/workspace/modal_dialogs/conflict.rs</files>
  <action>
    1. Přidej boolean pole pro sandbox režim do Settings structu (výchozí hodnota ON).
    2. Zajisti, že se hodnota ukládá do settings.toml a čte při loadu (settings.json zůstává legacy read-only vstup).
    3. Slouč Safe Mode (project_read_only) do sandbox režimu: zruš samostatnou persistenci a odvozuj jej ze sandbox režimu při migraci.
    4. Nahraď všechny call-site `settings.project_read_only` za nový zdroj pravdy (sandbox režim) podle logiky: sandbox ON = read-only sandbox, sandbox OFF = práce v rootu (read/write).
    5. Přidej unit test: round-trip save/load ověřuje, že `sandbox_mode` se persistuje a legacy `project_read_only` se mapuje správně.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor/.polycredo/sandbox && RUSTC_WRAPPER= cargo test settings -- --nocapture</automated>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor/.polycredo/sandbox && RUSTC_WRAPPER= cargo check</automated>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor/.polycredo/sandbox && ./check.sh</automated>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor/.polycredo/sandbox && rg -n "project_read_only" src || true</automated>
  </verify>
  <done>
    Settings obsahují sandbox režim, persistuje se do settings.toml, legacy migrace je bez regresí a všechna použití Safe Mode jsou převedena na sandbox režim.
  </done>
</task>

<task type="auto">
  <name>Task 2: UI přepínač v Settings + lokalizace + tooltip</name>
  <files>src/app/ui/workspace/modal_dialogs/settings.rs, locales/cs/ui.ftl, locales/en/ui.ftl</files>
  <action>
    1. Přidej přepínač „Režim sandboxu / Sandbox mode“ do Settings > Projekt s tooltipem (info ikona).
    2. Tooltip text popíše OFF režim jen v rozsahu fáze 4: práce v rootu, terminály v rootu, změna se projeví po znovuotevření projektu.
    3. Tooltip výslovně NEzmiňuje skrytí sandbox UI prvků ani vypnutí sync (to patří do Phase 5).
    4. Inline poznámka pod přepínačem: změna se projeví po znovuotevření projektu.
    5. Přepínač respektuje Save/Cancel; změny se neaplikují live.
    6. Uložení (Save) explicitně persistuje změnu `sandbox_mode` do settings.toml (nezávisle na theme změnách).
    7. Přidej toasty: jednorázový toast při prvním vypnutí sandboxu a krátký toast při znovuzapnutí. „Jednorázovost“ drž jen v rámci session (nepersistovat).
  </action>
  <verify>
    <manual>
      1) Otevři Settings > Projekt.
      2) Přepni sandbox OFF, zruš (Cancel) a ověř, že nastavení zůstalo.
      3) Přepni sandbox OFF, Save, ověř toast a zápis do settings.toml.
    </manual>
  </verify>
  <done>
    Přepínač je lokalizovaný, má tooltip + inline poznámku a respektuje Save/Cancel; změny se ukládají do settings.toml; toasty se zobrazují dle kontextu.
  </done>
</task>

<task type="auto">
  <name>Task 3: Aplikace sandbox režimu při startu projektu + guard na apply-on-reopen</name>
  <files>src/app/ui/workspace/state/init.rs, src/app/mod.rs, src/app/ui/terminal/mod.rs, src/app/ui/terminal/bottom/build_bar.rs</files>
  <action>
    1. Při initu workspace nastav `build_in_sandbox` a `file_tree_in_sandbox` podle uloženého sandbox režimu.
    2. Zajisti, že runtime UI nečte `settings.sandbox_mode` přímo (terminály/build bar mají používat stabilní flagy z `WorkspaceState`).
    3. Zajisti, že změna režimu se projeví až po znovuotevření projektu (žádné live přepínání z modal okna).
    4. Přidej jednoduchý guard, aby se hodnoty nenastavovaly z preview stavu v Settings.
  </action>
  <verify>
    <manual>
      1) Nastav sandbox OFF a ulož.
      2) Znovu otevři projekt.
      3) Ověř, že build_in_sandbox a file_tree_in_sandbox odpovídají OFF.
    </manual>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor/.polycredo/sandbox && rg -n "settings\.sandbox_mode" src/app | rg -v "state/init.rs|modal_dialogs/settings.rs"</automated>
  </verify>
  <done>
    Start projektu čte sandbox režim z settings a runtime UI používá stabilní flagy; apply-on-reopen je vynuceno.
  </done>
</task>

<task type="auto">
  <name>Task 4: Terminály v rootu + labely</name>
  <files>src/app/ui/terminal/mod.rs, src/app/ui/terminal/instance/mod.rs, src/app/ui/terminal/bottom/build_bar.rs, locales/cs/ui.ftl, locales/en/ui.ftl</files>
  <action>
    1. Při sandbox OFF nastav cwd terminálů na root projektu (AI i Build).
    2. Label: sandbox ON = "Sandbox"; OFF = "Terminal — <short path>".
    3. Přidej krátkou poznámku (UI text) že po změně režimu se terminálové procesy při reopen znovu spustí.
  </action>
  <verify>
    <manual>
      1) Nastav sandbox OFF a reopen projekt.
      2) Ověř, že terminály běží v rootu a label obsahuje cestu.
      3) Nastav sandbox ON a ověř label "Sandbox".
    </manual>
  </verify>
  <done>
    Terminály respektují sandbox režim pro cwd i labely.
  </done>
</task>

</tasks>

<verification>
- `RUSTC_WRAPPER= cargo test settings -- --nocapture`
- `RUSTC_WRAPPER= cargo check`
- `./check.sh`
- `rg -n "project_read_only" src` vrátí 0 výsledků
- `rg -n "settings\.sandbox_mode" src/app` vrací pouze init + settings modal
- Settings UI: přepínač, tooltip a inline poznámka jsou lokalizované v cs/en
- Tooltip nezmiňuje UI/sync (Phase 5)
- Toggle se projeví až po reopen projektu
- Terminály mají správný cwd a label podle režimu
</verification>

<success_criteria>
- `SETT-01`: přepínač je v Settings > Projekt
- `SETT-02`: sandbox režim persistuje v settings.toml
- `SETT-03`: build_in_sandbox je false při OFF
- `SETT-04`: file_tree_in_sandbox je false při OFF
- `SETT-05`: Safe Mode je sloučen do sandbox režimu
- `TERM-01..TERM-03`: terminály běží v rootu při OFF a mají správný label
</success_criteria>
