---
phase: 20-gsd-core-state-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/ui/terminal/ai_chat/gsd/mod.rs
  - src/app/ui/terminal/ai_chat/gsd/config.rs
  - src/app/ui/terminal/ai_chat/gsd/paths.rs
  - src/app/ui/terminal/ai_chat/slash.rs
  - src/app/ui/terminal/ai_chat/mod.rs
autonomous: true
requirements:
  - CORE-03
  - CORE-04
  - CORE-05

must_haves:
  truths:
    - "User types /gsd and sees GSD help with list of subcommands"
    - "User types /gsd config get <key> and sees the config value from .planning/config.json"
    - "User types /gsd config set <key> <value> and config.json is updated on disk"
    - "When .planning/ directory is missing, GSD commands show friendly message instead of crashing"
    - "Path helpers resolve phase directories, generate slugs, and handle decimal phase numbering"
  artifacts:
    - path: "src/app/ui/terminal/ai_chat/gsd/mod.rs"
      provides: "GSD subcommand dispatch, help text, check_planning_dir guard"
      exports: ["cmd_gsd", "matching_subcommands"]
    - path: "src/app/ui/terminal/ai_chat/gsd/config.rs"
      provides: "GsdConfig with load/get/set/save, cmd_config slash handler"
      contains: "pub struct GsdConfig"
    - path: "src/app/ui/terminal/ai_chat/gsd/paths.rs"
      provides: "Path helpers, slug generation, phase numbering utilities"
      contains: "pub fn phase_dir"
  key_links:
    - from: "src/app/ui/terminal/ai_chat/slash.rs"
      to: "src/app/ui/terminal/ai_chat/gsd/mod.rs"
      via: "match branch 'gsd' => gsd::cmd_gsd(ws, args)"
      pattern: "gsd::cmd_gsd"
    - from: "src/app/ui/terminal/ai_chat/gsd/config.rs"
      to: ".planning/config.json"
      via: "serde_json read/write"
      pattern: "serde_json::from_str"
---

<objective>
Create the GSD module skeleton with dispatch, config management, and path helpers.

Purpose: Establishes the GSD command entry point in the slash system, wires up subcommand routing, and provides config.json management and path utilities that all GSD commands will use. Runs in parallel with Plan 01 (frontmatter parser) since these have no file overlap.

Output: Working `/gsd`, `/gsd help`, `/gsd config get/set` commands, path helper utilities with tests.
</objective>

<execution_context>
@/home/stkremen/.claude/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-gsd-core-state-engine/20-CONTEXT.md
@.planning/phases/20-gsd-core-state-engine/20-RESEARCH.md

<interfaces>
<!-- Existing slash infrastructure to integrate with -->

From src/app/ui/terminal/ai_chat/slash.rs:
```rust
pub const SYSTEM_MSG_MARKER: &str = "\x00SYS\x00";

pub enum SlashResult {
    Immediate(String),
    Async { placeholder: String },
    Silent,
    NotACommand,
}

struct SlashCommand {
    name: &'static str,
    description: &'static str,
}

const COMMANDS: &[SlashCommand] = &[
    SlashCommand { name: "help", description: "Show available commands" },
    // ... 6 more commands
];

pub fn matching_commands(filter: &str) -> Vec<(&'static str, &'static str)> { ... }

// Dispatch match block (line 68-80) — add "gsd" branch here
```

From src/app/ui/workspace/state/mod.rs:
```rust
pub slash_autocomplete: SlashAutocomplete,
pub root_path: std::path::PathBuf,
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GSD dispatch module, config, and path helpers</name>
  <files>
    src/app/ui/terminal/ai_chat/gsd/mod.rs,
    src/app/ui/terminal/ai_chat/gsd/config.rs,
    src/app/ui/terminal/ai_chat/gsd/paths.rs
  </files>
  <behavior>
    - cmd_gsd("", ws) returns help text listing all GSD subcommands (state, progress, config, help)
    - cmd_gsd("help", ws) returns same help text
    - cmd_gsd("state", ws) returns placeholder "Not implemented" (wired in Plan 03)
    - cmd_gsd("unknown_cmd", ws) returns error with suggestion to use /gsd help
    - check_planning_dir returns Some(SlashResult) when .planning/ missing, None when present
    - GsdConfig::load reads .planning/config.json, returns empty object if file missing
    - GsdConfig::get("key") returns value, get("nested.key") traverses 2 levels
    - GsdConfig::set("key", value) + save() writes to disk
    - cmd_config(ws, "get workflow.auto_advance") returns the value
    - cmd_config(ws, "set workflow.auto_advance true") updates config.json
    - phase_dir(root, 20, "gsd-core") returns root/.planning/phases/20-gsd-core
    - slugify("GSD Core + State Engine") returns "gsd-core-state-engine"
    - phase_number_str(20) returns "20", phase_number_str(20.1) returns "20.1"
    - matching_subcommands("st") returns [("state", "...")]
  </behavior>
  <action>
    Create `src/app/ui/terminal/ai_chat/gsd/` directory with three files:

    **mod.rs**: GSD subcommand dispatch.
    - Import SlashResult from super::slash
    - Define GSD_SUBCOMMANDS static slice (similar to COMMANDS in slash.rs): state, progress, config, help
    - `pub fn cmd_gsd(ws: &mut WorkspaceState, args: &str) -> SlashResult` — parse first word as subcommand, rest as sub_args. Guard with check_planning_dir for all except "help".
    - `fn check_planning_dir(root: &Path) -> Option<SlashResult>` — check root/.planning/ exists
    - `fn cmd_gsd_help() -> SlashResult` — markdown table of GSD subcommands
    - `pub fn matching_subcommands(filter: &str) -> Vec<(&'static str, &'static str)>` — for autocomplete
    - Placeholder match arms for "state" and "progress" returning "Not yet implemented" (wired in Plan 03)
    - Declare `pub mod frontmatter; pub mod config; pub mod paths; pub mod state;` (state module created in Plan 03)

    **config.rs**: Config JSON management.
    - `pub struct GsdConfig { pub value: serde_json::Value, pub path: PathBuf }`
    - `GsdConfig::load(root: &Path) -> Self` — read .planning/config.json, fallback to empty object
    - `GsdConfig::get(&self, dot_path: &str) -> Option<&serde_json::Value>` — split('.') traversal, max 2 levels
    - `GsdConfig::set(&mut self, dot_path: &str, val: serde_json::Value)` — create intermediate objects if needed
    - `GsdConfig::save(&self) -> Result<(), String>` — create_dir_all + write pretty JSON
    - `pub fn cmd_config(ws: &mut WorkspaceState, args: &str) -> SlashResult` — parse "get <key>" / "set <key> <value>" / "" (show all)
    - For "set" value parsing: try parse as bool/int/float, fallback to string
    - Unit tests with tempdir

    **paths.rs**: Path utilities.
    - `pub fn planning_dir(root: &Path) -> PathBuf` — root/.planning
    - `pub fn phase_dir(root: &Path, number: &str, slug: &str) -> PathBuf` — root/.planning/phases/{number}-{slug}
    - `pub fn slugify(name: &str) -> String` — lowercase, replace non-alphanum with '-', collapse multiple '-', trim '-'
    - `pub fn state_path(root: &Path) -> PathBuf` — root/.planning/STATE.md
    - `pub fn roadmap_path(root: &Path) -> PathBuf` — root/.planning/ROADMAP.md
    - Unit tests for all helpers
  </action>
  <verify>
    <automated>cargo test --lib gsd -- --nocapture</automated>
  </verify>
  <done>
    - /gsd and /gsd help show subcommand list
    - /gsd config get/set reads and writes config.json with dot-notation
    - check_planning_dir returns friendly message when .planning/ missing
    - Path helpers resolve correct paths, slugify works
    - All unit tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire GSD into slash dispatch and autocomplete</name>
  <files>
    src/app/ui/terminal/ai_chat/slash.rs,
    src/app/ui/terminal/ai_chat/mod.rs,
    src/app/ui/terminal/ai_chat/render.rs
  </files>
  <action>
    **slash.rs changes:**
    - Add `mod gsd;` declaration (or add it in ai_chat/mod.rs depending on module structure)
    - Add to COMMANDS array: `SlashCommand { name: "gsd", description: "GSD project management (type /gsd help for subcommands)" }`
    - Add match branch in dispatch(): `"gsd" => gsd::cmd_gsd(ws, args)`
    - Update test_matching_commands test: change expected count from 7 to 8
    - Update test_dispatch_help: add assertion for /gsd in help output

    **mod.rs changes:**
    - Add `pub mod gsd;` if the module declaration belongs here (check existing module pattern — slash.rs is declared here)

    **render.rs autocomplete extension:**
    - In the autocomplete section (around line 410-491), detect when the prompt starts with `/gsd `:
      - Extract the filter text after `/gsd ` (e.g., from `/gsd st` extract `st`)
      - Call `gsd::matching_subcommands(filter)` instead of `slash::matching_commands(filter)`
      - When a subcommand is selected, replace prompt with `/gsd {subcommand} ` (with trailing space)
    - Keep existing behavior unchanged for non-`/gsd` prefixed prompts
  </action>
  <verify>
    <automated>cargo test --lib slash -- --nocapture && cargo test --lib gsd -- --nocapture</automated>
  </verify>
  <done>
    - /gsd dispatches to GSD module from slash.rs
    - /gsd appears in /help output
    - Autocomplete shows GSD subcommands when typing /gsd followed by space
    - All existing slash tests still pass (updated counts)
    - cargo build succeeds with no warnings in gsd module
  </done>
</task>

</tasks>

<verification>
cargo test --lib slash -- --nocapture
cargo test --lib gsd -- --nocapture
cargo build 2>&1 | grep -E "^error" | head -5
</verification>

<success_criteria>
- /gsd and /gsd help display subcommand list in chat
- /gsd config get/set manages .planning/config.json with dot-notation
- Missing .planning/ shows friendly message (not crash)
- Path helpers correctly resolve .planning/ paths and generate slugs
- Autocomplete works for /gsd subcommands
- All tests pass, no compiler warnings in gsd module
</success_criteria>

<output>
After completion, create `.planning/phases/20-gsd-core-state-engine/20-02-SUMMARY.md`
</output>
