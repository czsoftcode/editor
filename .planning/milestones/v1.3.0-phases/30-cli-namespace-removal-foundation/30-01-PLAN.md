---
phase: 30-cli-namespace-removal-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/mod.rs
  - src/app/ai_core/mod.rs
  - src/app/ai_core/types.rs
  - src/settings.rs
  - src/app/types.rs
  - src/app/ui/workspace/state/mod.rs
  - src/app/ui/workspace/state/init.rs
autonomous: true
requirements:
  - CLI-02

must_haves:
  truths:
    - "V `src/app/mod.rs` je root namespace přepnutý na `pub mod ai_core;` bez compat aliasu `cli`."
    - "Core state/settings/type vrstva kompiluje bez importů `crate::app::cli::*`."
    - "Nový `app::ai_core` exportuje minimální compile-first public surface pro další migration wave."
  artifacts:
    - path: "src/app/mod.rs"
      provides: "Root namespace switch z cli na ai_core"
      contains: "pub mod ai_core;"
    - path: "src/app/ai_core/mod.rs"
      provides: "Vstupní modul pro nový AI runtime namespace"
      contains: "pub mod"
  key_links:
    - from: "settings/types/workspace-state"
      to: "app::ai_core"
      via: "Přímá náhrada importů `crate::app::cli::*` v low-risk call-site vrstvě"
      pattern: "use crate::app::ai_core"
---

<objective>
Založit `app::ai_core` a převést low-risk základ (settings/types/workspace state),
aby byla připravená bezpečná migrace AI terminal UI a následný hard removal staré CLI vrstvy.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-cli-namespace-removal-foundation/30-CONTEXT.md
@.planning/phases/30-cli-namespace-removal-foundation/30-RESEARCH.md
@.planning/phases/30-cli-namespace-removal-foundation/30-VALIDATION.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Namespace bootstrap `app::ai_core` + root module switch</name>
  <files>src/app/mod.rs, src/app/ai_core/mod.rs, src/app/ai_core/types.rs</files>
  <action>
    1. Založ `src/app/ai_core/` a připrav minimální modulový surface pro compile-critical typy/runtime části.
    2. V `src/app/mod.rs` přepni root export z `pub mod cli;` na `pub mod ai_core;` bez dočasného aliasu.
    3. Potvrď, že scope zůstává foundation-only (bez přidávání nových feature).
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>
    `app::ai_core` existuje a může být použit v navazujících migračních plánech.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Import migration pass pro settings/types/workspace-state</name>
  <files>src/settings.rs, src/app/types.rs, src/app/ui/workspace/state/mod.rs, src/app/ui/workspace/state/init.rs</files>
  <action>
    1. Nahraď importy `crate::app::cli::*` za `crate::app::ai_core::*` v low-risk call-site vrstvě.
    2. Udrž compile-first baseline bez zásahu do AI terminal UI scope locku.
    3. Po změnách proveď průběžný `cargo check` a oprav chyby in-place.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>
    Základní vrstva je odpojená od `app::cli` a build zůstává zelený.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Audit evidence pro CLI-02 (foundation subset)</name>
  <files>src/settings.rs, src/app/types.rs, src/app/ui/workspace/state/mod.rs, src/app/ui/workspace/state/init.rs</files>
  <action>
    1. Proveď cílený grep audit legacy importů v této subset vrstvě.
    2. Pokud zůstane reference, oprav ji v rámci tohoto plánu.
    3. Zapiš průběžný výsledek do phase summary artefaktu.
  </action>
  <verify>
    <automated>rg -n "crate::app::cli|app::cli" src/settings.rs src/app/types.rs src/app/ui/workspace/state/mod.rs src/app/ui/workspace/state/init.rs</automated>
  </verify>
  <done>
    Foundation subset je průkazně bez `app::cli` importů.
  </done>
</task>

</tasks>

<verification>
cargo check
rg -n "crate::app::cli|app::cli" src/settings.rs src/app/types.rs src/app/ui/workspace/state/mod.rs src/app/ui/workspace/state/init.rs
</verification>

<success_criteria>
- `src/app/mod.rs` exportuje `app::ai_core` místo `app::cli`
- settings/types/workspace-state subset kompiluje bez `crate::app::cli::*`
- `cargo check` je zelený po foundation migration kroku
- CLI-02 je průběžně pokryto pro foundation subset
</success_criteria>
