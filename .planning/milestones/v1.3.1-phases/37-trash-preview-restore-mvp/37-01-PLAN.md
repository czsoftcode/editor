---
phase: 37-trash-preview-restore-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/trash.rs
  - src/app/project_config.rs
  - tests/phase37_restore_engine.rs
autonomous: true
requirements:
  - TRASHUI-01
  - RESTORE-01

must_haves:
  truths:
    - "Trash preview cte list polozek z `.polycredo/trash` pres stabilni engine API, ne z UI heuristik."
    - "Restore jedne polozky vraci data na puvodni cestu, pokud neni konflikt."
    - "Restore vytvari chybejici parent adresare a zustava fail-closed pri chybe."
    - "Metadata chyby jsou explicitni a vedou na citelny error kontrakt pro UI toast."
  artifacts:
    - path: "src/app/trash.rs"
      provides: "List + restore engine API a metadata kontrakty pro preview/restore"
      contains: "list_trash_entries|restore_from_trash|TrashRestoreOutcome"
    - path: "src/app/project_config.rs"
      provides: "Stabilni helpery cest pro trash root a relativni mapovani"
      contains: "project_trash_dir"
    - path: "tests/phase37_restore_engine.rs"
      provides: "Req-level dukaz pro preview-list a restore happy path"
      contains: "phase37_list_trash_entries|phase37_restore_to_original_path|phase37_restore_creates_parent_dirs"
  key_links:
    - from: "Trash list API"
      to: "TRASHUI-01"
      via: "Preview modal dostava deterministicky seznam trash polozek s metadaty"
      pattern: "phase37_list_trash_entries"
    - from: "Single-item restore path"
      to: "RESTORE-01"
      via: "Polozka se presune z trash na puvodni cestu bez konfliktu"
      pattern: "phase37_restore_to_original_path"
---

<objective>
Pripravit backend kontrakty pro preview + restore MVP tak, aby UI vrstva mohla bez blokace vlakna
spolehlive listovat trash obsah a obnovit jednu polozku na puvodni cestu.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-trash-preview-restore-mvp/37-CONTEXT.md
@.planning/phases/37-trash-preview-restore-mvp/37-RESEARCH.md
@./AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Engine API pro listovani trash preview</name>
  <files>src/app/trash.rs, tests/phase37_restore_engine.rs</files>
  <action>
    1. Dodej public API pro listovani trash polozek vratane metadata potrebnych pro preview.
    2. Zajisti razeni "nejnovejsi prvni" podle metadata casu smazani.
    3. Osetri nevalidni/chybejici metadata jako explicitni stav (bez panic) pouzitelny v UI.
    4. Pridat focused testy pro listovani, razeni a metadata error path.
  </action>
  <verify>
    <automated>cargo test phase37_list_trash_entries -- --nocapture && cargo test phase37_list_invalid_metadata -- --nocapture</automated>
  </verify>
  <done>
    Trash preview ma stabilni backend kontrakt pro zobrazeni seznamu polozek.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Single-item restore happy path + parent create</name>
  <files>src/app/trash.rs, src/app/project_config.rs, tests/phase37_restore_engine.rs</files>
  <action>
    1. Implementuj restore jedne polozky z trash na puvodni cestu pri stavu bez konfliktu.
    2. Pred restore vytvor chybejici parent adresare puvodni cesty.
    3. Vrat strukturovany outcome (restored_from/restored_to/original_target) pro navazny UI toast.
    4. Doplnenim testu over, ze po uspechu polozka zmizi z trash a je pritomna na cilove ceste.
  </action>
  <verify>
    <automated>cargo test phase37_restore_to_original_path -- --nocapture && cargo test phase37_restore_creates_parent_dirs -- --nocapture</automated>
  </verify>
  <done>
    RESTORE-01 engine kontrakt je implementovan a testove dolozen.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fail-closed restore error kontrakty</name>
  <files>src/app/trash.rs, tests/phase37_restore_engine.rs</files>
  <action>
    1. Osetri restore chyby (missing source, invalid metadata, i/o failure) bez destruktivniho fallbacku.
    2. Ujisti se, ze source data zustava nedotcena pri restore failu.
    3. Vrat konzistentni chyby mapovatelne na user-facing toast text.
  </action>
  <verify>
    <automated>cargo test phase37_restore_fail_closed -- --nocapture</automated>
  </verify>
  <done>
    Restore backend ma robustni fail-closed behavior pripraveny pro UI propagaci.
  </done>
</task>

</tasks>

<verification>
cargo test phase37_restore_engine -- --nocapture
cargo check
rg -n "list_trash_entries|restore_from_trash|TrashRestoreOutcome" src/app/trash.rs -S
</verification>

<verification_criteria>
- TRASHUI-01: backend list API poskytuje preview-ready data (nazev, typ, cas, puvodni cesta).
- RESTORE-01: restore vraci polozku na puvodni cestu a vytvari chybejici parent adresare.
- Error vetve jsou fail-closed a pripraveny pro navazne toast mapovani.
</verification_criteria>

<success_criteria>
- Wave 1 vytvori spustitelny backend zaklad bez scope driftu do modal UI detailu.
- Existuji focused test hooky pro preview list i restore happy/error path.
</success_criteria>

<output>
Po dokonceni vytvor `.planning/phases/37-trash-preview-restore-mvp/37-01-SUMMARY.md`.
</output>

