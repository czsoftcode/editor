---
phase: 02-terminal-git-barvy
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/app/ui/terminal/instance/mod.rs
  - src/app/ui/terminal/instance/render.rs
  - src/app/ui/terminal/instance/theme.rs
autonomous: true
requirements: [TERM-01, TERM-02, TERM-03, TERM-04]

must_haves:
  truths:
    - "Claude panel i Build terminál používají stejnou light-safe paletu v light mode; žádný default dark background `#181818` v light režimu"
    - "Přepnutí dark/light se projeví okamžitě bez restartu PTY procesu, protože theme se aplikuje při každém `Terminal::ui(...)` renderu"
    - "Scrollbar terminálu není hardcoded tmavý; track/thumb barvy se odvozují z aktivního tématu (idle + hover + drag)"
    - "Foreground a klíčové ANSI barvy v light mode mají dostatečný kontrast vůči světlému pozadí"
  artifacts:
    - path: "src/app/ui/terminal/instance/theme.rs"
      provides: "Resolver terminal theme (dark/light) + testovatelná definice palety"
      exports: ["terminal_theme_for_visuals", "terminal_palette"]
      contains: "pub(crate) fn terminal_theme_for_visuals"
    - path: "src/app/ui/terminal/instance/mod.rs"
      provides: "Runtime aplikace `TerminalView::set_theme(...)` pro oba terminály přes shared wrapper"
      contains: "set_theme(terminal_theme_for_visuals"
    - path: "src/app/ui/terminal/instance/render.rs"
      provides: "Theme-aware scrollbar color helpery bez hardcoded `0x18`"
      exports: ["scrollbar_track_color", "scrollbar_thumb_color"]
      contains: "scrollbar_thumb_color"
  key_links:
    - from: "Terminal::ui()"
      to: "egui_term::TerminalView"
      via: "set_theme(terminal_theme_for_visuals(ui.visuals()))"
      pattern: "set_theme\\(terminal_theme_for_visuals"
    - from: "draw_scrollbar()"
      to: "ui.visuals()"
      via: "scrollbar color helpery"
      pattern: "scrollbar_(track|thumb)_color"
    - from: "Terminal wrapper"
      to: "Claude + Build panel"
      via: "shared `Terminal::ui(...)`"
      pattern: "terminal\\.ui\\("
---

<objective>
Zavést plně theme-aware rendering terminálu tak, aby v light mode nebyl tmavý background ani tmavý scrollbar, a přepínání tématu fungovalo za běhu bez restartu backendu.

Purpose: Splnit `TERM-01..TERM-04` na sdílené vrstvě terminálu (`instance`), takže změna pokryje Claude panel i Build terminál zároveň.
Output: `theme.rs` resolver + aplikace `set_theme(...)` v `Terminal::ui(...)` + scrollbar helpery napojené na aktivní téma.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-terminal-git-barvy/02-CONTEXT.md
@.planning/phases/02-terminal-git-barvy/02-RESEARCH.md
@.planning/phases/02-terminal-git-barvy/02-VALIDATION.md

<interfaces>
```rust
// src/app/ui/terminal/instance/mod.rs
// Aktuálně:
let terminal = TerminalView::new(ui, backend)
    .set_focus(focused && !self.exited)
    .set_font(term_font)
    .set_size(egui::Vec2::new(term_width, term_height));

// Cíl:
let terminal = TerminalView::new(ui, backend)
    .set_theme(terminal_theme_for_visuals(ui.visuals()))
    .set_focus(focused && !self.exited)
    .set_font(term_font)
    .set_size(...);
```

```rust
// src/app/ui/terminal/instance/render.rs
// Aktuálně hardcoded tmavé barvy:
egui::Color32::from_rgb(0x18, 0x18, 0x18)
egui::Color32::from_gray(90)
egui::Color32::from_gray(140)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — terminal theme resolver + unit testy</name>
  <files>src/app/ui/terminal/instance/theme.rs, src/app/ui/terminal/instance/mod.rs</files>
  <behavior>
    - `terminal_theme_for_visuals(visuals)` vrací explicitní light/dark `TerminalTheme`
    - light varianta má světlé pozadí a tmavý foreground; dark varianta zachovává současnou dark čitelnost
    - resolver je čistá funkce, testovatelná bez UI renderu
    - testy ověří klíčové kontrastní páry (background vs foreground, yellow/cyan čitelnost)
  </behavior>
  <action>
    1. Přidej nový soubor `src/app/ui/terminal/instance/theme.rs`:
       - helper `terminal_palette(dark_mode: bool) -> egui_term::ColorPalette`
       - helper `terminal_theme_for_visuals(visuals: &egui::Visuals) -> egui_term::TerminalTheme`
       - `#[cfg(test)]` testy pro light/dark větve

    2. V `src/app/ui/terminal/instance/mod.rs`:
       - registruj `pub mod theme;`
       - importuj `theme::terminal_theme_for_visuals`
       - přidej `.set_theme(terminal_theme_for_visuals(ui.visuals()))` do builderu `TerminalView`

    3. Zachovej kompatibilitu: žádná změna lifecycle backendu, žádný call `restart()` při změně tématu.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor/.polycredo/sandbox && RUSTC_WRAPPER= cargo test terminal_theme -- --nocapture</automated>
  </verify>
  <done>
    Resolver existuje, testy jsou zelené a `Terminal::ui(...)` vždy nastavuje theme přes `set_theme(...)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Theme-aware scrollbar + helper testy (TERM-03)</name>
  <files>src/app/ui/terminal/instance/render.rs</files>
  <behavior>
    - scrollbar track/thumb barvy vychází z aktivního tématu
    - hover/drag stav zvyšuje kontrast thumbu bez změny šířky scrollbaru
    - žádná hardcoded tmavá konstanta typu `0x18`/`from_gray(90|140)`
  </behavior>
  <action>
    1. V `render.rs` extrahuj helpery:
       - `scrollbar_track_color(visuals: &egui::Visuals) -> egui::Color32`
       - `scrollbar_thumb_color(visuals: &egui::Visuals, active: bool) -> egui::Color32`

    2. `draw_scrollbar(...)` používej helpery pro:
       - track fill
       - thumb idle
       - thumb hover/drag

    3. Doplň `#[cfg(test)]` testy:
       - dark vs light track barva
       - thumb active je kontrastnější než idle
       - light track není tmavší než editor panel background
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor/.polycredo/sandbox && RUSTC_WRAPPER= cargo test terminal_scrollbar</automated>
  </verify>
  <done>
    Scrollbar helpery jsou použité v renderu a testy potvrzují theme-aware chování v light/dark režimu.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrace + regresní kontrola obou terminálů (TERM-01, TERM-02, TERM-04)</name>
  <files>src/app/ui/terminal/instance/mod.rs, src/app/ui/terminal/right/mod.rs, src/app/ui/terminal/bottom/mod.rs</files>
  <action>
    1. Ověř, že Claude panel i Build terminál renderují přes stejnou cestu `Terminal::ui(...)` (bez duplicitního theme setupu).
    2. Pokud se v unfocused overlay používají hardcoded tmavé barvy, napoj je na theme helpery tak, aby v light mode nezhoršovaly čitelnost textu.
    3. Ujisti se, že změna tématu nevyvolá restart backendu (žádné nové volání `restart()` v apply flow).
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor/.polycredo/sandbox && RUSTC_WRAPPER= cargo check</automated>
    <manual>
      1) Spusť dlouhý příkaz v Claude panelu i Build terminálu.
      2) Přepni dark/light v Settings.
      3) Ověř: proces běží dál, background je světlý v light mode, text zůstává čitelný.
    </manual>
  </verify>
  <done>
    Oba terminály sdílí stejné theme chování, runtime switch je okamžitý a bez přerušení procesu.
  </done>
</task>

</tasks>

<verification>
- `RUSTC_WRAPPER= cargo test terminal_theme terminal_scrollbar` — helper testy zelené
- `RUSTC_WRAPPER= cargo check` — bez compile regresí
- `rg -n "set_theme\\(terminal_theme_for_visuals" src/app/ui/terminal/instance/mod.rs` — runtime theme apply existuje
- `rg -n "from_rgb\\(0x18, 0x18, 0x18\\)|from_gray\\(90\\)|from_gray\\(140\\)" src/app/ui/terminal/instance/render.rs` — hardcoded tmavé barvy odstraněny nebo omezeny mimo scrollbar path
- Manuální smoke: Claude + Build terminál čitelné v light mode bez restartu procesu
</verification>

<success_criteria>
- `TERM-01`: Claude panel má světlé pozadí a čitelný text v light mode
- `TERM-02`: Build terminál má stejné light chování jako Claude panel
- `TERM-03`: Scrollbar terminálu je odvozený z aktivního tématu, ne z hardcoded dark barev
- `TERM-04`: Foreground/ANSI výstup je čitelný na světlém pozadí
- `RUSTC_WRAPPER= cargo check` i relevantní testy jsou zelené
</success_criteria>

<output>
Po dokončení vytvoř `.planning/phases/02-terminal-git-barvy/02-01-SUMMARY.md`.
</output>
