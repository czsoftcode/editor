---
phase: 03-light-varianty-settings-ui
plan: 02
type: tdd
wave: 2
depends_on: [03-01]
files_modified:
  - src/app/ui/workspace/modal_dialogs/settings.rs
  - src/app/ui/workspace/state/mod.rs
  - src/app/ui/workspace/state/init.rs
  - locales/cs/ui.ftl
  - locales/en/ui.ftl
  - locales/de/ui.ftl
  - locales/sk/ui.ftl
  - locales/ru/ui.ftl
autonomous: true
requirements: [SETT-01, SETT-02]

must_haves:
  truths:
    - "Settings modal zobrazi volic light variant pouze kdyz `draft.dark_theme == false`; v dark mode je picker skryty."
    - "Picker je kartovy: nabizi presne 3 volby (WarmIvory, CoolGray, Sepia), kazda karta obsahuje barevny swatch + lokalizovany nazev + jasny selected state (ramecek + check)."
    - "Zmena dark/light nebo light varianty se projevi okamzite (live preview) pres update `AppShared.settings` + `settings_version` bez restartu aplikace."
    - "Live preview nepumpuje version kazdy frame; update bezi jen na realne zmene fingerprintu `(dark_theme, light_variant)`."
    - "Sekvence `Light -> Dark -> Light` zachova naposledy zvolenou light variantu (bez resetu na default)."
  artifacts:
    - path: "src/app/ui/workspace/modal_dialogs/settings.rs"
      provides: "Kartovy light-variant picker (swatch + nazev + selected state) + live-preview apply helpery navazane na draft controls."
      exports: ["show"]
      contains: "settings-light-variant"
    - path: "src/app/ui/workspace/state/mod.rs"
      provides: "State pole potrebne pro preview lifecycle (snapshot metadata)."
      contains: "settings_original"
    - path: "locales/en/ui.ftl"
      provides: "Nove i18n klice pro light variant picker."
      contains: "settings-light-variant"
  key_links:
    - from: "Theme controls v settings modalu"
      to: "AppShared.settings_version"
      via: "preview apply helper"
      pattern: "settings_version\.fetch_add"
    - from: "Dark/Light radio + variant picker"
      to: "shared settings"
      via: "changed() guard"
      pattern: "\.changed\(\)"
    - from: "Localization"
      to: "picker labels"
      via: "settings-light-variant-* klice"
      pattern: "settings-light-variant"
---

<objective>
Dodat Settings UI pro kartovy vyber light variant a live preview bez restartu, vcetne i18n labelu a guardu proti zbytecnym update stormum.

Purpose: Splnit SETT-01 a SETT-02 na existujicim `settings_draft` flow bez velkeho refaktoru.
Output: `settings.rs` modal UI rozsireny o conditional picker + runtime preview; doplnene locale klice.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-light-varianty-settings-ui/03-CONTEXT.md
@.planning/phases/03-light-varianty-settings-ui/03-RESEARCH.md
@.planning/phases/03-light-varianty-settings-ui/03-VALIDATION.md
@.planning/phases/03-light-varianty-settings-ui/03-01-PLAN.md
@src/app/ui/workspace/modal_dialogs/settings.rs
@src/app/ui/workspace/state/mod.rs
@src/app/ui/workspace/state/init.rs
@locales/cs/ui.ftl
@locales/en/ui.ftl
@locales/de/ui.ftl
@locales/sk/ui.ftl
@locales/ru/ui.ftl

<interfaces>
```rust
// settings modal - editor sekce (aktualne)
ui.radio_value(&mut draft.dark_theme, true, i18n.get("settings-theme-dark"));
ui.radio_value(&mut draft.dark_theme, false, i18n.get("settings-theme-light"));

// cil:
// - conditional light-variant picker pouze pro light mode
// - changed() guard + okamzity preview apply do shared settings
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rozsirit i18n o labely light variant pickeru</name>
  <files>locales/cs/ui.ftl, locales/en/ui.ftl, locales/de/ui.ftl, locales/sk/ui.ftl, locales/ru/ui.ftl</files>
  <action>
    1. Pridej konzistentni klice pro picker (min. nadpis + 3 variant labels).
    2. Zachovej naming kompatibilni s existujicim prefixem `settings-*`.
    3. Over, ze zadny locale soubor nezustane bez noveho klice (zadne fallbacky).
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor/.polycredo/sandbox && rg -n "settings-light-variant" locales/cs/ui.ftl locales/en/ui.ftl locales/de/ui.ftl locales/sk/ui.ftl locales/ru/ui.ftl</automated>
  </verify>
  <done>
    Vsechny podporovane jazyky obsahuji kompletni sadu klicu pro 3 varianty.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementovat conditional kartovy picker pouze pro light mode (SETT-01)</name>
  <files>src/app/ui/workspace/modal_dialogs/settings.rs</files>
  <action>
    1. V editor kategorii pridej sekci light variant pickeru viditelnou pouze pri `!draft.dark_theme`.
    2. Picker renderuj jako 3 karty navazane na `LightVariant::{WarmIvory,CoolGray,Sepia}`.
    3. Kazda karta musi obsahovat:
       - swatch (vizualni vzorek varianty),
       - lokalizovany nazev (`settings-light-variant-*`),
       - jasny selected stav (ramecek + check nebo equivalent stavajiciho UI patternu).
    4. Klik na kartu meni `draft.light_variant` bez vedlejsich zmen mimo theme controls.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor/.polycredo/sandbox && RUSTC_WRAPPER= cargo check</automated>
    <manual>
      1) Otevri Settings -> Editor.
      2) V dark mode over, ze picker neni videt.
      3) Prepnout na light a over, ze jsou videt 3 karty.
      4) U kazde karty over swatch + lokalizovany nazev.
      5) Preklikni varianty a over, ze selected state je vzdy jednoznacny.
    </manual>
  </verify>
  <done>
    Kartovy picker respektuje dark/light viditelnost a obsahuje tri varianty se swatchem + nazvem + selected stavem.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Live preview s fingerprint guardem (SETT-02)</name>
  <files>src/app/ui/workspace/modal_dialogs/settings.rs, src/app/ui/workspace/state/mod.rs, src/app/ui/workspace/state/init.rs</files>
  <action>
    1. Vytvor helper pro theme fingerprint `(dark_theme, light_variant)` a helper pro runtime preview apply.
    2. Preview apply spoustej pouze pri `.changed()` na theme controls a jen pokud se fingerprint skutecne zmenil.
    3. Pri preview update zapis `shared.settings = Arc::new(draft.clone())` a bumpni `settings_version`, aby se zmena propsala i do deferred viewportu.
    4. Priprav stavovy prostor pro snapshot puvodnich settings (`settings_original`) pro navazny Cancel revert plan.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor/.polycredo/sandbox && RUSTC_WRAPPER= cargo check</automated>
    <manual>
      1) Otevri root + secondary viewport.
      2) Men light varianty v Settings v jednom okne.
      3) Over okamzitou zmenu vzhledu v obou oknech bez restartu procesu.
      4) Otestuj sekvenci `Light -> Dark -> Light` a over, ze se vrati naposledy zvolena light varianta.
    </manual>
  </verify>
  <done>
    Live preview funguje napric viewporty, neprovadi zbytecne version bump bez zmeny a drzi posledni light variantu po `Light -> Dark -> Light`.
  </done>
</task>

</tasks>

<verification>
- `RUSTC_WRAPPER= cargo check` je zeleny.
- `rg -n "settings-light-variant" locales/*/ui.ftl` potvrdi i18n coverage.
- `rg -n "settings_version\.fetch_add|\.changed\(\)" src/app/ui/workspace/modal_dialogs/settings.rs` potvrdi live preview guard.
- `rg -n "WarmIvory|CoolGray|Sepia" src/app/ui/workspace/modal_dialogs/settings.rs` potvrdi 3 explicitni karty/volby.
- Manual smoke: kartovy picker visibility dark/light + okamzita propagace do secondary viewportu + `Light -> Dark -> Light` drzi posledni light variantu.
</verification>

<success_criteria>
- `SETT-01`: Kartovy picker (swatch + lokalizovany nazev + selected state) pro 3 varianty je viditelny jen v light mode.
- `SETT-02`: Klik na variantu meni vzhled okamzite bez restartu.
- Preview update nebezi kazdy frame; provadi se jen na realne zmene.
- `Light -> Dark -> Light` vraci uzivatele na naposledy zvolenou light variantu.
- Locale soubory obsahuji labely pro vsechny 3 varianty.
</success_criteria>

<output>
Po dokonceni vytvor `.planning/phases/03-light-varianty-settings-ui/03-02-SUMMARY.md`.
</output>
