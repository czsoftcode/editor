---
phase: 05-okam-it-aplikov-n-zm-ny-re-imu-sandboxu-po-p-epnut-checkboxu
plan: 01
type: feature
wave: 1
depends_on: []
files_modified:
  - src/app/ui/workspace/modal_dialogs/settings.rs
  - src/app/ui/workspace/state/mod.rs
  - src/app/ui/workspace/state/init.rs
  - src/app/mod.rs
  - src/app/ui/workspace/mod.rs
  - src/app/ui/workspace/modal_dialogs/conflict.rs
  - locales/cs/ui.ftl
  - locales/en/ui.ftl
autonomous: false
requirements: [SANDBOX-01, SANDBOX-02]

must_haves:
  truths:
    - "Změna sandbox režimu se aplikuje až po Save, Cancel vrací původní režim a runtime změny jsou revertované."
    - "Po Save se režim nejdřív persistuje na disk, teprve poté se spustí runtime přepnutí."
    - "Při změně režimu je změna propagovaná do všech oken stejného projektu."
    - "OFF režim vyžaduje potvrzení; při otevřeném jiném dialogu je nabídnuto odložit/udělat hned."
    - "Selhání persistu zobrazí toast s volbou revert / ponechat dočasně."
  artifacts:
    - path: "src/app/ui/workspace/modal_dialogs/settings.rs"
      provides: "Save/Cancel flow + potvrzení OFF + nové toasty + volba odložit apply."
      contains: "sandbox"
    - path: "src/app/ui/workspace/state/mod.rs"
      provides: "Helper pro runtime apply sandboxu a signalizace do oken."
      contains: "sandbox_mode_enabled"
    - path: "src/app/mod.rs"
      provides: "Hook pro detekci změny settings a dispatch runtime apply do oken."
      contains: "settings_version"
  key_links:
    - from: "Settings Save"
      to: "runtime apply"
      via: "persist -> apply"
      pattern: "sandbox"
    - from: "settings_version"
      to: "multi-window"
      via: "shared settings update"
      pattern: "settings_version"
---

<objective>
Zavést okamžité apply sandbox režimu po Save v Settings včetně potvrzení při OFF, správného pořadí persist → runtime apply, a bezpečné propagace do všech oken stejného projektu s korektními toasty a možností odložení.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-okam-it-aplikov-n-zm-ny-re-imu-sandboxu-po-p-epnut-checkboxu/05-CONTEXT.md
@.planning/phases/05-okam-it-aplikov-n-zm-ny-re-imu-sandboxu-po-p-epnut-checkboxu/05-RESEARCH.md
@.planning/phases/05-okam-it-aplikov-n-zm-ny-re-imu-sandboxu-po-p-epnut-checkboxu/05-VALIDATION.md
@src/app/ui/workspace/modal_dialogs/settings.rs
@src/app/ui/workspace/state/mod.rs
@src/app/mod.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Save/Cancel flow pro sandbox režim + potvrzení OFF + odložení apply</name>
  <files>src/app/ui/workspace/modal_dialogs/settings.rs, src/app/ui/workspace/modal_dialogs/conflict.rs, locales/cs/ui.ftl, locales/en/ui.ftl</files>
  <action>
    1. V Save handleru detekuj změnu sandbox režimu (dirty jen při změně) a vyžádej potvrzení při OFF.
    2. Pokud je otevřený jiný dialog, ukaž toast s volbou „apply now / defer“ (akční tlačítka).
    3. Cancel musí vracet původní hodnotu a zároveň revertovat všechny runtime změny, které mohly být naplánované.
    4. Přidej toasty: OFF (potvrzovací text + varování dopadů), ON (krátký info).
    5. Aktualizuj lokalizace (cs/en) pro nové texty a tooltip/inlinenote upřesnit o okamžitou změnu.
  </action>
  <verify>
    <manual>
      1) Otevři Settings > Projekt.
      2) Přepni sandbox OFF, potvrď a Save.
      3) Ověř, že se zobrazí odpovídající toast a změna se neaplikuje při Cancel.
      4) Otevři jiný dialog, Save sandbox změnu a ověř „apply now / defer“ volbu.
    </manual>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor && RUSTC_WRAPPER= cargo check</automated>
  </verify>
  <done>
    Save/Cancel respektuje sandbox změnu, OFF vyžaduje potvrzení, a odložené apply jde vybrat přes toast.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Runtime apply helper + multi-window dispatch</name>
  <files>src/app/ui/workspace/state/mod.rs, src/app/ui/workspace/state/init.rs, src/app/mod.rs, src/app/ui/workspace/mod.rs</files>
  <action>
    1. Zaveď helper (např. `apply_sandbox_mode_change`) v `WorkspaceState` pro runtime přepnutí, ale až po persistu.
    2. Helper nastaví `sandbox_mode_enabled`, `build_in_sandbox`, `file_tree_in_sandbox` a vydá signál pro subsystémy (terminály, file tree, taby).
    3. Přidej hook na změnu `settings_version`, který v každém okně detekuje změnu sandbox režimu a spustí runtime apply.
    4. Zajisti, že nedochází k apply při pouhém toggle v modalu bez Save.
    5. Přidej ochranu proti souběžným změnám (druhé okno dostane toast/dialog o konfliktu).
  </action>
  <verify>
    <manual>
      1) Otevři dvě okna stejného projektu.
      2) V jednom okně přepni sandbox ON/OFF a ulož.
      3) Ověř, že druhé okno dostane upozornění a režim se aplikuje.
    </manual>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor && RUSTC_WRAPPER= cargo check</automated>
  </verify>
  <done>
    Runtime apply funguje po Save a propaguje se do všech oken bezpečně a bez nežádoucího live preview.
  </done>
</task>

<task type="auto">
  <name>Task 3: Persist → apply pořadí + error handling</name>
  <files>src/app/ui/workspace/modal_dialogs/settings.rs, src/app/ui/workspace/state/mod.rs</files>
  <action>
    1. Uprav Save flow tak, aby při sandbox změně nejdřív proběhl persist settings na disk.
    2. Při chybě persistu zobraz toast s volbou „revert / ponechat dočasně“; při revert se runtime apply nesmí spustit.
    3. Pokud uživatel zvolí ponechat dočasně, runtime apply proběhne, ale persist je označen jako neuložený (např. další toast nebo status indikace).
  </action>
  <verify>
    <manual>
      1) Nasimuluj selhání persistu (např. read-only settings.toml).
      2) Ověř, že se zobrazí toast s volbami a že „revert“ neaplikuje runtime změnu.
    </manual>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor && RUSTC_WRAPPER= cargo check</automated>
  </verify>
  <done>
    Persist je vždy před runtime apply a error handling je uživatelsky srozumitelný.
  </done>
</task>

</tasks>

<verification>
- `RUSTC_WRAPPER= cargo check`
- `./check.sh`
- Save/Cancel sandbox režimu funguje bez live apply
- Persist fail nabízí revert/keep a nedělá tichý fallback
- Multi-window upozornění funguje a apply se spustí v obou oknech
</verification>

<success_criteria>
- Okamžitá aplikace sandbox režimu probíhá pouze po Save
- Runtime apply je sdílený helper použitý napříč okny
- OFF režim je potvrzovaný a Cancel vrací původní stav
</success_criteria>
