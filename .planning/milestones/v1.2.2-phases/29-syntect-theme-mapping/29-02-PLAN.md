---
phase: 29-syntect-theme-mapping
plan: 02
type: execute
wave: 2
depends_on:
  - 29-01
files_modified:
  - src/app/ui/terminal/instance/theme.rs
autonomous: true
gap_closure: true
requirements:
  - SYNTAX-02

must_haves:
  truths:
    - "Pozadí terminálu je navázané na aktivní barevnou variantu (4 light + 2 dark) a v dark režimu není světlé/oslňující."
    - "Mapování backgroundu je deterministické a centralizované v terminal theme vrstvě, bez ad-hoc přepisů v UI panelech."
    - "Existuje regresní test gate pro kontrast a variant mapping terminal backgroundu."
  artifacts:
    - path: "src/app/ui/terminal/instance/theme.rs"
      provides: "Variant-aware mapování terminal backgroundu pro light i dark režim"
      contains: "terminal_palette"
    - path: "src/app/ui/terminal/instance/theme.rs"
      provides: "Regresní testy pro dark non-white background a variant mapping"
      contains: "terminal_theme_"
  key_links:
    - from: "Settings::to_egui_visuals()"
      to: "terminal_palette"
      via: "egui::Visuals (dark_mode + panel_fill)"
      pattern: "terminal_theme_for_visuals"
---

<objective>
Uzavřít major gap z verifikace fáze 29: terminálové pozadí musí konzistentně odpovídat aktivní theme variantě a v dark režimu nesmí být bílé nebo oslňující.

Purpose: odstranit mismatch mezi syntax theme mappingem a reálným vzhledem terminálu.
Output: robustní variant-aware background mapping + regresní testy.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-syntect-theme-mapping/VERIFICATION.md
@.planning/phases/29-syntect-theme-mapping/29-CONTEXT.md
@src/app/ui/terminal/instance/theme.rs
@src/settings.rs

Scope limit:
- Jen terminal background theme mismatch z VERIFICATION.md.
- Bez změn syntect mapování, bez UI refaktoru mimo terminal theme vrstvu.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Navázat terminal background na aktivní theme variantu včetně dark variant</name>
  <files>src/app/ui/terminal/instance/theme.rs</files>
  <action>
    1. Upravit generování `ColorPalette.background` tak, aby vycházelo z aktivních `egui::Visuals` (dark_mode + variant-specific tone přes panel_fill) místo implicitního světlení, které může vést k bílému pozadí.
    2. Pro dark režim zavést explicitní kontrakt: background musí zůstat tmavý a čitelný; nesmí degradovat na světlý fallback.
    3. Zachovat centralizaci logiky v `theme.rs`; nepřidávat duplikovanou korekci v `bottom/right` rendererech terminálu.
  </action>
  <verify>
    <automated>cargo test -q terminal_theme_dark_background_stays_dark</automated>
    <manual>Ručně přepnout 2 dark varianty (Default, Midnight) a potvrdit, že terminálové pozadí není bílé/oslňující.</manual>
  </verify>
  <done>
    Terminal background respektuje aktivní dark/light variantu a dark režim konzistentně drží tmavé pozadí.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dopsat regresní testy pro background kontrast a variant mapping</name>
  <files>src/app/ui/terminal/instance/theme.rs</files>
  <action>
    1. Doplnit/rozšířit testy tak, aby explicitně kryly mapping pozadí napříč 4 light + 2 dark variantami.
    2. Přidat test, který failne při světlém backgroundu v dark variantách (guard proti regresi "bílé pozadí v dark").
    3. Přidat test kontrastu foreground/background pro dark varianty, aby byl zachován čitelný výstup.
  </action>
  <verify>
    <automated>cargo test -q terminal_theme_light_variant_backgrounds_are_distinct_across_all_three</automated>
    <automated>cargo test -q terminal_theme_dark_background_stays_dark</automated>
    <automated>cargo test -q terminal_theme_</automated>
  </verify>
  <done>
    Test gate zachytí regresi variant mappingu i kontrastu terminal backgroundu.
  </done>
</task>

</tasks>

<verification>
cargo check
./check.sh
</verification>

<success_criteria>
- Terminálové pozadí koresponduje s aktivní variantou tématu
- V dark režimu není background světlý/oslňující
- Regresní testy pokrývají variant mapping i kontrast backgroundu
</success_criteria>

<output>
Po exekuci vytvořit `.planning/phases/29-syntect-theme-mapping/29-02-SUMMARY.md`.
</output>
