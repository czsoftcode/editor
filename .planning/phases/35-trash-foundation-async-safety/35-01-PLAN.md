---
phase: 35-trash-foundation-async-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/project_config.rs
  - src/app/mod.rs
  - src/app/trash.rs
  - src/app/ui/file_tree/dialogs.rs
  - src/app/ui/file_tree/mod.rs
autonomous: true
requirements:
  - TRASH-03
  - RELIAB-01

must_haves:
  truths:
    - "Trash adresar se musi vytvaret on-demand v `.polycredo/trash`, nikoli pri otevreni projektu."
    - "Delete tok ve fazi 35 nesmi fallbackovat na hard delete, pokud create/move selze."
    - "I/O kroky delete foundation toku musi bezet mimo UI vlakno."
    - "Phase 35 musi definovat minimalni metadata kontrakt pro trash polozku (trash_name <-> puvodni cesta/cas)."
  artifacts:
    - path: "src/app/project_config.rs"
      provides: "Deterministicky helper pro trash cestu v project-level `.polycredo` namespace"
      contains: "project_config_dir|trash"
    - path: "src/app/trash.rs"
      provides: "Foundation API pro ensure/create trash + metadata kontrakt"
      contains: "ensure_trash_dir|TrashEntryMeta|Result"
    - path: "src/app/ui/file_tree/dialogs.rs"
      provides: "Delete entrypoint napojeny na async trash foundation tok"
      contains: "spawn_task|pending_error"
  key_links:
    - from: "On-demand create trash"
      to: "TRASH-03"
      via: "Delete flow pred operaci zajisti `.polycredo/trash` bez predvytvareni pri startupu"
      pattern: "ensure_trash_dir|create_dir_all"
    - from: "Async execution model"
      to: "RELIAB-01"
      via: "File-tree delete flow neprovadi tezke fs operace na UI threadu"
      pattern: "spawn_task|try_recv"
---

<objective>
Zavest foundation vrstvu pro `.polycredo/trash` a prepnout delete foundation tok na non-blocking execution
s fail-closed chovanim pri create/move problemech.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-trash-foundation-async-safety/35-CONTEXT.md
@.planning/phases/35-trash-foundation-async-safety/35-RESEARCH.md
@.planning/phases/35-trash-foundation-async-safety/35-VALIDATION.md
@./AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pridat trash path helpery a foundation modul</name>
  <files>src/app/project_config.rs, src/app/mod.rs, src/app/trash.rs</files>
  <action>
    1. Rozsir `project_config` o helper na `.polycredo/trash` a helper pro vytvoreni parent vetve.
    2. Pridej novy modul `src/app/trash.rs` s API `ensure_trash_dir(project_root)` a jednotnym error contractem.
    3. Napoj modul do app namespace tak, aby byl dostupny pro file-tree tok bez velkeho refactoru.
  </action>
  <verify>
    <automated>cargo check && rg -n "trash" src/app/project_config.rs src/app/trash.rs -S</automated>
  </verify>
  <done>
    Foundation helpery existuji a vraci deterministickou `.polycredo/trash` cestu + create behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Definovat minimalni metadata kontrakt phase35</name>
  <files>src/app/trash.rs</files>
  <action>
    1. Definuj minimalni metadata strukturu pro trash entry (unikatni trash nazev, puvodni relativni cesta, deleted_at, typ polozky).
    2. Navaz metadata kontrakt na foundation delete tok tak, aby bylo mozne pripraveny zaznam pouzit v navazne restore fazi.
    3. Nepokracuj do plne persistence/UI browse; jen kontrakt + priprava zapisoveho bodu.
  </action>
  <verify>
    <automated>cargo check && rg -n \"trash_name|original_relative_path|deleted_at|entry_kind\" src/app/trash.rs -S</automated>
  </verify>
  <done>
    Metadata kontrakt je explicitne definovany a pripraveny pro navaznou implementaci restore flow.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prepnout delete foundation tok na async + fail-closed</name>
  <files>src/app/ui/file_tree/dialogs.rs, src/app/ui/file_tree/mod.rs</files>
  <action>
    1. Preved delete confirm tok tak, aby create trash + move foundation operace bezely mimo UI vlakno.
    2. Pri selhani create/move zastav operaci a nastav explicitni `pending_error` pro toast surfacing.
    3. Nepridavej hard-delete fallback; preserve bezpecny fail-closed behavior.
  </action>
  <verify>
    <automated>cargo check && ! rg -n "remove_dir_all\(|remove_file\(" src/app/ui/file_tree/dialogs.rs -S</automated>
  </verify>
  <done>
    Delete foundation tok bezi async, je fail-closed a ma pripraveny error surfacing do UI.
  </done>
</task>

</tasks>

<verification>
cargo check
./check.sh
! rg -n "remove_dir_all\(|remove_file\(" src/app/ui/file_tree/dialogs.rs -S
rg -n "ensure_trash_dir|spawn_task|pending_error" src/app/ui/file_tree/dialogs.rs src/app/trash.rs -S
rg -n "trash_name|original_relative_path|deleted_at|entry_kind" src/app/trash.rs -S
</verification>

<verification_criteria>
- TRASH-03: `.polycredo/trash` se zaklada on-demand pred delete operaci.
- RELIAB-01: Delete foundation tok neprovadi tezky I/O na UI threadu.
- Pri failure create/move nevznika hard-delete fallback.
- Metadata kontrakt pro trash entry je explicitni a pripraveny pro phase 37 restore mapovani.
</verification_criteria>

<success_criteria>
- Foundation API a path helpery jsou pripraveny pro navazne faze.
- Delete tok je non-blocking a fail-closed.
- Kod je kompilovatelny a traceovatelny na TRASH-03 + RELIAB-01.
</success_criteria>

<output>
Po dokonceni vytvor `.planning/phases/35-trash-foundation-async-safety/35-01-SUMMARY.md`.
</output>
