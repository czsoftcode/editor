---
phase: 13-provider-foundation
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/app/ui/workspace/state/mod.rs
  - src/app/ui/workspace/state/init.rs
  - src/app/ui/background.rs
  - src/app/ui/terminal/right/ai_bar.rs
  - src/config.rs
autonomous: false
requirements: [PROV-03]

must_haves:
  truths:
    - "Editor automaticky detekuje bezici Ollama server na localhost:11434 kazdych 10 sekund"
    - "Status ikona v AI baru je zelena kdyz Ollama bezi, cervena kdyz ne, seda pri prvnim checku"
    - "ComboBox v AI baru zobrazuje dostupne Ollama modely (jen nazev bez :latest)"
    - "Prazdny stav ComboBoxu zobrazi 'No models available' s tooltipem"
    - "Posledni zvoleny model se zapamatuje v eframe persistence"
    - "Polling neprobehne behem aktivniho streamu"
  artifacts:
    - path: "src/app/ui/workspace/state/mod.rs"
      provides: "Ollama-specific state fields (ollama_status, ollama_models, ollama_check_rx, ollama_selected_model)"
      contains: "ollama_"
    - path: "src/app/ui/background.rs"
      provides: "Polling logic for Ollama detection every 10s"
      contains: "spawn_ollama_check"
    - path: "src/app/ui/terminal/right/ai_bar.rs"
      provides: "Status icon + model ComboBox in AI bar"
      contains: "ollama_models"
    - path: "src/config.rs"
      provides: "OLLAMA_CHECK_INTERVAL_SECS constant (10)"
      contains: "OLLAMA_CHECK_INTERVAL_SECS"
  key_links:
    - from: "src/app/ui/background.rs"
      to: "src/app/ai/ollama.rs"
      via: "calls spawn_ollama_check() for polling"
      pattern: "spawn_ollama_check"
    - from: "src/app/ui/terminal/right/ai_bar.rs"
      to: "src/app/ui/workspace/state/mod.rs"
      via: "reads ws.ollama_models and ws.ollama_status for UI"
      pattern: "ws\\.ollama_"
    - from: "src/app/ui/background.rs"
      to: "src/app/ui/workspace/state/mod.rs"
      via: "updates ws.ollama_status and ws.ollama_models from rx"
      pattern: "ws\\.ollama_status"
---

<objective>
Napojit OllamaProvider na existujici UI — polling detekce, status ikona v AI baru, model ComboBox.

Purpose: Uzivatel vidi stav Ollama serveru a muze vybrat model — zaklad pro budouci chat UI (Phase 15).
Output: Polling v background.rs, state fields ve WorkspaceState, status ikona + model picker v ai_bar.rs
</objective>

<execution_context>
@/home/stkremen/.claude/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-provider-foundation/13-CONTEXT.md
@.planning/phases/13-provider-foundation/13-01-SUMMARY.md

@src/app/ui/workspace/state/mod.rs
@src/app/ui/workspace/state/init.rs
@src/app/ui/background.rs
@src/app/ui/terminal/right/ai_bar.rs
@src/config.rs

<interfaces>
<!-- From Plan 01 (provider.rs + ollama.rs) -->

From src/app/ai/provider.rs:
```rust
pub struct ProviderCapabilities { pub supports_streaming: bool, pub supports_tools: bool }
pub struct ProviderConfig { pub base_url: String, pub model: String, pub temperature: f64, pub num_ctx: u64 }
pub enum StreamEvent { Token(String), Done { model, prompt_tokens, completion_tokens }, Error(String), ToolCall { id, name, arguments } }
pub trait AiProvider: Send + Sync { fn name() -> &str; fn is_available() -> bool; fn available_models() -> Result<Vec<String>, String>; ... }
```

From src/app/ai/ollama.rs:
```rust
pub enum OllamaStatus { Available(Vec<String>), Unavailable }
pub struct OllamaProvider { /* ... */ }
pub fn spawn_ollama_check(base_url: String) -> mpsc::Receiver<OllamaStatus>;
```

From existing polling pattern (src/app/ui/background.rs lines 135-154):
```rust
// Existing ai_tool_check pattern:
if let Some(rx) = &ws.ai_tool_check_rx && let Ok(status) = rx.try_recv() {
    ws.ai_tool_available = status;
    ws.ai_tool_check_rx = None;
    ws.ai_tool_last_check = std::time::Instant::now();
}
if ws.ai_tool_last_check.elapsed().as_secs() >= crate::config::AI_TOOL_CHECK_INTERVAL_SECS
    && ws.ai_tool_check_rx.is_none() { /* spawn check */ }
```

From existing settings access (settings.rs + plugins modal):
```rust
// Ollama URL is at: settings.plugins["ollama"].config["API_URL"]
// Default: "http://localhost:11434"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WorkspaceState fields + polling v background.rs + config konstanta</name>
  <files>src/app/ui/workspace/state/mod.rs, src/app/ui/workspace/state/init.rs, src/app/ui/background.rs, src/config.rs</files>
  <action>
1. Pridat do `src/config.rs`:
   - `pub const OLLAMA_CHECK_INTERVAL_SECS: u64 = 10;`
   - `pub const OLLAMA_DEFAULT_URL: &str = "http://localhost:11434";`

2. Pridat do `WorkspaceState` (src/app/ui/workspace/state/mod.rs):
   - `pub ollama_status: OllamaConnectionStatus` — enum: Connected, Disconnected, Checking (defaultne Checking)
   - `pub ollama_models: Vec<String>` — seznam dostupnych modelu
   - `pub ollama_selected_model: String` — aktualne zvoleny model
   - `pub ollama_check_rx: Option<mpsc::Receiver<OllamaStatus>>` — pro async polling
   - `pub ollama_last_check: std::time::Instant` — cas posledniho checku
   - `pub ollama_base_url: String` — URL serveru (ctena ze settings pri inicializaci)
   - Definovat `OllamaConnectionStatus` enum v tomto souboru nebo v types.

3. Inicializace v `src/app/ui/workspace/state/init.rs`:
   - `ollama_status: OllamaConnectionStatus::Checking`
   - `ollama_models: Vec::new()`
   - `ollama_selected_model: String::new()` (bude nacteno z eframe persistence)
   - `ollama_check_rx: None`
   - `ollama_last_check: Instant::now() - Duration::from_secs(OLLAMA_CHECK_INTERVAL_SECS)` — okamzity prvni check
   - `ollama_base_url`: precist z settings.plugins["ollama"].config["API_URL"], fallback na OLLAMA_DEFAULT_URL

4. Pridat polling logiku do `src/app/ui/background.rs` (vedle existujiciho ai_tool_check):
   - Prijmout vysledek z ollama_check_rx pres try_recv():
     - OllamaStatus::Available(models) -> ws.ollama_status = Connected, ws.ollama_models = models, pokud ws.ollama_selected_model je prazdny nebo neni v seznamu -> nastavit na prvni model
     - OllamaStatus::Unavailable -> ws.ollama_status = Disconnected, ws.ollama_models.clear()
     - Po prijeti: ws.ollama_check_rx = None, ws.ollama_last_check = Instant::now()
   - Spustit novy check: pokud ws.ollama_last_check.elapsed() >= OLLAMA_CHECK_INTERVAL_SECS a ws.ollama_check_rx.is_none() a ws.ai_loading == false (nepollovat behem streamu):
     - ws.ollama_check_rx = Some(spawn_ollama_check(ws.ollama_base_url.clone()))

Import spawn_ollama_check z crate::app::ai::ollama (nebo pres re-export z crate::app::ai).
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor && cargo check 2>&1 | tail -10</automated>
  </verify>
  <done>
    - WorkspaceState ma ollama_* pole
    - Background polling volne spawn_ollama_check kazdych 10s
    - Polling se preskoci behem aktivniho streamu (ai_loading == true)
    - Prvni check probehne okamzite po spusteni (last_check inicializovany v minulosti)
    - cargo check projde bez erroru
  </done>
</task>

<task type="auto">
  <name>Task 2: Status ikona + model ComboBox v AI baru</name>
  <files>src/app/ui/terminal/right/ai_bar.rs</files>
  <action>
1. Pridat do render_ai_bar() status ikonu Ollama serveru:
   - Pouzit egui kruhovy indikator (ui.painter().circle_filled nebo barevny text/emoji):
     - Connected (zelena): egui::Color32::from_rgb(0, 180, 0) nebo theme-aware zelena
     - Disconnected (cervena): egui::Color32::from_rgb(220, 50, 50)
     - Checking (seda): egui::Color32::GRAY
   - Tooltip na ikone: "Ollama: connected" / "Ollama: disconnected" / "Ollama: checking..."
   - Ikona je vzdy viditelna v AI baru

2. Pridat model ComboBox:
   - Pokud ws.ollama_models.is_empty(): zobrazit disabled ComboBox s textem "No models available", tooltip: "Run 'ollama pull <model>' to download a model"
   - Pokud modely existuji: ComboBox s ws.ollama_selected_model, sirka ~200px
   - Zobrazovat jen nazev modelu (Plan 01 uz stripuje :latest v parse_tags_response)
   - Pri zmene modelu ulozit do ws.ollama_selected_model

3. Persistence zvoleneho modelu:
   - Precist z eframe storage pri inicializaci (v init.rs) pres `cc.storage` s klicem "ollama_selected_model"
   - Ulozit pri zmene nebo v save() — pouzit existujici eframe persistence pattern z projektu

4. Layout: status ikona | model ComboBox | existujici agent picker | start button
   Pouzit ui.horizontal(), ui.spacing() pro vizualni oddeleni.

Nepouzivat hardcoded barvy — pokud je to mozne, pouzit theme-aware approach pres ui.visuals().
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor && cargo check 2>&1 | tail -10</automated>
  </verify>
  <done>
    - AI bar zobrazuje status ikonu (zelena/cervena/seda) podle ws.ollama_status
    - AI bar zobrazuje model ComboBox s dostupnymi modely
    - Prazdny stav ma disabled ComboBox s "No models available" a tooltip
    - Zvoleny model se uklada do eframe persistence
    - cargo check projde bez erroru
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Vizualni overeni Ollama detekce a model pickeru</name>
  <files>N/A</files>
  <action>
Uzivatel vizualne overi funkcnost Ollama auto-detect, status ikony a model pickeru.
Automatizovane tasky (1 a 2) jiz dokoncily implementaci — tento checkpoint je pro vizualni verifikaci.

Kroky k overeni:
1. Spustit Ollama server: `ollama serve` (nebo overit ze bezi)
2. Spustit editor: `cargo run`
3. Otevrit AI panel (pravy panel)
4. Overit ze AI bar zobrazuje ZELENOU status ikonu (Ollama connected)
5. Overit ze ComboBox zobrazuje dostupne modely (nazvy bez :latest)
6. Vybrat jiny model, zavrit a znovu otevrit editor — overit ze vyber se zapamatoval
7. Zastavit Ollama: `systemctl stop ollama` nebo kill procesu
8. Pockat ~10s — overit ze ikona se zmeni na CERVENOU (disconnected)
9. Overit ze ComboBox je prazdny/disabled s textem "No models available"
10. Znovu spustit Ollama — overit ze ikona se vrati na zelenou do 10s
11. Behem interakce overit ze editor zustava responzivni (klikat na taby, scrollovat)
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor && cargo test 2>&1 | tail -5</automated>
  </verify>
  <done>Uzivatel potvrdil "approved" — status ikona, model picker a polling funguji spravne</done>
</task>

</tasks>

<verification>
```bash
# Polling fields existuji
grep -q "ollama_status" src/app/ui/workspace/state/mod.rs
grep -q "ollama_check_rx" src/app/ui/workspace/state/mod.rs
grep -q "spawn_ollama_check" src/app/ui/background.rs

# Status ikona v AI baru
grep -q "ollama_status" src/app/ui/terminal/right/ai_bar.rs

# Konstanta pro interval
grep -q "OLLAMA_CHECK_INTERVAL_SECS" src/config.rs

# Kompilace
cargo check

# Vsechny testy
cargo test
```
</verification>

<success_criteria>
- Ollama server detekovan automaticky kazdych 10s
- Status ikona v AI baru reflektuje stav (zelena/cervena/seda)
- Model ComboBox zobrazuje dostupne modely a pamatuje si vyber
- Prazdny stav ComboBoxu ma spravny text a tooltip
- Polling se neprovadi behem aktivniho streamu
- Editor zustava responzivni (zadne blokovani UI threadu)
</success_criteria>

<output>
After completion, create `.planning/phases/13-provider-foundation/13-02-SUMMARY.md`
</output>
