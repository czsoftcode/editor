---
phase: 13-provider-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/ui/workspace/state/init.rs
  - src/app/ui/workspace/state/mod.rs
  - src/app/ai/ollama.rs
autonomous: true
gap_closure: true
requirements:
  - PROV-03

must_haves:
  truths:
    - "Kdyz uzivatel ma v plugin settings API_URL nastavenou na https://ollama.com (nebo jinou ne-API URL), native Ollama polling pouzije default localhost:11434 misto te URL"
    - "Kdyz uzivatel ma validni lokalni Ollama URL v plugin settings, native provider ji pouzije"
    - "Kdyz Ollama nebezi, ComboBox je prazdny/disabled a nepusobi chybove"
  artifacts:
    - path: "src/app/ui/workspace/state/init.rs"
      provides: "Validace ollama_base_url pri inicializaci"
      contains: "validate_ollama_url\\|is_valid_ollama_url\\|OLLAMA_DEFAULT_URL"
    - path: "src/app/ai/ollama.rs"
      provides: "URL validacni funkce pro Ollama endpoint"
      exports: ["validate_ollama_url"]
  key_links:
    - from: "src/app/ui/workspace/state/init.rs"
      to: "src/app/ai/ollama.rs"
      via: "validate_ollama_url call"
      pattern: "validate_ollama_url"
---

<objective>
Opravit bug kdy native Ollama provider cte URL z WASM plugin settings (settings.plugins["ollama"].config["API_URL"]) bez validace. Kdyz uzivatel ma v plugin settings nastavenou nevalidni URL (napr. https://ollama.com — webova stranka, ne API), polling selze a ComboBox se chova chybove.

Purpose: Uzavrit UAT gap #3 — native provider musi byt odolny vuci spatnym URL v plugin settings.
Output: Validacni logika pro ollama_base_url, fallback na OLLAMA_DEFAULT_URL pri nevalidni URL.
</objective>

<execution_context>
@/home/stkremen/.claude/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-provider-foundation/13-02-SUMMARY.md

<interfaces>
From src/app/ai/ollama.rs:
```rust
pub fn spawn_ollama_check(base_url: String) -> mpsc::Receiver<OllamaStatus>;
pub enum OllamaStatus { Available(Vec<String>), Unavailable }
```

From src/config.rs:
```rust
pub const OLLAMA_DEFAULT_URL: &str = "http://localhost:11434";
pub const OLLAMA_CHECK_INTERVAL_SECS: u64 = 10;
```

From src/app/ui/workspace/state/init.rs (lines 209-213) — current bug:
```rust
ollama_base_url: settings
    .plugins
    .get("ollama")
    .and_then(|p| p.config.get("API_URL").cloned())
    .unwrap_or_else(|| crate::config::OLLAMA_DEFAULT_URL.to_string()),
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pridat validate_ollama_url a opravit init.rs</name>
  <files>src/app/ai/ollama.rs, src/app/ui/workspace/state/init.rs</files>
  <behavior>
    - Test: validate_ollama_url("http://localhost:11434") vraci Some("http://localhost:11434")
    - Test: validate_ollama_url("http://192.168.1.100:11434") vraci Some(...)
    - Test: validate_ollama_url("https://ollama.com") vraci None (webova stranka, ne API)
    - Test: validate_ollama_url("") vraci None
    - Test: validate_ollama_url("not-a-url") vraci None
    - Test: validate_ollama_url("http://localhost:11434/") vraci URL bez trailing slash
  </behavior>
  <action>
    1. V src/app/ai/ollama.rs pridat verejnou funkci `pub fn validate_ollama_url(url: &str) -> Option<String>`:
       - Validuje ze URL je parsovatelna (url crate nebo manualni kontrola)
       - Odmitne URL ktere NEMAJI port (https://ollama.com nema port → neni Ollama API server)
       - Odmitne prazdne retezce
       - Odrizne trailing slash
       - Vraci Some(cleaned_url) pro validni URL, None pro nevalidni

    2. V src/app/ui/workspace/state/init.rs zmenit radky 209-213:
       Misto primeho cteni z plugin settings pouzit:
       ```rust
       ollama_base_url: settings
           .plugins
           .get("ollama")
           .and_then(|p| p.config.get("API_URL"))
           .and_then(|url| crate::app::ai::ollama::validate_ollama_url(url))
           .unwrap_or_else(|| crate::config::OLLAMA_DEFAULT_URL.to_string()),
       ```

    Logika validace: URL musi mit explicitni port (napr. :11434) NEBO byt localhost/127.0.0.1 — tim se odmitne https://ollama.com (nema port), ale povoli http://localhost:11434, http://192.168.1.50:11434, http://my-server:11434.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor && cargo test validate_ollama -- --nocapture</automated>
  </verify>
  <done>validate_ollama_url spravne odmita nevalidni URL (vcetne https://ollama.com), init.rs pouziva validaci, testy prochazi</done>
</task>

<task type="auto">
  <name>Task 2: Overit kompilaci a existujici testy</name>
  <files>src/app/ui/workspace/state/init.rs</files>
  <action>
    Spustit `cargo check` a `cargo test` pro overeni ze zmena neporusila zadnou existujici funkcionalitu. Pokud se objevi compile error kvuli zmene v init.rs (napr. zmena typu), opravit.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor && cargo check 2>&1 && cargo test 2>&1 | tail -5</automated>
  </verify>
  <done>cargo check i cargo test prochazi bez chyb, zadna regrese</done>
</task>

</tasks>

<verification>
1. cargo test validate_ollama — nove testy prochazi
2. cargo check — kompilace bez chyb
3. cargo test — vsechny existujici testy prochazi
</verification>

<success_criteria>
- URL https://ollama.com v plugin settings NEVEDE k pouziti teto URL pro polling — misto toho se pouzije default localhost:11434
- Validni lokalni URL (http://localhost:11434, http://192.168.1.50:11434) funguje normalne
- Zadna regrese v existujicich testech
</success_criteria>

<output>
After completion, create `.planning/phases/13-provider-foundation/13-03-SUMMARY.md`
</output>
