---
phase: 25-unsaved-close-guard
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - src/app/ui/workspace/mod.rs
  - src/app/ui/workspace/menubar/mod.rs
  - src/app/ui/editor/ui.rs
  - src/app/ui/editor/tabs.rs
autonomous: true
requirements:
  - GUARD-01
  - GUARD-03

must_haves:
  truths:
    - "Všechny tab-close spouštěče (`Ctrl+W`, menu, tab `×`, middle-click) používají stejný guard request."
    - "Přímé zavření dirty tabu bez rozhodnutí uživatele není možné."
    - "`Cancel` vždy ruší close operaci bez side-effectů na taby."
  artifacts:
    - path: "src/app/ui/workspace/mod.rs"
      provides: "Ctrl+W route do guard flow"
      contains: "request_close_active_tab"
    - path: "src/app/ui/editor/ui.rs"
      provides: "TabBarAction::Close route do guard flow"
      contains: "TabBarAction::Close"
  key_links:
    - from: "Menu Close Tab"
      to: "Workspace close guard API"
      via: "menubar akce volá guard-aware helper"
      pattern: "actions.close_file"
---

<objective>
Sjednotit všechny vstupy pro zavření tabu na jednu guard-aware trasu, aby `GUARD-01` a
větvení `Save/Discard/Cancel` měly konzistentní chování bez ohledu na trigger.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-unsaved-close-guard/25-CONTEXT.md
@.planning/phases/25-unsaved-close-guard/25-RESEARCH.md
@.planning/phases/25-unsaved-close-guard/25-VALIDATION.md
@./CLAUDE.md
@src/app/ui/workspace/mod.rs
@src/app/ui/workspace/menubar/mod.rs
@src/app/ui/editor/ui.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Přesměrování tab-close triggerů na guard API</name>
  <files>src/app/ui/workspace/mod.rs, src/app/ui/workspace/menubar/mod.rs, src/app/ui/editor/ui.rs</files>
  <action>
    1. Nahraď přímá volání `editor.clear()` / `close_tab` guard-aware helperem.
    2. Ujisti se, že `Ctrl+W`, menu `Close Tab` i `TabBarAction::Close` (včetně middle-click cesty) vedou přes stejný vstup.
    3. Zachovej fast-path bez dialogu při clean tabu.
  </action>
  <verify>
    <automated>cargo test unsaved_close_guard_tab_triggers -- --nocapture</automated>
  </verify>
  <done>
    Všechny tab-close spouštěče jsou jednotně napojené na unsaved guard flow.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ochrana interního `close_tab` API před obcházením guardu</name>
  <files>src/app/ui/editor/tabs.rs, src/app/ui/workspace/mod.rs</files>
  <action>
    1. Omez přímé použití nízkoúrovňového zavření tabu na interní guard schválené cesty.
    2. Doplň komentář/pojmenování helperu tak, aby bylo zřejmé, kdy je povolené volat přímý close.
    3. Odstraň zjevné body, kde by dirty tab šel zavřít bez rozhodnutí uživatele.
  </action>
  <verify>
    <automated>cargo test unsaved_close_guard_tab_triggers -- --nocapture</automated>
  </verify>
  <done>
    Guard route je vynucená a obcházení přes staré call-site je odstraněné.
  </done>
</task>

</tasks>

<verification>
cargo check
cargo test unsaved_close_guard_tab_triggers -- --nocapture
</verification>

<success_criteria>
- `Ctrl+W`, menu `Close Tab`, tab `×` i middle-click sdílí stejný guard flow
- Dirty tab se nezavře bez explicitní volby `Save`/`Discard`
- `Cancel` ukončí close operaci a tab zůstane otevřený
</success_criteria>
