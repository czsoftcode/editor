---
phase: 32-cleanup-tests-and-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/ui/terminal/ai_chat/logic.rs
  - src/app/ui/terminal/ai_chat/slash.rs
  - src/app/ui/terminal/ai_chat/approval.rs
  - src/app/ui/background.rs
  - src/app/ai_core/executor.rs
  - tests/phase32_namespace_guard.rs
  - tests/phase32_runtime_stability.rs
autonomous: true
requirements:
  - STAB-01
  - STAB-02

must_haves:
  truths:
    - "Scope je pouze stabilizace po phase 31; žádné nové capability ani UX redesign."
    - "Regresní priorita je prompt/stream/slash/approval, včetně recovery cest (toast, retry, approval blokace)."
    - "Namespace guard brání návratu `app::cli` referencí v aktivních assistant-only tocích."
  artifacts:
    - path: "tests/phase32_namespace_guard.rs"
      provides: "Explicitní STAB namespace regression guard pro aktivní runtime callsite"
      contains: "app::cli"
    - path: "tests/phase32_runtime_stability.rs"
      provides: "Cílené smoke/regression testy prompt/stream/slash/approval flow"
      contains: "prompt|stream|slash|approval"
    - path: "src/app/ui/background.rs"
      provides: "Nezablokovaný runtime event processing pro stream/error/approval update"
      contains: "process_background_events"
  key_links:
    - from: "Quality gate po CLI cleanup"
      to: "STAB-01"
      via: "Konzistentní PASS přes `cargo check` a `./check.sh`"
      pattern: "check.sh"
    - from: "Regression coverage klíčových toků"
      to: "STAB-02"
      via: "Aktualizované testy pro nový namespace + runtime guardy"
      pattern: "phase32_"
---

<objective>
Stabilizovat assistant-only runtime po phase 31: uzavřít STAB-01 hard gate a STAB-02 regresní pokrytí
pro prompt/stream/slash/approval bez rozšiřování produktového scope.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-cleanup-tests-and-stabilization/32-CONTEXT.md
@.planning/phases/32-cleanup-tests-and-stabilization/32-RESEARCH.md
@.planning/phases/32-cleanup-tests-and-stabilization/32-VALIDATION.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: STAB-02 namespace regression guard pro aktivní assistant-only runtime</name>
  <files>tests/phase32_namespace_guard.rs, tests/phase30_plan01_foundation_imports.rs, tests/phase30_plan04_ai_terminal_imports.rs</files>
  <action>
    1. Přidej explicitní phase32 guard test s jasným seznamem kritických souborů z prompt/stream/slash/approval toku.
    2. Validuj zákaz `crate::app::cli` i `app::cli` patternů v tomto seznamu.
    3. Zachovej historii starších testů; pouze doplň nebo zpřesni aktivní coverage.
  </action>
  <verify>
    <automated>cargo test phase32_namespace_guard -- --nocapture</automated>
  </verify>
  <done>
    Aktivní runtime callsite jsou explicitně chráněné proti návratu legacy CLI namespace.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: STAB-02 runtime smoke/regression pro prompt/stream/slash/approval</name>
  <files>tests/phase32_runtime_stability.rs, src/app/ui/terminal/ai_chat/logic.rs, src/app/ui/terminal/ai_chat/slash.rs, src/app/ui/terminal/ai_chat/approval.rs, src/app/ui/background.rs, src/app/ai_core/executor.rs</files>
  <action>
    1. Přidej nebo aktualizuj cílené smoke/regression testy pro prompt normalizaci, stream non-freeze zpracování, slash stale-guard a approval flow.
    2. Ověř failure/recovery minimum: toast/error visibility, retry cesta, approval blokace.
    3. Opravy v `src/` drž low-risk a pouze v rozsahu potřebném k návratu testů do green stavu.
  </action>
  <verify>
    <automated>cargo test phase32_runtime_stability -- --nocapture && cargo test slash::tests -- --nocapture && cargo test approval -- --nocapture && cargo test background::tests -- --nocapture</automated>
  </verify>
  <done>
    Klíčové assistant-only toky mají cílené regresní důkazy bez změny produktového chování.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: STAB-01 hard gate uzávěra</name>
  <files>src/, tests/</files>
  <action>
    1. Spusť `cargo check` a `./check.sh`; při failu proveď pouze minimální stabilizační opravy v aktivním scope.
    2. Neprováděj refaktor mimo stabilizační potřebu a nepřidávej nové capability.
    3. Zapiš command-level evidence (co běželo a že je PASS) pro následný docs sign-off.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>
    STAB-01 je uzavřen plným quality gate průchodem po cleanupu CLI vrstvy.
  </done>
</task>

</tasks>

<verification>
cargo test phase32_namespace_guard -- --nocapture
cargo test phase32_runtime_stability -- --nocapture
cargo test slash::tests -- --nocapture
cargo test approval -- --nocapture
cargo test background::tests -- --nocapture
! rg -n "crate::app::cli|app::cli" src/app/ui/terminal src/app/ai_core
cargo check
./check.sh
</verification>

<verification_criteria>
- STAB-01: `cargo check` a `./check.sh` jsou PASS bez výjimek.
- STAB-02: existují phase32 testy hlídající nový namespace a klíčové assistant-only runtime toky.
- Prompt/stream/slash/approval regression priority je pokryta automatizovanými testy a recovery scénáři.
- Změny nepřidávají nové capability ani neporušují assistant-only boundary.
</verification_criteria>

<success_criteria>
- Stabilizační testy jsou green a mapují se přímo na STAB-01/STAB-02.
- Quality gate je deterministicky průchodný po phase 31 migraci.
- Kódová změna je minimální, cílená a bez scope creep.
</success_criteria>
