---
phase: 38-watcher-stability-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/app/ui/background.rs
  - src/app/ui/file_tree/mod.rs
  - tests/phase38_background_orchestration.rs
autonomous: true
requirements:
  - RELIAB-03

must_haves:
  truths:
    - "Background pipeline spotrebuje watcher batch deterministicky a bez O(event_count) granular replaye pri overflow." 
    - "Overflow fallback aktivuje jeden full reload trigger na batch, ne reload loop."
    - "Delete->restore sekvence v orchestraci nevede k opakovanemu lagujicimu preindexovani UI."
    - "Error/disconnect vetve watcher channelu zustavaji fail-visible a nenarusuji UI responsiveness."
  artifacts:
    - path: "src/app/ui/background.rs"
      provides: "Batch-aware orchestrace watcher eventu a overflow fallback handoff"
      contains: "process_background_events|watcher batch handling|single reload fallback"
    - path: "src/app/ui/file_tree/mod.rs"
      provides: "Deterministicky reload handoff helpery pro fallback path"
      contains: "request_reload|request_reload_and_expand"
    - path: "tests/phase38_background_orchestration.rs"
      provides: "Dukazy, ze fallback nespousti reload storm a bezny batch je dedupe-applied"
      contains: "phase38_overflow_triggers_single_reload|phase38_batch_applies_deduped_changes"
  key_links:
    - from: "Overflow -> single reload orchestrace"
      to: "RELIAB-03"
      via: "Pri burstu se aktivuje kontrolovana degradace bez event storm loopu"
      pattern: "phase38_overflow_triggers_single_reload"
    - from: "Deduped batch apply"
      to: "RELIAB-03"
      via: "Bezna delete/restore sekvence neeskaluje pocet aplikovanych reload/index operaci"
      pattern: "phase38_batch_applies_deduped_changes"
---

<objective>
Napojit watcher stabilizovany batch do UI background orchestrace tak, aby overflow vedl na jeden
safe reload a bezne davky se aplikovaly bez lagujiciho event replaye.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-watcher-stability-verification/38-CONTEXT.md
@.planning/phases/38-watcher-stability-verification/38-RESEARCH.md
@.planning/phases/38-watcher-stability-verification/38-VALIDATION.md
@.planning/phases/38-watcher-stability-verification/38-01-PLAN.md
@./AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrace batch kontraktu do `process_background_events`</name>
  <files>src/app/ui/background.rs, tests/phase38_background_orchestration.rs</files>
  <action>
    1. Spotrebuj novy watcher batch kontrakt a aplikuj pouze deduplikovany seznam zmen.
    2. U bezneho batch path redukuj redundantni reload/index kroky.
    3. Doplnenim testu over, ze aplikace zmen odpovida finalnimu dedupe vystupu.
  </action>
  <verify>
    <automated>cargo test phase38_batch_applies_deduped_changes -- --nocapture</automated>
  </verify>
  <done>
    Background vrstva zpracovava jen stabilizovany watcher output.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Overflow fallback na single reload handoff</name>
  <files>src/app/ui/background.rs, src/app/ui/file_tree/mod.rs, tests/phase38_background_orchestration.rs</files>
  <action>
    1. Pri `overflowed=true` proved jednotny fallback: jeden reload handoff misto granularniho replaye.
    2. Osetri guard, aby v jednom polling cyklu nedochazelo k vice reload triggerum pro stejny overflow batch.
    3. Doplnenim testu over, ze overflow aktivuje maximalne jeden reload trigger.
  </action>
  <verify>
    <automated>cargo test phase38_overflow_triggers_single_reload -- --nocapture</automated>
  </verify>
  <done>
    Overflow path je stabilni, ciselne omezena a pripravena proti reload stormu.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Disconnect/error path bez UI regresi</name>
  <files>src/app/ui/background.rs, tests/phase38_background_orchestration.rs</files>
  <action>
    1. Potvrd fail-visible handling watcher disconnect/error vetve v background pipeline.
    2. Udrz konzistentni behavior bez panic a bez nekonecneho pokusu o cteni odpojeneho channelu.
    3. Doplnenim focused testu over graceful shutdown branch.
  </action>
  <verify>
    <automated>cargo test phase38_watcher_disconnect_handled_once -- --nocapture</automated>
  </verify>
  <done>
    Error vetve watcher pollingu nevyvolavaji UI lag loop.
  </done>
</task>

</tasks>

<verification>
cargo test phase38_background_orchestration -- --nocapture
cargo check
rg -n "overflow|request_reload|process_background_events|watcher" src/app/ui/background.rs -S
</verification>

<verification_criteria>
- RELIAB-03: overflow fallback spousti jednotny reload handoff a netvori reload storm.
- Bezne delete/restore batche se aplikuji dedupe-aware a nezvysuji zbytecne UI praci.
- Disconnect/error path je fail-visible a nezaklada polling loop regresi.
</verification_criteria>

<success_criteria>
- Wave 2 propoji watcher a UI pipeline bez driftu do jinych subsystému.
- Existuji focused orchestration testy mapovane na RELIAB-03.
</success_criteria>

<output>
Po dokonceni vytvor `.planning/phases/38-watcher-stability-verification/38-02-SUMMARY.md`.
</output>
