---
phase: 10-ui-state-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/ui/workspace/modal_dialogs/sandbox.rs
  - src/app/ui/workspace/modal_dialogs.rs
  - src/app/ui/workspace/modal_dialogs/settings.rs
  - src/app/ui/terminal/bottom/build_bar.rs
  - src/app/ui/file_tree/mod.rs
  - src/app/ui/file_tree/render.rs
  - src/app/ui/panels.rs
  - src/app/ui/workspace/state/init.rs
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - STATE-01
  - STATE-02
  - STATE-03
  - STATE-04

must_haves:
  truths:
    - "Settings dialog neobsahuje sandbox toggle, tooltip ani inline poznamku"
    - "Build bar nezobrazuje Terminal label se sandbox hover textem"
    - "File tree nepouziva is_sandbox parametr a line count je globalni feature"
    - "Soubor modal_dialogs/sandbox.rs neexistuje"
    - "Gitignore filtr nefiltruje slovo sandbox"
    - "Projekt se kompiluje (cargo check)"
  artifacts:
    - path: "src/app/ui/workspace/modal_dialogs/settings.rs"
      provides: "Settings dialog bez sandbox bloku"
      contains: "else if selected_cat == \"editor\""
    - path: "src/app/ui/file_tree/render.rs"
      provides: "File tree s globalnim line count"
      contains: "if node.line_count.is_none()"
    - path: "src/app/ui/terminal/bottom/build_bar.rs"
      provides: "Build bar bez Terminal labelu"
  key_links:
    - from: "src/app/ui/panels.rs"
      to: "src/app/ui/file_tree/mod.rs"
      via: "file_tree.ui() call bez is_sandbox"
      pattern: "file_tree\\.ui\\(ui, i18n\\)"
    - from: "src/app/ui/workspace/modal_dialogs.rs"
      to: "modal_dialogs/sandbox.rs"
      via: "mod sandbox deklarace odstranena"
      pattern: "mod sandbox"
---

<objective>
Odstranit vsechny sandbox UI prvky z editoru: settings dialog sandbox blok, modal_dialogs/sandbox.rs, build bar Terminal label, file tree is_sandbox parametr, a gitignore sandbox filtr.

Purpose: Uzivatel nesmi videt zadne sandbox prvky v UI. State fieldy (STATE-01 az STATE-04) a toast akce (UI-06) byly jiz odstraneny v Phase 9.
Output: Cista kompilace bez sandbox UI prvku.
</objective>

<execution_context>
@/home/stkremen/.claude/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ui-state-cleanup/10-CONTEXT.md
@.planning/phases/10-ui-state-cleanup/10-RESEARCH.md

<interfaces>
<!-- Soubory k uprave — executor nepotrebuje dalsi exploraci -->

From src/app/ui/workspace/modal_dialogs.rs:
```rust
// Radky 12-14 k smazani:
// TODO: Phase 10 — remove modal_dialogs/sandbox.rs entirely
#[cfg(never)]
mod sandbox;

// Radek 85 k smazani:
// 6a. Sandbox sync dialog removed (Phase 9)
```

From src/app/ui/workspace/modal_dialogs/settings.rs:
```rust
// Radky 272-294 k smazani: separator, sandbox checkbox stub, tooltip, hint, terminal note
// Blok konci pred "} else if selected_cat == \"editor\""
```

From src/app/ui/terminal/bottom/build_bar.rs:
```rust
// Radky 14-16 k smazani:
let mode_label = "Terminal".to_string();
ui.label(egui::RichText::new(mode_label))
    .on_hover_text(i18n.get("hover-build-sandbox"));
```

From src/app/ui/file_tree/mod.rs:
```rust
// Radek 96: odstranit is_sandbox: bool z parametru ui()
// Radek 125: odstranit is_sandbox z volani Self::show_node()
```

From src/app/ui/file_tree/render.rs:
```rust
// Radek 31: odstranit is_sandbox: bool z parametru show_node()
// Radek 61: odstranit is_sandbox z rekurzivniho volani show_node()
// Radek 115: zmenit "if is_sandbox && node.line_count.is_none()" na "if node.line_count.is_none()"
// Radek 132: zmenit "if is_sandbox && let Some(count)..." na "if let Some(count)..."
```

From src/app/ui/panels.rs:
```rust
// Radek 48: zmenit "ws.file_tree.ui(ui, i18n, false)" na "ws.file_tree.ui(ui, i18n)"
```

From src/app/ui/workspace/state/init.rs:
```rust
// Radek 218: zmenit 'if line.contains(".polycredo") || line.contains("sandbox")' na 'if line.contains(".polycredo")'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smazat sandbox modal a vycistit settings/build_bar/modal_dialogs</name>
  <files>
    src/app/ui/workspace/modal_dialogs/sandbox.rs
    src/app/ui/workspace/modal_dialogs.rs
    src/app/ui/workspace/modal_dialogs/settings.rs
    src/app/ui/terminal/bottom/build_bar.rs
    src/app/ui/workspace/state/init.rs
  </files>
  <action>
1. Smazat soubor `src/app/ui/workspace/modal_dialogs/sandbox.rs` (celych 80 radku, 100% sandbox-only)

2. V `modal_dialogs.rs`:
   - Smazat radky 12-14 (TODO komentar + #[cfg(never)] mod sandbox;)
   - Smazat radek 85 (komentar "6a. Sandbox sync dialog removed (Phase 9)")

3. V `settings.rs`:
   - Smazat radky 272-294 (cely sandbox blok v kategorii "general"): separator, ui.add_space, allocate_ui_with_layout se sandbox checkbox stub, tooltip on_hover_text, hint label a terminal-note label
   - Ponechat vse pred (vyber jazyka, default project path) a za (else if selected_cat == "editor")
   - NEPOUZIVAT i18n klice sandbox — zustanou osirele az do Phase 12

4. V `build_bar.rs`:
   - Smazat radky 14-16: let mode_label, ui.label(RichText::new(mode_label)).on_hover_text(...)
   - Ponechat separator na radku 18

5. V `state/init.rs`:
   - Radek 218: zmenit `if line.contains(".polycredo") || line.contains("sandbox")` na `if line.contains(".polycredo")`
   - Ponechat zbytek gitignore logiky beze zmeny

6. Spustit `cargo check` a opravit pripadne kompilacni chyby
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor && cargo check 2>&1</automated>
  </verify>
  <done>
    - sandbox.rs modal soubor neexistuje
    - modal_dialogs.rs neobsahuje "mod sandbox" ani TODO komentar
    - settings.rs neobsahuje sandbox checkbox/tooltip/hint blok (radky 272-294 smazany)
    - build_bar.rs neobsahuje "Terminal" mode_label ani hover-build-sandbox
    - init.rs gitignore filtr neobsahuje "sandbox"
    - cargo check prochazi
  </done>
</task>

<task type="auto">
  <name>Task 2: Odstranit is_sandbox parametr z file tree</name>
  <files>
    src/app/ui/file_tree/mod.rs
    src/app/ui/file_tree/render.rs
    src/app/ui/panels.rs
  </files>
  <action>
1. V `file_tree/mod.rs`:
   - Radek 96: odstranit parametr `is_sandbox: bool` ze signatury `pub fn ui()`
   - Radek 125: odstranit argument `is_sandbox` z volani `Self::show_node(...)`

2. V `file_tree/render.rs`:
   - Radek 31: odstranit parametr `is_sandbox: bool` ze signatury `pub fn show_node()`
   - Radek 61: odstranit argument `is_sandbox` z rekurzivniho volani `show_node()`
   - Radek 115: zmenit `if is_sandbox && node.line_count.is_none()` na `if node.line_count.is_none()` — line count se stava globalni feature
   - Radek 132: zmenit `if is_sandbox && let Some(count) = node.line_count && count >= 500` na `if let Some(count) = node.line_count && count >= 500` — zvyrazneni velkych souboru se stava globalni feature

3. V `panels.rs`:
   - Radek 48: zmenit `ws.file_tree.ui(ui, i18n, false)` na `ws.file_tree.ui(ui, i18n)`

4. Spustit `cargo check` a opravit pripadne kompilacni chyby
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor && cargo check 2>&1 && cargo test 2>&1</automated>
  </verify>
  <done>
    - file_tree/mod.rs: ui() nema is_sandbox parametr
    - file_tree/render.rs: show_node() nema is_sandbox parametr, line count a zvyrazneni velkych souboru funguji pro vsechny soubory (globalni feature)
    - panels.rs: volani file_tree.ui() bez tretiho argumentu
    - cargo check a cargo test prochazi
  </done>
</task>

</tasks>

<verification>
- `cargo check` — projekt se kompiluje bez chyb
- `cargo test` — vsechny existujici testy prochazi
- `grep -r "is_sandbox" src/app/ui/file_tree/` — zadne vysledky
- `grep -r "mod sandbox" src/app/ui/workspace/modal_dialogs.rs` — zadne vysledky
- `ls src/app/ui/workspace/modal_dialogs/sandbox.rs` — soubor neexistuje
- `grep "sandbox" src/app/ui/workspace/state/init.rs` — zadne vysledky
</verification>

<success_criteria>
1. Soubor modal_dialogs/sandbox.rs neexistuje
2. Settings dialog neobsahuje sandbox toggle/tooltip/hint blok
3. Build bar neobsahuje Terminal label se sandbox hover textem
4. File tree nepouziva is_sandbox parametr — line count a zvyrazneni jsou globalni
5. Gitignore filtr nefiltruje slovo "sandbox"
6. cargo check a cargo test prochazi
</success_criteria>

<output>
After completion, create `.planning/phases/10-ui-state-cleanup/10-01-SUMMARY.md`
</output>
