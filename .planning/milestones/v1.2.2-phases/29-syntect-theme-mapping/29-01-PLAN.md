---
phase: 29-syntect-theme-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/settings.rs
autonomous: true
requirements:
  - SYNTAX-01
  - SYNTAX-02

must_haves:
  truths:
    - "Každá light varianta (WarmIvory, CoolGray, Sepia, WarmTan) mapuje na odlišné syntect téma."
    - "Dark varianty (Default, Midnight) mapují na odlišná syntect dark témata."
    - "Při chybějícím mapovaném tématu aplikace nepadá a vrací bezpečný fallback s warning logem."
    - "Mapování je deterministické a testované na úrovni Settings (bez duplikace logiky v UI)."
  artifacts:
    - path: "src/settings.rs"
      provides: "Kompletní mapovací matice variant -> syntect theme + fallback resolver"
      contains: "pub fn syntect_theme_name"
    - path: "src/settings.rs"
      provides: "Unit gate testy pro SYNTAX-01 a SYNTAX-02"
      contains: "syntax01_"
  key_links:
    - from: "Settings.light_variant"
      to: "Settings::syntect_theme_name()"
      via: "match self.light_variant"
      pattern: "LightVariant::WarmTan"
    - from: "Settings.dark_variant"
      to: "Settings::syntect_theme_name()"
      via: "match self.dark_variant"
      pattern: "DarkVariant::Midnight"
    - from: "Settings::syntect_theme_name()"
      to: "Highlighter::set_theme/highlight"
      via: "existing editor render path"
      pattern: "theme_name"
---

<objective>
Nastavit robustní a testované mapování syntect témat pro všech 6 barevných variant (4 light + 2 dark), včetně fallbacku bez pádu.

Purpose: splnit SYNTAX-01 a SYNTAX-02 bez zásahu do UI architektury.
Output: explicitní mapovací matice v `Settings`, validovaný fallback a unit test gate.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
@/home/stkremen/.codex/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/29-syntect-theme-mapping/29-CONTEXT.md
@.planning/phases/29-syntect-theme-mapping/29-RESEARCH.md
@.planning/phases/29-syntect-theme-mapping/29-VALIDATION.md
@src/settings.rs
@src/highlighter.rs

Boundary:
- Scope: pouze mapování + fallback + test gate.
- Out of scope: nové UI capability, custom tmTheme loader, refaktor highlight pipeline.
</context>

<tasks>

<task id="29-01-01" type="auto">
  <name>Zafixovat explicitní mapovací matici všech variant v Settings</name>
  <files>src/settings.rs</files>
  <action>
    1. Upravit `Settings::syntect_theme_name()` na explicitní větvení bez hrubého dark/light splitu:
       - 4 light varianty: WarmIvory, CoolGray, Sepia, WarmTan
       - 2 dark varianty: Default, Midnight
    2. Zavést explicitní checkpoint rozhodnutí pro `light` mapování:
       - pouze built-in témata z `ThemeSet::load_defaults().themes`,
       - 4/4 light varianty musí mít unikátní názvy témat.
    3. Pokud není dostupný vhodný 4. light built-in kandidát, označit to jako blocker fáze:
       - nezavádět tichou výjimku ani duplicitu,
       - zapsat blocker stav + důvod do summary výstupu fáze.
    4. Zajistit vizuální charakter mapování podle context rozhodnutí:
       - `WarmIvory` = baseline neutrální light,
       - `WarmTan` = teplejší/měkčí než `WarmIvory`,
       - `Midnight` = chladnější než `Default`.
    5. Zajistit, že mapování `DarkVariant::Default` a `DarkVariant::Midnight` je odlišné.
    6. Nepřesouvat mapování do UI/highlighter modulu; zdroj pravdy zůstává v `Settings`.
  </action>
  <verify>
    <automated>cargo test -q syntax01_light_mapping_matrix_complete --lib</automated>
    <automated>cargo test -q syntax01_light_mapping_unique --lib</automated>
    <manual>Potvrdit v task summary, že checkpoint 4/4 unique + built-in only je splněn, nebo je explicitně zapsán blocker.</manual>
  </verify>
  <done>
    `syntect_theme_name()` pokrývá všech 6 variant explicitně, vrací deterministické názvy témat a obsahuje rozhodnutí 4/4 unikátních light built-in mapování (nebo explicitní blocker stav).
  </done>
</task>

<task id="29-01-02" type="auto">
  <name>Přidat validaci názvu tématu a bezpečný fallback s warning logem</name>
  <files>src/settings.rs</files>
  <action>
    1. Přidat helper, který ověří dostupnost mapovaného theme name proti `ThemeSet::load_defaults().themes`.
    2. Pokud název neexistuje, vrátit fallback (`base16-ocean.dark`) a vypsat warning log.
    3. Udržet fallback testovatelný bez GUI a bez blokujících operací v UI vlákně.
  </action>
  <verify>
    <automated>cargo test -q syntect_theme_fallback_contract --lib</automated>
  </verify>
  <done>
    Chybějící syntect téma nevede k pádu; fallback je konzistentní a signalizovaný warningem.
  </done>
</task>

<task id="29-01-03" type="auto">
  <name>Doplnit unit gate testy a uzavřít requirement coverage</name>
  <files>src/settings.rs</files>
  <action>
    1. Doplnit testy s traceability na requirement ID:
       - `syntax01_light_mapping_matrix_complete`
       - `syntax01_light_mapping_unique`
       - `syntax02_dark_mapping_matrix_complete`
       - `syntax02_dark_variants_are_distinct`
       - `syntect_theme_fallback_contract`
    2. V testech ověřit:
       - 4/4 light mapování je unikátní,
       - 2/2 dark mapování je vyplněné a odlišné,
       - fallback vrací validní bezpečné téma.
    3. Doplnit ruční verify checkpoint pro vizuální charakter:
       - `WarmIvory` baseline neutrální light,
       - `WarmTan` teplejší/měkčí než `WarmIvory`,
       - `Midnight` chladnější než `Default`.
  </action>
  <verify>
    <automated>cargo test -q syntax02_dark_variants_are_distinct --lib</automated>
    <manual>Přepnout 4 light + 2 dark varianty na stejném souboru a potvrdit požadovaný vizuální charakter mapování (WarmIvory baseline, WarmTan teplejší/měkčí, Midnight chladnější než Default).</manual>
  </verify>
  <done>
    Unit gate pokrývá oba requirementy, chrání proti regresi mapování a verify checklist obsahuje explicitní vizuální kontrolu charakteru variant.
  </done>
</task>

</tasks>

<wave_verification>
- Wave/final gate po dokončení všech tasků:
  - `cargo check`
  - `./check.sh` (informational, pokud selže jen na známém out-of-scope fmt driftu)
</wave_verification>

<verification>
- [ ] `syntect_theme_name()` má explicitní mapování všech 6 variant
- [ ] 4 light varianty mapují na 4 odlišné názvy syntect témat
- [ ] 2 dark varianty mapují na 2 odlišné dark názvy syntect témat
- [ ] Checkpoint 4/4 unique light + built-in only je explicitně uzavřen (nebo je explicitně zapsán blocker s důvodem)
- [ ] Vizuální charakter je potvrzen: WarmIvory baseline neutrální light, WarmTan teplejší/měkčí, Midnight chladnější než Default
- [ ] Fallback při missing theme je bezpečný + warning log
- [ ] `cargo check` je green (wave/final gate)
- [ ] `./check.sh` je green nebo má zdokumentovanou známou out-of-scope odchylku (wave/final informational gate)
</verification>

<success_criteria>
Syntax highlighting používá správná a odlišná syntect témata pro každou light i dark variantu, bez tichých pádů při chybějícím tématu.
</success_criteria>

<output>
Po exekuci vytvořit `.planning/phases/29-syntect-theme-mapping/29-01-SUMMARY.md`.
</output>
