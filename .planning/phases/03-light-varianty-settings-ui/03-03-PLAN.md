---
phase: 03-light-varianty-settings-ui
plan: 03
type: tdd
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - src/app/ui/workspace/modal_dialogs/settings.rs
  - src/app/ui/workspace/modal_dialogs.rs
  - src/app/ui/workspace/state/mod.rs
  - src/app/ui/workspace/state/init.rs
  - src/settings.rs
autonomous: true
requirements: [SETT-03, SETT-02]

must_haves:
  truths:
    - "Pri otevreni Settings se ulozi puvodni snapshot; `Cancel` vrati runtime settings na snapshot a zahodi draft zmeny."
    - "`Save` porovnava theme fingerprint `(dark_theme, light_variant)` mezi snapshotem a draftem; disk persist do `settings.toml` probehne jen pri realne zmene fingerprintu."
    - "Storage kontrakt je explicitni: canonical soubor je `settings.toml`, `settings.json` je pouze legacy kompatibilni vstup pro migraci na TOML."
    - "Globalni confirm-discard flow pro zavreni modalu uklidi `settings_draft` i `settings_original`, aby nezustal leak preview stavu."
    - "Persistence cesta je testovana: roundtrip save/load v `settings.toml` + legacy migrace `settings.json -> settings.toml` zachova variantu i rezim."
  artifacts:
    - path: "src/app/ui/workspace/state/mod.rs"
      provides: "Doplneny snapshot field pro settings modal lifecycle."
      contains: "settings_original"
    - path: "src/app/ui/workspace/modal_dialogs/settings.rs"
      provides: "Save/Cancel logika s preview revert semantikou + fingerprint diff guard."
      contains: "SettingsModalAction::Cancel"
    - path: "src/settings.rs"
      provides: "Persistence testy pro dark_theme/light_variant + legacy migration + fingerprint helper testy."
      contains: "settings.toml"
  key_links:
    - from: "Settings modal open"
      to: "WorkspaceState snapshot"
      via: "settings_original capture"
      pattern: "settings_original"
    - from: "SettingsModalAction::Save"
      to: "settings.toml write"
      via: "theme fingerprint diff guard"
      pattern: "theme_fingerprint|SettingsModalAction::Save"
    - from: "SettingsModalAction::Cancel"
      to: "AppShared.settings"
      via: "restore snapshot + settings_version bump"
      pattern: "SettingsModalAction::Cancel"
    - from: "Settings::load/save"
      to: "canonical + legacy storage contract"
      via: "`settings.toml` write + `settings.json` migrate"
      pattern: "OLD_SETTINGS_FILE|SETTINGS_FILE"
---

<objective>
Uzavrit persistence a modal semantiku tak, aby `Save/Cancel` fungovaly korektne i pri live preview, vcetne fingerprint diff guardu a jasneho storage kontraktu.

Purpose: Dokoncit SETT-03 bez rozsirovani scope mimo settings lifecycle.
Output: Snapshot-aware Settings modal, cleanup v global discard flow, Save-only-on-theme-change guard, a testy canonical/legacy persistence kontraktu.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-light-varianty-settings-ui/03-CONTEXT.md
@.planning/phases/03-light-varianty-settings-ui/03-RESEARCH.md
@.planning/phases/03-light-varianty-settings-ui/03-VALIDATION.md
@.planning/phases/03-light-varianty-settings-ui/03-01-PLAN.md
@.planning/phases/03-light-varianty-settings-ui/03-02-PLAN.md
@src/app/ui/workspace/modal_dialogs/settings.rs
@src/app/ui/workspace/modal_dialogs.rs
@src/app/ui/workspace/state/mod.rs
@src/app/ui/workspace/state/init.rs
@src/settings.rs

<interfaces>
```rust
// cilovy lifecycle semantiky
// open settings: settings_original = current shared settings
// live preview: shared.settings <- draft
// Save:
//   if theme_fingerprint(draft) != theme_fingerprint(settings_original) { draft.save(); }
//   clear snapshot
// Cancel: shared.settings <- settings_original; clear draft + snapshot
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Snapshot lifecycle pro Save/Cancel pri live preview</name>
  <files>src/app/ui/workspace/state/mod.rs, src/app/ui/workspace/state/init.rs, src/app/ui/workspace/modal_dialogs/settings.rs</files>
  <action>
    1. Dopl `WorkspaceState` o `settings_original: Option<Settings>` a inicializuj na `None`.
    2. Pri prvnim otevreni settings modalu zaloz snapshot z aktualnich `shared.settings`.
    3. `Save`: porovnej `theme_fingerprint(settings_original)` vs `theme_fingerprint(draft)`; `draft.save()` proved jen pri rozdilu, runtime + i18n nastav jako dnes, pak vycisti `settings_draft` i `settings_original`.
    4. `Cancel`: vrat runtime na `settings_original`, bumpni `settings_version` jen kdyz je treba, vycisti draft i snapshot.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor/.polycredo/sandbox && RUSTC_WRAPPER= cargo check</automated>
    <manual>
      1) Otevri Settings, prepnout theme/variantu, potvrdit vizualni preview.
      2) Klikni Cancel.
      3) Over, ze aplikace se vrati na puvodni theme/variantu.
      4) Otevri Settings bez zmeny, klikni Save a over, ze Save probehne bez zbytecneho prepisu settings souboru.
    </manual>
  </verify>
  <done>
    Save/Cancel semantika je konzistentni i se zapnutym live preview a Save persistuje pouze realnou zmenu theme fingerprintu.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cleanup pri global confirm-discard</name>
  <files>src/app/ui/workspace/modal_dialogs.rs, src/app/ui/workspace/modal_dialogs/settings.rs</files>
  <action>
    1. V globalnim discard branchi zajisti uklid `settings_original` stejne jako `settings_draft`.
    2. Pokud byl aplikovan preview stav, proved revert na snapshot pred zavrenim modalu.
    3. Zachovej stavajici behavior pro ostatni modaly (plugins/new project/ai) bez vedlejsich zmen.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor/.polycredo/sandbox && RUSTC_WRAPPER= cargo check</automated>
  </verify>
  <done>
    Zavreni settings pres discard nenechava runtime v neulozenem preview stavu.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persistence testy pro SETT-03 (canonical settings.toml + legacy migrace)</name>
  <files>src/settings.rs</files>
  <action>
    1. Rozsir testy v `settings.rs` o overeni, ze `dark_theme` + `light_variant` prezijou save/load roundtrip v canonical `settings.toml`.
    2. Dopl test legacy migrace: simulovany `settings.json` obsah se nacte, prepise na `settings.toml`, a zachova `light_variant`.
    3. Testy izoluj pres docasny config dir (napr. env override), aby nezasahovaly realny user config.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor/.polycredo/sandbox && RUSTC_WRAPPER= cargo test settings::tests -- --nocapture</automated>
  </verify>
  <done>
    Automaty potvrzuji canonical `settings.toml` persistenci i kompatibilitu legacy `settings.json` migrace.
  </done>
</task>

</tasks>

<verification>
- `RUSTC_WRAPPER= cargo test settings::tests -- --nocapture` je zeleny.
- `RUSTC_WRAPPER= cargo check` je zeleny.
- `rg -n "settings_original|theme_fingerprint|SettingsModalAction::(Save|Cancel)" src/app/ui/workspace/modal_dialogs/settings.rs src/app/ui/workspace/state/mod.rs` potvrdi snapshot/revert + Save diff guard wiring.
- `rg -n "OLD_SETTINGS_FILE|SETTINGS_FILE" src/settings.rs` potvrdi canonical/legacy storage kontrakt.
- Manual smoke: Save prezije restart, Cancel vrati puvodni vzhled, discard modal nezachova neulozeny preview, Save bez zmeny fingerprintu neprepise `settings.toml`.
</verification>

<success_criteria>
- `SETT-03`: Ulozene dark/light + light variant se po restartu aplikace korektne obnovi z canonical `settings.toml`; legacy `settings.json` zustava kompatibilni migracni vstup.
- `SETT-02`: Live preview zustava funkcni a Cancel/discard korektne revertuji puvodni stav.
- Lock: `Save` uklada pouze pri realne zmene theme fingerprintu `(dark_theme, light_variant)`.
- Snapshot lifecycle nezanechava stale state v `WorkspaceState` po zavreni modalu.
- Zmena je lokalizovana na settings lifecycle a persistence testy, bez refaktoru mimo scope.
</success_criteria>

<output>
Po dokonceni vytvor `.planning/phases/03-light-varianty-settings-ui/03-03-SUMMARY.md`.
</output>
