---
phase: 17-i18n-wasm-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/app/registry/plugins/mod.rs
  - src/app/registry/plugins/security.rs
  - src/app/registry/plugins/types.rs
  - src/app/registry/plugins/host/mod.rs
  - src/app/registry/plugins/host/fs.rs
  - src/app/registry/plugins/host/sys.rs
  - src/app/registry/plugins/host/search.rs
  - src/plugins/ollama/src/lib.rs
  - src/plugins/gemini/src/lib.rs
  - src/plugins/hello/src/lib.rs
  - src/app/ui/workspace/modal_dialogs/plugins.rs
  - src/app/registry/mod.rs
  - src/app/mod.rs
  - src/app/types.rs
  - src/app/ui/workspace/menubar/file.rs
  - src/app/ui/workspace/menubar/mod.rs
  - src/app/ui/workspace/modal_dialogs.rs
  - src/app/ui/workspace/mod.rs
  - src/app/ui/workspace/state/mod.rs
  - src/app/ui/workspace/state/init.rs
  - src/settings.rs
  - Cargo.toml
  - locales/cs/ui.ftl
  - locales/en/ui.ftl
  - locales/de/ui.ftl
  - locales/ru/ui.ftl
  - locales/sk/ui.ftl
  - locales/cs/menu.ftl
  - locales/en/menu.ftl
  - locales/de/menu.ftl
  - locales/ru/menu.ftl
  - locales/sk/menu.ftl
autonomous: true
requirements:
  - CLEN-02

must_haves:
  truths:
    - "WASM plugin system je kompletne odstranen — zadne extism, PluginManager, plugin source"
    - "Editor kompiluje a funguje bez WASM runtime"
    - "Menu neobsahuje polozku Plugins"
    - "Settings neobsahuje plugin-specificka pole (PluginSettings, migration flag)"
    - "Plugin-related i18n klice jsou odstraneny z ui.ftl a menu.ftl"
    - "Vsechny testy prochazi"
  artifacts:
    - path: "Cargo.toml"
      provides: "No extism dependency"
    - path: "src/app/registry/mod.rs"
      provides: "Clean registry without WASM plugin references"
    - path: "src/app/types.rs"
      provides: "AppAction without Plugin* variants"
  key_links:
    - from: "src/app/mod.rs"
      to: "src/app/registry/mod.rs"
      via: "Registry initialization without PluginManager"
      pattern: "Registry::new"
    - from: "src/app/types.rs"
      to: "src/app/mod.rs"
      via: "AppAction handlers — no Plugin* variants"
      pattern: "AppAction"
---

<objective>
WASM plugin system removal: delete ~3000 LOC of plugin infrastructure, clean up all references

Purpose: Complete removal of the legacy WASM plugin system (extism, PluginManager, plugin source code, samples) that has been replaced by native Rust AI providers in Phases 13-16. Eliminates unnecessary dependency and dead code.
Output: Clean codebase without WASM references, reduced Cargo.toml dependencies, ~3000 LOC deleted
</objective>

<execution_context>
@/home/stkremen/.claude/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-i18n-wasm-cleanup/17-CONTEXT.md
@.planning/phases/17-i18n-wasm-cleanup/17-RESEARCH.md
@.planning/phases/17-i18n-wasm-cleanup/17-01-SUMMARY.md

@src/app/registry/mod.rs
@src/app/mod.rs
@src/app/types.rs
@src/app/ui/workspace/menubar/file.rs
@src/app/ui/workspace/menubar/mod.rs
@src/app/ui/workspace/modal_dialogs.rs
@src/app/ui/workspace/mod.rs
@src/app/ui/workspace/state/mod.rs
@src/settings.rs
@Cargo.toml
@locales/en/ui.ftl
@locales/en/menu.ftl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove WASM plugin code, types, and UI references</name>
  <files>
    src/app/registry/plugins/mod.rs,
    src/app/registry/plugins/security.rs,
    src/app/registry/plugins/types.rs,
    src/app/registry/plugins/host/mod.rs,
    src/app/registry/plugins/host/fs.rs,
    src/app/registry/plugins/host/sys.rs,
    src/app/registry/plugins/host/search.rs,
    src/plugins/ollama/src/lib.rs,
    src/plugins/gemini/src/lib.rs,
    src/plugins/hello/src/lib.rs,
    src/app/ui/workspace/modal_dialogs/plugins.rs,
    src/app/registry/mod.rs,
    src/app/mod.rs,
    src/app/types.rs,
    src/app/ui/workspace/menubar/file.rs,
    src/app/ui/workspace/menubar/mod.rs,
    src/app/ui/workspace/modal_dialogs.rs,
    src/app/ui/workspace/mod.rs,
    src/app/ui/workspace/state/mod.rs,
    src/app/ui/workspace/state/init.rs,
    src/settings.rs,
    Cargo.toml
  </files>
  <action>
    Follow the WASM deletion dependency graph from RESEARCH.md. After each major step, run `cargo check` to catch broken references early.

    **Step 1 — Remove menu entry (menubar/file.rs):**
    Remove the "Plugins" / "Plugin Manager" menu item. In menubar/mod.rs, remove any plugin action handling.

    **Step 2 — Remove plugins modal (modal_dialogs/):**
    Delete `src/app/ui/workspace/modal_dialogs/plugins.rs`.
    In `modal_dialogs.rs` (or mod.rs): remove `pub mod plugins;` and any plugins modal rendering/trigger.

    **Step 3 — Remove Plugin* AppAction variants (types.rs):**
    Remove `AppAction::PluginLoaded`, `AppAction::PluginOutput`, `AppAction::PluginError`, `AppAction::PluginApprovalResponse`, and any other `Plugin*` variants from the `AppAction` enum.
    Remove `PluginApprovalResponse` enum if it exists.
    Remove the old `pending_plugin_approval`, `pending_ask_user` fields and old `render_approval_ui`/`render_ask_user_ui` functions — only keep the native `render_tool_approval_ui`/`render_tool_ask_ui` from Phase 16.

    **Step 4 — Remove plugin action handlers (app/mod.rs):**
    Remove match arms for deleted `AppAction::Plugin*` variants.
    Remove plugin initialization code (PluginManager::new, plugin loading on startup).
    Remove any `use` imports for plugin types.

    **Step 5 — Remove plugin commands from registry (registry/mod.rs):**
    Remove `pub mod plugins;` declaration.
    Remove `use ... PluginManager` and plugin-related imports.
    Remove `plugins: Arc<PluginManager>` field from `Registry` struct.
    Remove `CommandAction::Plugin` variant if it exists (or its handler).
    Remove plugin commands from `init_defaults()` (command-name-plugin-hello, command-name-plugin-gemini, command-name-plugin-ollama, command-name-plugin-ai-chat, command-name-show-plugins).
    Simplify `Registry::new()` if `project_root` parameter was only needed for plugins.
    Keep PanelRegistry and PanelArea (per research recommendation — small generic infrastructure).

    **Step 6 — Remove plugin-related state fields (workspace/state/, workspace/mod.rs):**
    Remove `show_plugins`, `plugins_draft` and any other plugin-related fields from WorkspaceState.
    Update workspace/state/init.rs if it references plugin keys.

    **Step 7 — Delete plugin source directories:**
    ```bash
    rm -rf src/app/registry/plugins/
    rm -rf src/plugins/
    rm -rf docs/samples/hello-plugin/
    ```

    **Step 8 — Remove settings fields (settings.rs):**
    Remove `plugins: HashMap<String, PluginSettings>` field.
    Remove `PluginSettings` struct.
    Remove `ai_settings_migrated: bool` field (one-time migration flag, no longer needed).
    Remove `migrate_plugin_ai_settings()` function.
    Keep `blacklist` field (renamed to "CLI Blacklist" in Plan 01).

    **Step 9 — Remove extism from Cargo.toml:**
    Remove `extism = "1.5"` (or whatever version) from `[dependencies]`.

    **Step 10 — Final cleanup:**
    Search entire codebase for remaining references: `grep -rn 'plugin\|Plugin\|extism\|PluginManager\|wasm\|WASM' src/ --include='*.rs'`.
    Clean up any orphaned `use` statements or comments.
    Keep only legitimate non-plugin uses of "plugin" word (if any).

    **CRITICAL:** Compile after each major step to catch cascading errors early. The research identifies 14+ files with plugin tentacles.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor && cargo check 2>&1 | tail -10</automated>
  </verify>
  <done>
    - src/app/registry/plugins/ directory deleted (~1931 LOC)
    - src/plugins/ directory deleted (~822 LOC)
    - docs/samples/hello-plugin/ deleted
    - modal_dialogs/plugins.rs deleted
    - AppAction has no Plugin* variants
    - Registry has no PluginManager
    - Settings has no PluginSettings/plugins field
    - extism removed from Cargo.toml
    - No menu entry for Plugins
    - cargo check passes cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove plugin i18n keys and run full test suite</name>
  <files>
    locales/cs/ui.ftl, locales/en/ui.ftl, locales/de/ui.ftl, locales/ru/ui.ftl, locales/sk/ui.ftl,
    locales/cs/menu.ftl, locales/en/menu.ftl, locales/de/menu.ftl, locales/ru/menu.ftl, locales/sk/menu.ftl
  </files>
  <action>
    **Step 1 — Remove plugin keys from ui.ftl (all 5 languages):**
    Delete these keys from ui.ftl in cs, en, de, ru, sk:
    ```
    plugins-title, plugins-list-label, plugins-no-selection, plugins-enabled-label,
    plugins-config-label, plugins-unknown-agent, plugins-category-ai, plugins-category-general,
    plugins-item-settings, plugins-item-welcome, plugins-welcome-title, plugins-welcome-text,
    plugins-welcome-hint, plugins-security-info, plugins-settings-saved,
    plugins-placeholder-api-key, plugins-placeholder-model,
    command-name-show-plugins, command-name-plugin-hello,
    command-name-plugin-gemini, command-name-plugin-ollama, command-name-plugin-ai-chat,
    plugin-auth-bar-msg, plugin-auth-bar-allow, plugin-auth-bar-deny,
    plugin-error-title, plugin-error-heading
    ```

    **Step 2 — Remove plugin keys from menu.ftl (all 5 languages):**
    Delete these keys from menu.ftl in cs, en, de, ru, sk:
    ```
    menu-file-plugins, menu-file-plugins-manager
    ```

    **Step 3 — Verify no orphaned key references remain:**
    Search Rust source for any of the deleted key names to confirm nothing references them:
    ```bash
    grep -rn 'plugins-\|plugin-auth-bar\|plugin-error\|command-name-plugin\|command-name-show-plugins\|menu-file-plugins' src/ --include='*.rs'
    ```
    If any hits found, remove those references too.

    **Step 4 — Run full test suite:**
    ```bash
    cargo test
    ```
    All tests must pass, especially `all_lang_keys_match_english` which validates i18n key parity across all 5 languages.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor && cargo test 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All plugin-* and plugins-* i18n keys removed from ui.ftl (5 languages)
    - menu-file-plugins* keys removed from menu.ftl (5 languages)
    - No orphaned key references in Rust source
    - Full cargo test passes (including all_lang_keys_match_english)
    - Editor compiles and runs without WASM runtime
  </done>
</task>

</tasks>

<verification>
```bash
# Full compilation
cargo check 2>&1 | tail -10

# Full test suite
cargo test 2>&1 | tail -20

# Verify no plugin/WASM references remain in source
grep -rn 'extism\|PluginManager\|PluginSettings' src/ --include='*.rs' | head -10

# Verify plugin directories deleted
ls src/app/registry/plugins/ 2>&1
ls src/plugins/ 2>&1
ls docs/samples/hello-plugin/ 2>&1

# Verify no plugin i18n keys remain
grep -n 'plugins-\|plugin-auth\|plugin-error' locales/en/ui.ftl | head -10
grep -n 'menu-file-plugins' locales/en/menu.ftl | head -10

# Verify extism removed from Cargo.toml
grep 'extism' Cargo.toml
```
</verification>

<success_criteria>
- Zero plugin/WASM source files remain (3 directories deleted)
- Zero extism references in Cargo.toml
- Zero plugin-related AppAction variants or PluginManager references
- Zero plugin-related i18n keys in any FTL file
- Full cargo test passes
- Editor compiles cleanly without WASM runtime
- Net ~3000 LOC deleted
</success_criteria>

<output>
After completion, create `.planning/phases/17-i18n-wasm-cleanup/17-02-SUMMARY.md`
</output>
