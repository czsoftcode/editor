---
phase: 25-unsaved-close-guard
plan: 09
type: execute
wave: 3
depends_on:
  - 25-08
  - 25-10
files_modified:
  - src/app/ui/workspace/mod.rs
  - src/app/ui/editor/tabs.rs
  - src/app/ui/dialogs/confirm.rs
autonomous: true
gap_closure: true
requirements:
  - GUARD-01
  - GUARD-03

must_haves:
  truths:
    - "Esc v unsaved guard dialogu funguje jako Cancel a close flow se bezpečně ukončí."
    - "Během guard flow se fokus nepřepíná zpět do editoru před potvrzením volby uživatele."
    - "Cancel nezavírá tab ani projekt a nevytváří vedlejší změny v obsahu."
  artifacts:
    - path: "src/app/ui/dialogs/confirm.rs"
      provides: "Explicitní mapování Esc -> Cancel pro unsaved guard dialog"
      contains: "keyboard handling Escape"
    - path: "src/app/ui/workspace/mod.rs"
      provides: "Orchestrace guard flow bez předčasného refokusu editoru"
      contains: "process_unsaved_close_guard_dialog"
    - path: "src/app/ui/editor/tabs.rs"
      provides: "Kontrolované použití focus_editor_requested při close guard flow"
      contains: "open_file/focus handoff"
  key_links:
    - from: "ConfirmDialog (Esc / Cancel)"
      to: "workspace guard orchestrace"
      via: "ConfirmDialogResult::Cancel"
      pattern: "confirm.rs -> process_unsaved_close_guard_dialog"
    - from: "workspace guard orchestrace"
      to: "editor focus handoff"
      via: "focus_editor_requested gate až po ukončení flow"
      pattern: "process_unsaved_close_guard_dialog -> tabs.rs focus request"
    - from: "aktivní guard modal"
      to: "blokace návratu fokusu do editoru"
      via: "guard-active condition"
      pattern: "dialog_open_base/guard flag -> tabs focus path"
---

<objective>
Zajistit, že guard modal je skutečně modal: Esc musí fungovat jako Cancel a editor
nesmí přebrat fokus, dokud guard flow neskončí.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/25-unsaved-close-guard/25-UAT.md
@.planning/debug/unsaved-guard-esc-focus.md
@src/app/ui/workspace/mod.rs
@src/app/ui/editor/tabs.rs
@src/app/ui/dialogs/confirm.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Esc -> Cancel v confirm dialogu</name>
  <files>src/app/ui/dialogs/confirm.rs</files>
  <action>
    1. Přidej explicitní zpracování `Escape` pro unsaved guard confirm dialog.
    2. Mapuj Esc na stejnou větev jako tlačítko `Cancel`.
    3. Ověř, že Esc nepropadne do editoru ani do jiných globálních handlerů.
  </action>
  <verify>
    <automated>cargo test unsaved_close_guard_esc_cancel -- --nocapture</automated>
  </verify>
  <done>
    Uživatel může modal zavřít Esc a flow se ukončí stejně jako při Cancel.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Potlačení refokusu editoru během aktivního guard flow</name>
  <files>src/app/ui/workspace/mod.rs, src/app/ui/editor/tabs.rs</files>
  <action>
    1. Uprav orchestrace kroky tak, aby `open_file`/focus request neběžel během aktivního guard dialogu.
    2. Po `Save`/`Discard` posuň flow na další položku pouze bez refokusu editoru mezi kroky.
    3. Po `Cancel` obnov standardní fokusování až po plném ukončení guard flow.
  </action>
  <verify>
    <automated>cargo test unsaved_close_guard_focus_handoff -- --nocapture</automated>
  </verify>
  <done>
    Fokus zůstává na modalu a editor přebere fokus až po uzavření guard flow.
  </done>
</task>

</tasks>

<verification>
cargo check
cargo test unsaved_close_guard -- --nocapture
./check.sh
</verification>

<success_criteria>
- Esc zavolá Cancel větev guard dialogu
- Guard flow nevrací fokus editoru před dokončením uživatelské volby
- Cancel nemá vedlejší efekty (tab zůstává otevřený, obsah beze změny)
</success_criteria>
