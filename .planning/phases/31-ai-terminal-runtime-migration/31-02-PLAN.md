---
phase: 31-ai-terminal-runtime-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - src/app/ui/terminal/ai_chat/logic.rs
  - src/app/ui/terminal/ai_chat/slash.rs
  - src/app/ui/background.rs
  - src/app/ui/workspace/state/mod.rs
  - src/app/ui/terminal/ai_chat/mod.rs
  - src/app/ui/terminal/right/ai_bar.rs
autonomous: true
requirements:
  - TERM-01
  - TERM-02
  - TERM-03

must_haves:
  truths:
    - "Prompt -> stream -> done tok zůstává neblokující a UI během streamu reaguje."
    - "AI terminal běží v assistant-only režimu bez model picker/Ollama UI-runtime couplingu."
    - "Slash/GSD asynchronní placeholder update nemění konverzaci po resetu/generation změně a nepoužívá interní PolyCredo slash příkazy."
  artifacts:
    - path: "src/app/ui/background.rs"
      provides: "Nezablokovaný polling stream eventů a tool/slash eventů"
      contains: "try_recv"
    - path: "src/app/ui/terminal/ai_chat/logic.rs"
      provides: "Stabilní send path pro prompt a start streamu"
      contains: "stream_chat"
    - path: "src/app/ui/terminal/right/ai_bar.rs"
      provides: "AI bar bez model pickeru a bez ws.ai.ollama.* vazeb"
      contains: "ai_bar"
  key_links:
    - from: "Send + stream orchestrace"
      to: "TERM-01"
      via: "Prompt je zpracován a stream se opravdu spustí"
      pattern: "send_query_to_agent"
    - from: "Non-blocking event loop"
      to: "TERM-02"
      via: "UI loop nepoužívá blokující čekání v runtime toku"
      pattern: "try_recv"
    - from: "Assistant-only slash continuity"
      to: "TERM-03"
      via: "External slash + /gsd fungují po migration změnách bez interních PolyCredo slash příkazů"
      pattern: "/gsd|dispatch|external"
---

<objective>
Zpevnit TERM runtime tok (open panel -> send prompt -> streaming -> assistant-only slash interakce)
bez blokování UI, bez model picker/Ollama couplingu a bez regresí slash-GSD chování.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-ai-terminal-runtime-migration/31-CONTEXT.md
@.planning/phases/31-ai-terminal-runtime-migration/31-RESEARCH.md
@.planning/phases/31-ai-terminal-runtime-migration/31-VALIDATION.md
@./CLAUDE.md
@.planning/phases/31-ai-terminal-runtime-migration/31-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TERM-01 send path hardening</name>
  <files>src/app/ui/terminal/ai_chat/logic.rs, src/app/ui/terminal/ai_chat/mod.rs</files>
  <action>
    1. Ověř a oprav stavové přechody při odeslání promptu (validace vstupu, vytvoření user zprávy, start streamu).
    2. Ujisti se, že selhání provideru vrací kontrolovaný user-visible feedback bez rozbití konverzace.
    3. Zachovej stávající UI kontrakt panelu bez redesignu.
  </action>
  <verify>
    <automated>cargo test ai_chat -- --nocapture</automated>
  </verify>
  <done>
    Odeslání promptu je stabilní a panel workflow plní TERM-01.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TERM-02 streaming bez freeze</name>
  <files>src/app/ui/background.rs, src/app/ui/workspace/state/mod.rs</files>
  <action>
    1. Projdi `process_background_events` a odstraň/přepiš blokující runtime body.
    2. Udrž non-blocking polling (`try_recv`) pro stream/tool/slash eventy s korektním vyprázdněním fronty.
    3. Přidej nebo uprav regresní test/smoke check pro stream token flow bez zamrznutí UI.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>
    Streaming tok je neblokující a UI zůstává interaktivní během odpovědi.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TERM-03 assistant-only slash/GSD continuity</name>
  <files>src/app/ui/terminal/right/ai_bar.rs, src/app/ui/terminal/ai_chat/slash.rs, src/app/ui/workspace/state/mod.rs</files>
  <action>
    1. Odstraň z AI bar/runtime cesty model picker/Ollama coupling (`ws.ai.ollama.*`, `selected_model`) v souladu s assistant-only boundary.
    2. Ověř `/gsd` a externí slash dispatch placeholder->final update tok a generation guardy.
    3. Ujisti se, že interní PolyCredo slash příkazy (např. `/git`, `/build`) nejsou součástí AI terminal kontraktu.
    4. Uprav pouze body nutné pro parity fix; bez přidávání nových feature.
  </action>
  <verify>
    <automated>cargo test gsd -- --nocapture && ! rg -n "\"/(git|build)\"" src/app/ui/terminal/ai_chat/slash.rs && ! rg -n "ws\.ai\.ollama|selected_model|OllamaProvider" src/app/ui/terminal/right/ai_bar.rs src/app/ui/terminal/ai_chat/logic.rs src/app/ui/workspace/state/mod.rs && ! rg -n "app::cli" src/app/ui src/app/ai_core</automated>
  </verify>
  <done>
    Assistant-only slash/GSD workflow je funkční v terminal-only runtime bez model picker/Ollama couplingu.
  </done>
</task>

</tasks>

<verification>
cargo check
cargo test ai_chat -- --nocapture
cargo test gsd -- --nocapture
! rg -n "\"/(git|build)\"" src/app/ui/terminal/ai_chat/slash.rs
! rg -n "ws\.ai\.ollama|selected_model|OllamaProvider" src/app/ui/terminal/right/ai_bar.rs src/app/ui/terminal/ai_chat/logic.rs src/app/ui/workspace/state/mod.rs
! rg -n "app::cli" src/app/ui src/app/ai_core
./check.sh
</verification>

<verification_criteria>
- TERM-01: otevření panelu a odeslání promptu vytvoří běžící konverzační tok.
- TERM-02: stream běží bez UI freeze a bez blokujících volání v hlavní event loop cestě.
- TERM-03: assistant-only slash workflow (`/gsd` + externí slash dispatch) zůstává funkční bez interních PolyCredo slash příkazů.
</verification_criteria>

<success_criteria>
- Prompt/stream stavový automat funguje konzistentně v běžných i chybových cestách.
- Event loop je neblokující během streamingu.
- Assistant-only slash funkcionalita má parity vůči požadované boundary bez legacy CLI/Ollama couplingu.
</success_criteria>
