---
phase: 01-zaklad
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/settings.rs
autonomous: true
requirements: [THEME-01, THEME-02, THEME-04, SETT-04, UI-01, UI-02, UI-03]

must_haves:
  truths:
    - "Settings struct obsahuje `light_variant: LightVariant` pole s `#[serde(default)]` — staré TOML bez tohoto pole se načte bez paniku"
    - "`syntect_theme_name()` vrátí `\"base16-ocean.dark\"` pro dark mode a `\"Solarized (light)\"` pro light mode"
    - "`to_egui_visuals()` vrátí `Visuals` s `dark_mode: true` pro dark a `dark_mode: false` pro light"
    - "`Settings::apply()` volá `ctx.set_visuals(self.to_egui_visuals())` místo přímého if/else"
  artifacts:
    - path: "src/settings.rs"
      provides: "LightVariant enum, syntect_theme_name(), to_egui_visuals(), apply() update, unit testy"
      exports: ["LightVariant", "Settings::syntect_theme_name", "Settings::to_egui_visuals"]
      contains: "#[serde(default)]\\npub light_variant: LightVariant"
  key_links:
    - from: "Settings::apply()"
      to: "ctx.set_visuals()"
      via: "self.to_egui_visuals()"
      pattern: "set_visuals\\(self\\.to_egui_visuals"
    - from: "LightVariant"
      to: "serde"
      via: "#[serde(default)]"
      pattern: "serde\\(default\\)"
---

<objective>
Rozšíření Settings struct o LightVariant enum a theme-aware metody. Základ pro celý dark/light milestone — bez těchto změn nemohou fungovat ani Highlighter, ani startup apply, ani Phase 3 light varianty.

Purpose: Centralizovat theme logiku do Settings, eliminovat přímý if/else v apply(), připravit datový model pro Phase 3 light varianty.
Output: `src/settings.rs` s LightVariant enum, `syntect_theme_name()`, `to_egui_visuals()`, upravenou `apply()` a unit testy pokrývajícími THEME-01, THEME-02, THEME-04, SETT-04.
</objective>

<execution_context>
@/home/stkremen/.claude/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existující Settings struct — executor musí rozšířit, ne přepsat -->
<!-- src/settings.rs řádky 76–234 -->

```rust
// Existující struct (zkráceno — přidej light_variant za ostatní pole):
#[derive(serde::Serialize, serde::Deserialize, Clone)]
pub struct Settings {
    #[serde(default = "default_dark_theme")]
    pub dark_theme: bool,
    // ... ostatní pole ...
}

// Existující impl Default for Settings — přidej light_variant: LightVariant::default()
impl Default for Settings {
    fn default() -> Self {
        Self {
            // ... ostatní pole ...
        }
    }
}

// Existující apply() — nahradit if/else za to_egui_visuals():
pub fn apply(&self, ctx: &eframe::egui::Context) {
    if self.dark_theme {
        ctx.set_visuals(eframe::egui::Visuals::dark());
    } else {
        ctx.set_visuals(eframe::egui::Visuals::light());
    }
    ctx.style_mut(|style| { /* font — beze změny */ });
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — testy pro Settings rozšíření</name>
  <files>src/settings.rs</files>
  <behavior>
    - test_theme01_light_variant_serde_default: `toml::from_str::<Settings>("[editor_font_size]\neditor_font_size = 14.0")` neskončí pádem; `result.light_variant == LightVariant::WarmIvory`
    - test_theme01_light_variant_roundtrip: serializace a deserializace LightVariant::CoolGray round-trip přes TOML
    - test_theme02_syntect_theme_name_dark: `Settings { dark_theme: true, .. }.syntect_theme_name() == "base16-ocean.dark"`
    - test_theme02_syntect_theme_name_light: `Settings { dark_theme: false, .. }.syntect_theme_name() == "Solarized (light)"`
    - test_theme02_to_egui_visuals_dark: výsledek má `dark_mode == true`
    - test_theme02_to_egui_visuals_light: výsledek má `dark_mode == false`
    - test_sett04_backward_compat: TOML řetězec BEZ `light_variant` klíče se deserializuje na `WarmIvory` default bez paniku
  </behavior>
  <action>
    RED fáze: Přidej `#[cfg(test)] mod tests { ... }` blok na konec `src/settings.rs` se všemi 7 testy z `<behavior>`. Testy kompilovat nesmí — LightVariant ani metody ještě neexistují. Spusť `cargo test --lib settings::tests 2>&1 | tail -20` — MUSÍ selhat s compile error (ne s runtime error).

    GREEN fáze: Implementuj vše potřebné v `src/settings.rs`:

    1. Před `struct Settings` přidej enum:
    ```rust
    #[derive(serde::Serialize, serde::Deserialize, Clone, PartialEq, Debug, Default)]
    #[serde(rename_all = "snake_case")]
    pub enum LightVariant {
        #[default]
        WarmIvory,
        CoolGray,
        Sepia,
    }
    ```

    2. Do `struct Settings` přidej pole (za `dark_theme`):
    ```rust
    /// Light mode color variant (only relevant when dark_theme = false).
    /// Phase 3 will use this for per-variant panel colors.
    #[serde(default)]
    pub light_variant: LightVariant,
    ```

    3. Do `impl Default for Settings` přidej do Self { ... } bloku:
    ```rust
    light_variant: LightVariant::default(),
    ```

    4. Do `impl Settings` přidej dvě metody (vložit PŘED metodu `load()`):
    ```rust
    /// Returns the syntect theme name for the current mode.
    /// Dark → "base16-ocean.dark", Light → "Solarized (light)".
    pub fn syntect_theme_name(&self) -> &'static str {
        if self.dark_theme {
            "base16-ocean.dark"
        } else {
            "Solarized (light)"
        }
    }

    /// Returns egui Visuals for the current theme.
    /// Phase 1: basic Visuals::dark()/light(). Phase 3 will add per-variant colors.
    pub fn to_egui_visuals(&self) -> eframe::egui::Visuals {
        if self.dark_theme {
            eframe::egui::Visuals::dark()
        } else {
            eframe::egui::Visuals::light()
        }
    }
    ```

    5. Nahraď tělo `apply()` metody:
    ```rust
    pub fn apply(&self, ctx: &eframe::egui::Context) {
        ctx.set_visuals(self.to_egui_visuals());
        ctx.style_mut(|style| {
            style.text_styles.insert(
                eframe::egui::TextStyle::Monospace,
                eframe::egui::FontId::new(
                    self.editor_font_size,
                    eframe::egui::FontFamily::Monospace,
                ),
            );
        });
    }
    ```

    REFACTOR: Zkontroluj přehlednost, přidej komentáře "Phase 1 / Phase 3" kde relevantní.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor/.polycredo/sandbox && cargo test --lib settings 2>&1 | tail -20</automated>
  </verify>
  <done>
    `cargo test --lib settings` hlásí 7 passing testů. `cargo build 2>&1` bez chyb. `src/settings.rs` obsahuje LightVariant enum, `syntect_theme_name()`, `to_egui_visuals()`, upravenou `apply()`.
  </done>
</task>

</tasks>

<verification>
- `cargo test --lib settings 2>&1` — všechny testy zelené
- `cargo build 2>&1` — žádné compile chyby, žádné regrese
- `grep -n "light_variant" src/settings.rs` — pole přítomno v struct i v Default impl
- `grep -n "to_egui_visuals\|syntect_theme_name" src/settings.rs` — obě metody přítomny
- `grep -n "set_visuals(self.to_egui_visuals" src/settings.rs` — apply() používá novou metodu
</verification>

<success_criteria>
- LightVariant enum existuje v `src/settings.rs` s WarmIvory jako default
- `#[serde(default)]` na `light_variant` poli zajišťuje zpětnou kompatibilitu (SETT-04)
- `syntect_theme_name()` vrací správné hodnoty pro oba módy (THEME-02)
- `to_egui_visuals()` vrací správné Visuals pro oba módy (THEME-02)
- `apply()` deleguje na `to_egui_visuals()` — žádný přímý if/else pro Visuals (THEME-04, UI-01)
- 7 unit testů zelených (cargo test --lib settings)
- Projekt se kompiluje bez chyb
</success_criteria>

<output>
Po dokončení vytvoř `.planning/phases/01-zaklad/01-01-SUMMARY.md`
</output>
