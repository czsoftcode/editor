# Poznámky k vývoji (21. 2. 2026)

## Aktuální stav (v0.5.6)
- **AI Kontext (Bod z roadmapy hotov):** 
    - Agent v terminálu nyní při startu (nebo přes tlačítko **Sync**) dostává seznam otevřených souborů a chyby z buildu.
    - Implementováno v `src/app/ui/ai_panel.rs`.
- **Git Sync:** Všechny změny (Smart Typing, LSP vylepšení, AI kontext) byly pushnuty na GitHub.
- **Kompilace:** Projekt se kompiluje, ale generuje 2 varování o nevyužitém kódu (`dead_code`) v `src/app/ui/editor/mod.rs`.

## Co dál (Příští session)
1. **LSP Reference — Dokončení (HOTOVO):** Vyřešeny warningy. Picker referencí nyní zobrazuje náhled řádku a správně ovládá fokus po otevření (využita pole `text` a `focus_requested`).
2. **AI Diff náhled:** Začít s výzkumem, jak vizualizovat navržené změny před jejich zápisem na disk.
3. **Refaktoring:** Pokud `src/app/ui/editor/mod.rs` dál poroste (aktuálně >900 řádků), zvážit rozdělení podle mandátu v `GEMINI.md`.
