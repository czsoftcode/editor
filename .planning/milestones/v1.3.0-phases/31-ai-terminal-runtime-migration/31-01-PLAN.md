---
phase: 31-ai-terminal-runtime-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/ui/terminal/ai_chat/logic.rs
  - src/app/ui/terminal/ai_chat/mod.rs
  - src/app/ui/terminal/ai_chat/render.rs
  - src/app/ui/terminal/ai_chat/slash.rs
  - src/app/ui/workspace/state/mod.rs
  - src/app/ui/background.rs
  - src/app/ui/ai_panel.rs
  - src/app/ai_core/mod.rs
autonomous: true
requirements:
  - TERM-01
  - TERM-03
  - SAFE-03

must_haves:
  truths:
    - "AI terminal runtime je jediná podporovaná cesta; žádný návrat k PolyCredo CLI infrastruktuře."
    - "Prompt entrypoint a panel flow zůstává funkční při terminal-only boundary."
    - "Slash/GSD tok je explicitně omezen na externí/terminal workflow bez legacy PolyCredo slash semantiky."
  artifacts:
    - path: "src/app/ui/terminal/ai_chat/logic.rs"
      provides: "Prompt entrypoint bez legacy CLI branchí"
      contains: "send_query_to_agent"
    - path: "src/app/ui/terminal/ai_chat/slash.rs"
      provides: "Slash dispatch policy pro external assistant workflow"
      contains: "dispatch"
    - path: "src/app/ui/background.rs"
      provides: "Viditelný error handling v runtime event pumpě"
      contains: "error"
  key_links:
    - from: "Terminal-only boundary"
      to: "TERM-01"
      via: "Zachování panel+prompt cesty bez CLI fallbacku"
      pattern: "ai_chat"
    - from: "External slash policy"
      to: "TERM-03"
      via: "Slash/GSD funguje bez interní PolyCredo CLI vrstvy"
      pattern: "slash"
    - from: "Error visibility parity"
      to: "SAFE-03"
      via: "Chyby jsou viditelné v chatu/toastu, ne tiché"
      pattern: "toast|system"
---

<objective>
Uzamknout runtime boundary na AI terminal-only architekturu, odstranit zbylé legacy CLI semantiky v entrypointech
ai panelu/slash dispatchi a připravit bezpečný základ pro TERM/SAFE hardening bez behavior regressí.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-ai-terminal-runtime-migration/31-CONTEXT.md
@.planning/phases/31-ai-terminal-runtime-migration/31-RESEARCH.md
@.planning/phases/31-ai-terminal-runtime-migration/31-VALIDATION.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Runtime boundary audit a cleanup legacy CLI semantik</name>
  <files>src/app/ui/terminal/ai_chat/logic.rs, src/app/ui/ai_panel.rs, src/app/ai_core/mod.rs</files>
  <action>
    1. Proveď cílený audit runtime entrypointů (`ai_panel`, `ai_chat`) a odstraň branch/popis/chování navázané na původní PolyCredo CLI.
    2. Zachovej pouze AI terminal cestu bez fallback UI nebo fallback runtime větví.
    3. Uprav importy/re-exporty jen v rozsahu nutném pro čistý terminal-only tok.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>
    Entrypointy běží čistě přes AI terminal runtime a nejsou navázané na legacy CLI semantiku.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Slash dispatch policy alignment (externí slash + GSD)</name>
  <files>src/app/ui/terminal/ai_chat/slash.rs, src/app/ui/workspace/state/mod.rs</files>
  <action>
    1. Zreviduj dispatch pravidla a odstraň interní PolyCredo CLI slash body, pokud zůstaly po phase 30.
    2. Zachovej funkční externí slash workflow a `/gsd` path dle 31-CONTEXT.
    3. Doplň guardy proti stale async update po `/clear` nebo nové konverzaci.
  </action>
  <verify>
    <automated>cargo test slash -- --nocapture</automated>
  </verify>
  <done>
    Slash tok je konzistentní s external-assistant modelem a TERM-03 zůstává funkční.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SAFE-03 baseline pro viditelný runtime error/audit feedback</name>
  <files>src/app/ui/terminal/ai_chat/logic.rs, src/app/ui/terminal/ai_chat/mod.rs, src/app/ui/terminal/ai_chat/render.rs, src/app/ui/background.rs</files>
  <action>
    1. Zkontroluj, že chyby v runtime/slash/tool cestě končí ve viditelném feedbacku (toast/system message).
    2. Odstraň tiché swallow body a sjednoť route na existující UI feedback mechaniky.
    3. Doplň explicitní Retry cestu pro dočasné selhání asistenta dle locked decision v 31-CONTEXT (znovupoužití posledního validního promptu).
    4. Potvrď, že změny nemění approval policy ani security guard kontrakt.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>
    Error handling je uživatelsky viditelný, obsahuje Retry flow a je připravený pro SAFE flow testy v další vlně.
  </done>
</task>

</tasks>

<verification>
cargo check
rg -n "PolyCredo CLI|app::cli|legacy cli" src/app/ui/terminal/ai_chat src/app/ui/ai_panel.rs src/app/ui/background.rs
rg -n "gsd|slash" src/app/ui/terminal/ai_chat/slash.rs
rg -n "Retry|retry|zkusit znovu" src/app/ui/terminal/ai_chat src/app/ui/background.rs
</verification>

<verification_criteria>
- TERM-01: panel lze otevřít a vstup promptu jde přes čistý AI terminal entrypoint.
- TERM-03: slash/GSD dispatch běží bez interní PolyCredo CLI infrastruktury.
- SAFE-03: runtime chyby nejsou tiché, ale viditelné v UI feedbacku.
- Failure UX: dočasné selhání nabízí explicitní Retry akci dle 31-CONTEXT.
</verification_criteria>

<success_criteria>
- Žádný runtime entrypoint nemá behavior závislý na PolyCredo CLI vrstvě.
- Slash policy odpovídá 31-CONTEXT (externí slash + GSD, bez legacy interních slash).
- `cargo check` je zelený po boundary cleanup passu.
</success_criteria>
