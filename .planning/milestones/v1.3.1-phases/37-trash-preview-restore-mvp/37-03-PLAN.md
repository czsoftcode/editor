---
phase: 37-trash-preview-restore-mvp
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - src/app/trash.rs
  - src/app/ui/panels.rs
  - src/app/ui/file_tree/mod.rs
  - src/app/ui/editor/tabs.rs
  - src/app/ui/background.rs
  - tests/phase37_restore_ui_sync.rs
autonomous: true
requirements:
  - RESTORE-02
  - RESTORE-03
  - TRASHUI-01
  - RESTORE-01

must_haves:
  truths:
    - "Pri konfliktu cilove cesty restore nikdy neprepisuje existujici data a pouziva pouze copy policy."
    - "Po uspesnem restore se UI konzistentne obnovi: file tree reload + expand/highlight obnovene cesty."
    - "Restore neotevira automaticky novy tab; tab sync pouze dorovnava realny stav."
    - "Success/error feedback jde konzistentne pres toast pipeline a pending handoff bez UI freeze."
  artifacts:
    - path: "src/app/trash.rs"
      provides: "Deterministicka copy-suffix policy a conflict-safe restore finalizace"
      contains: "resolve_restore_copy_destination|RestoreConflictPolicy::RestoreAsCopy"
    - path: "src/app/ui/panels.rs"
      provides: "Post-restore UI orchestrace (reload, expand/highlight, success toast)"
      contains: "pending_restored|request_reload_and_expand"
    - path: "src/app/ui/editor/tabs.rs"
      provides: "Tab state synchronization bez auto-open restore souboru"
      contains: "restore tab sync"
    - path: "tests/phase37_restore_ui_sync.rs"
      provides: "Requirement-level dukaz pro conflict policy a UI konzistenci"
      contains: "phase37_restore_conflict_as_copy|phase37_restore_triggers_reload_highlight|phase37_restore_no_auto_open_tab"
  key_links:
    - from: "Conflict restore as copy"
      to: "RESTORE-02"
      via: "Engine i UI trasa vynucuje nedestruktivni copy variantu bez overwrite"
      pattern: "phase37_restore_conflict_as_copy"
    - from: "Post-restore UI refresh contract"
      to: "RESTORE-03"
      via: "Restore vysledek spousti file tree reload + highlight + tab sync bez restartu"
      pattern: "phase37_restore_triggers_reload_highlight|phase37_restore_no_auto_open_tab"
    - from: "Preview to restore completion loop"
      to: "TRASHUI-01, RESTORE-01"
      via: "Preview flow umozni vyber a dokonci restore az do user-visible success stavu"
      pattern: "phase37_preview_restore_roundtrip"
---

<objective>
Finalizovat restore conflict policy a UI konzistenci po restore, aby preview->restore tok
dobihal deterministicky bez overwrite a bez nekonzistence ve file tree/tab stavech.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-trash-preview-restore-mvp/37-CONTEXT.md
@.planning/phases/37-trash-preview-restore-mvp/37-RESEARCH.md
@.planning/phases/37-trash-preview-restore-mvp/37-01-PLAN.md
@.planning/phases/37-trash-preview-restore-mvp/37-02-PLAN.md
@./AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dokoncit conflict-safe restore as copy v engine</name>
  <files>src/app/trash.rs, tests/phase37_restore_ui_sync.rs</files>
  <action>
    1. Dodefinuj deterministic suffix resolver pro "obnovit jako kopii" (bounded pokusy, jasny error pri vycerpani).
    2. Potvrd, ze konfliktni restore nikdy nespousti overwrite vetvu.
    3. Otestuj konfliktni restore scenar se zachovanim puvodniho souboru.
  </action>
  <verify>
    <automated>cargo test phase37_restore_conflict_as_copy -- --nocapture && ! rg -n "overwrite_existing|force_overwrite" src/app/trash.rs -S</automated>
  </verify>
  <done>
    RESTORE-02 je finalne uzavren na engine vrstve nedestruktivni policy.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UI konzistence po restore (reload + highlight + tabs sync)</name>
  <files>src/app/ui/panels.rs, src/app/ui/file_tree/mod.rs, src/app/ui/editor/tabs.rs, src/app/ui/background.rs, tests/phase37_restore_ui_sync.rs</files>
  <action>
    1. Po uspesnem restore nastav pending signal tak, aby probehl immediate reload file tree a expand/highlight obnovene cesty.
    2. Udrz tab synchronizaci konzistentni s realnym stavem bez auto-open noveho tabu.
    3. Osetri async channel disconnect/error path do pending_error -> toast.
    4. Doplnenim testu over roundtrip preview->restore->UI refresh.
  </action>
  <verify>
    <automated>cargo test phase37_restore_triggers_reload_highlight -- --nocapture && cargo test phase37_restore_no_auto_open_tab -- --nocapture && cargo test phase37_preview_restore_roundtrip -- --nocapture</automated>
  </verify>
  <done>
    RESTORE-03 je splneny konzistentnim a uzivatelsky citelnym post-restore stavem UI.
  </done>
</task>

</tasks>

<verification>
cargo test phase37 -- --nocapture
cargo check
./check.sh
</verification>

<verification_criteria>
- RESTORE-02: conflict restore je vyhradne "restore as copy" nebo "cancel", bez overwrite.
- RESTORE-03: po restore je file tree i tab state konzistentni bez restartu aplikace.
- TRASHUI-01 + RESTORE-01: preview->restore loop je funkcni az do user-visible success/error toastu.
- Full quality gate (`cargo check` + `./check.sh`) zustava soucasti exit criteria planu.
</verification_criteria>

<success_criteria>
- Wave 3 uzavira behavior a UI sync cast restore flow bez scope driftu do lokalizaci/reportingu.
- Plan je pripraven pro samostatnou exekuci v `$gsd-execute-phase` bez dalsiho planovani.
</success_criteria>

<output>
Po dokonceni vytvor `.planning/phases/37-trash-preview-restore-mvp/37-03-SUMMARY.md`.
</output>
