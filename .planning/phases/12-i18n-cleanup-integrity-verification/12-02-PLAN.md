---
phase: 12-i18n-cleanup-integrity-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/ui/workspace/modal_dialogs.rs
  - src/app/ui/workspace/modal_dialogs/settings.rs
  - src/app/ui/background.rs
  - src/app/ui/workspace/mod.rs
autonomous: true
requirements:
  - INT-01
  - INT-02
  - INT-03

must_haves:
  truths:
    - "cargo build projde bez warningů"
    - "cargo test projde (57+ testů)"
    - "grep -r sandbox src/ vrátí 0 výsledků"
  artifacts:
    - path: "src/app/ui/workspace/modal_dialogs.rs"
      provides: "Cleaned modal_dialogs without unused re-export"
      contains: "NOT restore_runtime_settings_from_snapshot"
    - path: "src/app/ui/workspace/modal_dialogs/settings.rs"
      provides: "Settings show() without unused id_salt parameter"
      contains: "NOT id_salt: &std::ffi::OsStr"
    - path: "src/app/ui/background.rs"
      provides: "Background events without unused egui_ctx parameter"
      contains: "NOT egui_ctx: &egui::Context"
  key_links:
    - from: "src/app/ui/workspace/modal_dialogs.rs"
      to: "settings::show()"
      via: "function call"
      pattern: "settings::show\\(ctx, ws, shared, i18n\\)"
    - from: "src/app/ui/workspace/mod.rs"
      to: "process_background_events()"
      via: "function call"
      pattern: "process_background_events\\(ws, shared, i18n\\)"
---

<objective>
Opravit 3 compile warningy, odstranit sandbox komentáře z src/ a provést finální verifikaci integrity celého v1.1.0 milestonu.

Purpose: Editor se zkompiluje bez warningů, všechny testy projdou a v src/ nezbývá žádná zmínka o sandboxu.
Output: Čistý build, 0 warningů, 0 sandbox referencí v kódu.
</objective>

<execution_context>
@/home/stkremen/.claude/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-i18n-cleanup-integrity-verification/12-CONTEXT.md
</context>

<interfaces>
<!-- Aktuální signatury, které se budou měnit -->

From src/app/ui/workspace/modal_dialogs.rs:
```rust
pub(crate) use settings::restore_runtime_settings_from_snapshot; // LINE 15 — SMAZAT (unused re-export)
settings::show(ctx, ws, shared, i18n, &id_salt); // LINE 41 — odstranit &id_salt argument
```

From src/app/ui/workspace/modal_dialogs/settings.rs:
```rust
pub fn show(
    ctx: &egui::Context,
    ws: &mut WorkspaceState,
    shared: &Arc<Mutex<AppShared>>,
    i18n: &I18n,
    id_salt: &std::ffi::OsStr, // LINE 130 — SMAZAT tento parametr
) {
```

From src/app/ui/background.rs:
```rust
pub(super) fn process_background_events(
    ws: &mut WorkspaceState,
    shared: &Arc<Mutex<AppShared>>,
    i18n: &crate::i18n::I18n,
    egui_ctx: &egui::Context, // LINE 33 — SMAZAT tento parametr
) {
```

From src/app/ui/workspace/mod.rs:
```rust
process_background_events(ws, shared, i18n, ctx); // LINE 69 — odstranit ctx argument
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Opravit compile warningy a odstranit sandbox komentáře</name>
  <files>src/app/ui/workspace/modal_dialogs.rs, src/app/ui/workspace/modal_dialogs/settings.rs, src/app/ui/background.rs, src/app/ui/workspace/mod.rs</files>
  <action>
**Warning 1 — unused import restore_runtime_settings_from_snapshot:**
V `src/app/ui/workspace/modal_dialogs.rs` řádek 15: smazat řádek `pub(crate) use settings::restore_runtime_settings_from_snapshot;`. Funkce je používána pouze interně v settings.rs (volá ji discard_settings_draft), takže re-export není potřeba.

**Warning 2 — unused id_salt v settings::show:**
V `src/app/ui/workspace/modal_dialogs/settings.rs`: odstranit parametr `id_salt: &std::ffi::OsStr` z funkce `show()` (řádek 130). Funkce interně používá hardcoded salts ("settings_cat_scroll", "settings_content_scroll", "settings_lang_combo") a parametr id_salt nikdy nečte.
V `src/app/ui/workspace/modal_dialogs.rs` řádek 41: aktualizovat volání na `settings::show(ctx, ws, shared, i18n);` (bez &id_salt).

**Warning 3 — unused egui_ctx v process_background_events:**
V `src/app/ui/background.rs`: odstranit parametr `egui_ctx: &egui::Context` z funkce `process_background_events()` (řádek 33). Parametr není nikde v těle funkce používán.
V `src/app/ui/workspace/mod.rs` řádek 69: aktualizovat volání na `process_background_events(ws, shared, i18n);` (bez ctx).

**Sandbox komentáře:**
Projít všechny soubory v src/ a smazat komentáře obsahující slovo "sandbox" (git historie je dostatečná dokumentace). Spustit `grep -rn "sandbox" src/` a pro každý výskyt:
- Pokud je to komentář (// ...) — smazat celý řádek
- Pokud je to kód — nahlásit (neměl by existovat, ale ověřit)
- Pokud je to string literal v kódu (ne i18n klíč) — posoudit a aktualizovat/smazat

Po každé změně spustit `cargo check` a ověřit, že se neobjevily nové warningy.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor && cargo check 2>&1 | grep -c "warning"</automated>
  </verify>
  <done>cargo check vrátí 0 warningů. grep -r "sandbox" src/ vrátí 0 výsledků (žádné sandbox komentáře ani kód).</done>
</task>

<task type="auto">
  <name>Task 2: Finální verifikace integrity v1.1.0 milestonu</name>
  <files></files>
  <action>
Spustit kompletní verifikaci:

1. `cargo build 2>&1` — musí projít bez warningů
2. `cargo test 2>&1` — všech 57+ testů musí projít
3. `grep -r "sandbox" src/` — musí vrátit 0 výsledků
4. `grep -r "sandbox" locales/` — musí vrátit 0 výsledků (ověření po plánu 12-01)

Pokud jakýkoli krok selže, opravit problém a znovu ověřit.

Tato úloha je čistě verifikační — nemění kód, pokud vše prochází. Pokud něco selže, provede minimální opravu.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor && cargo build 2>&1 | grep -c "warning" && cargo test 2>&1 | tail -1 && grep -rc "sandbox" src/ locales/ | grep -v ":0$" | wc -l</automated>
  </verify>
  <done>cargo build: 0 warningů. cargo test: 57+ testů prochází, 0 selhání. grep sandbox v src/ i locales/: 0 výsledků. Editor je připraven k release v1.1.0.</done>
</task>

</tasks>

<verification>
- `cargo build` projde bez warningů
- `cargo test` — všech 57+ testů projde
- `grep -r "sandbox" src/` = 0 výsledků
- `grep -r "sandbox" locales/` = 0 výsledků
</verification>

<success_criteria>
Projekt se kompiluje bez warningů, všechny testy procházejí a v celém codebase (src/ + locales/) nezbývá žádná zmínka o sandboxu. Milestone v1.1.0 Sandbox Removal je kompletní.
</success_criteria>

<output>
After completion, create `.planning/phases/12-i18n-cleanup-integrity-verification/12-02-SUMMARY.md`
</output>
