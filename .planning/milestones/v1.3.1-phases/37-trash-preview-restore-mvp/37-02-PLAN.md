---
phase: 37-trash-preview-restore-mvp
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/app/ui/workspace/menubar/mod.rs
  - src/app/ui/widgets/command_palette.rs
  - src/app/registry/mod.rs
  - src/app/ui/file_tree/mod.rs
  - src/app/ui/file_tree/dialogs.rs
  - tests/phase37_trash_preview_ui.rs
autonomous: true
requirements:
  - TRASHUI-01
  - RESTORE-01
  - RESTORE-02

must_haves:
  truths:
    - "Trash preview je dostupny pres menu/command a otevira modal bez blokujiciho I/O na UI vlakne."
    - "Modal zobrazuje seznam trash polozek s filtrem a umozni restore jedne vybrane polozky."
    - "Restore se pousti asynchronne pres background job pipeline a chyby jdou do toast flow."
    - "Konflikt cilove cesty nikdy nejde tichym overwrite smerem."
  artifacts:
    - path: "src/app/ui/workspace/menubar/mod.rs"
      provides: "UI entrypoint do trash preview workflow"
      contains: "MenuActions|Trash Preview"
    - path: "src/app/ui/widgets/command_palette.rs"
      provides: "Command palette trigger pro trash preview"
      contains: "CommandId::TrashPreview"
    - path: "src/app/ui/file_tree/dialogs.rs"
      provides: "Trash preview modal + restore trigger + conflict modal orchestrace"
      contains: "show_trash_preview_dialog|show_restore_conflict_dialog"
    - path: "src/app/ui/file_tree/mod.rs"
      provides: "Async restore polling a pending signal bridge"
      contains: "restore_rx|pending_restored|pending_error"
    - path: "tests/phase37_trash_preview_ui.rs"
      provides: "UI orchestration test hooks pro preview/restore trigger"
      contains: "phase37_open_trash_preview_from_menu|phase37_restore_job_result_to_pending"
  key_links:
    - from: "Preview modal entry"
      to: "TRASHUI-01"
      via: "Menu/command action otevre modal se seznamem trashe a restore akcemi"
      pattern: "phase37_open_trash_preview_from_menu"
    - from: "Restore trigger orchestration"
      to: "RESTORE-01"
      via: "Vyber polozky v preview spusti async restore job a prijme vysledek"
      pattern: "phase37_restore_job_result_to_pending"
    - from: "Conflict path routing"
      to: "RESTORE-02"
      via: "Pri existujici cilove ceste je cesta vedena na conflict modal bez overwrite"
      pattern: "phase37_conflict_routes_to_modal"
---

<objective>
Dodat uzivatelske MVP rozhrani pro trash preview + restore trigger, vcetne async orchestrace a jasneho
konfliktniho rozhodnuti bez silent overwrite.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-trash-preview-restore-mvp/37-CONTEXT.md
@.planning/phases/37-trash-preview-restore-mvp/37-RESEARCH.md
@.planning/phases/37-trash-preview-restore-mvp/37-01-PLAN.md
@./AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Entrypoint menu + command palette pro Trash Preview</name>
  <files>src/app/ui/workspace/menubar/mod.rs, src/app/ui/widgets/command_palette.rs, src/app/registry/mod.rs, tests/phase37_trash_preview_ui.rs</files>
  <action>
    1. Pridej akci "Trash Preview" do menubar workflow a command palette registrace.
    2. Napoj trigger tak, aby nastavil otevreni trash preview modalu.
    3. Udrz naming konzistentni s existujicimi command IDs a i18n patterny.
    4. Doplnenim testu over, ze menu/command route aktivuje preview stav.
  </action>
  <verify>
    <automated>cargo test phase37_open_trash_preview_from_menu -- --nocapture && cargo test phase37_open_trash_preview_from_command -- --nocapture</automated>
  </verify>
  <done>
    Uzivatel ma dva stabilni vstupy do preview flow (menu + command).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Trash preview modal + filter + restore trigger</name>
  <files>src/app/ui/file_tree/dialogs.rs, src/app/ui/file_tree/mod.rs, tests/phase37_trash_preview_ui.rs</files>
  <action>
    1. Implementuj modal se seznamem trash polozek (nazev, typ, cas smazani, puvodni cesta) a textovym filtrem.
    2. Umoznuj vyber jedne polozky a akci restore z modalu.
    3. Restore akci ved pres async background job (`spawn_task` + `mpsc`), ne pres blokujici I/O v UI.
    4. Dopln test pro polling restore vysledku do `pending_restored`/`pending_error`.
  </action>
  <verify>
    <automated>cargo test phase37_preview_filtering -- --nocapture && cargo test phase37_restore_job_result_to_pending -- --nocapture</automated>
  </verify>
  <done>
    TRASHUI-01 je funkcni v MVP rozsahu a restore se spousti neblokujicim zpusobem.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Conflict modal routing bez silent overwrite</name>
  <files>src/app/ui/file_tree/dialogs.rs, src/app/ui/file_tree/mod.rs, tests/phase37_trash_preview_ui.rs</files>
  <action>
    1. Pri detekci existujici cilove cesty otevri conflict modal.
    2. V modalu ponech pouze dve volby: "Obnovit jako kopii" a "Zrusit".
    3. Zajisti, ze path "overwrite existing" vubec nevznikne.
    4. Doplnenim focused testu over conflict route + no-overwrite guard.
  </action>
  <verify>
    <automated>cargo test phase37_conflict_routes_to_modal -- --nocapture && cargo test phase37_conflict_has_no_overwrite_action -- --nocapture</automated>
  </verify>
  <done>
    RESTORE-02 je v UI vrstve vynucen explicitni nedestruktivni policy.
  </done>
</task>

</tasks>

<verification>
cargo test phase37_trash_preview_ui -- --nocapture
cargo check
rg -n "TrashPreview|show_trash_preview_dialog|show_restore_conflict_dialog|pending_restored|restore_rx" src/app/ui -S
</verification>

<verification_criteria>
- TRASHUI-01: modal preview je otevritelny pres menu/command a umoznuje restore trigger.
- RESTORE-01: restore trigger jde pres async job pipeline s jasnym result handoffem.
- RESTORE-02: konfliktni stav nenabizi overwrite a vyzaduje nedestruktivni volbu.
</verification_criteria>

<success_criteria>
- Wave 2 doda spustitelny UX tok preview -> restore trigger -> pending result bez UI freeze.
- Konfliktni behavior je explicitni a bez rizika ticheho prepisu.
</success_criteria>

<output>
Po dokonceni vytvor `.planning/phases/37-trash-preview-restore-mvp/37-02-SUMMARY.md`.
</output>

