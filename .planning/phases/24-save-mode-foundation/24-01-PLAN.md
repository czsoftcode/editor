---
phase: 24-save-mode-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/settings.rs
autonomous: true
requirements:
  - MODE-01
  - MODE-02

must_haves:
  truths:
    - "Po čistém startu aplikace je výchozí režim ukládání `Manual`."
    - "Po změně režimu a restartu aplikace uživatel vidí stejný režim, který naposledy uložil."
    - "U starších instalací bez pole `save_mode` aplikace naběhne bez chyby a používá `Manual`."
  artifacts:
    - path: "src/settings.rs"
      provides: "SaveMode enum, pole save_mode v Settings, serde/default kompatibilita"
      contains: "pub enum SaveMode"
  key_links:
    - from: "src/settings.rs"
      to: "~/.config/polycredo-editor/settings.toml"
      via: "Settings::try_save serializuje save_mode do TOML"
      pattern: "try_save_to_config_dir"
---

<objective>
Zavést datový základ save režimu (`Automatic` / `Manual`) do persistentních settings tak,
aby byl zpětně kompatibilní se staršími konfiguracemi a okamžitě použitelný pro runtime gating v dalších plánech.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-save-mode-foundation/24-CONTEXT.md
@.planning/phases/24-save-mode-foundation/24-RESEARCH.md
@.planning/phases/24-save-mode-foundation/24-VALIDATION.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SaveMode enum + Settings pole + default/manual fallback</name>
  <files>src/settings.rs</files>
  <action>
    1. Přidej `SaveMode` enum se serde reprezentací (`snake_case`) a default variantou `Manual`.
    2. Rozšiř `Settings` o pole `save_mode` s `#[serde(default)]` tak, aby starší konfigurace bez pole byly validní.
    3. Doplň `Default for Settings`, aby nové instance vždy startovaly v `Manual` režimu.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>
    Datový model save režimu je zavedený a kompiluje.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Jednotkové testy serde/default/backward compatibility</name>
  <files>src/settings.rs</files>
  <action>
    1. Přidej test na default `save_mode == Manual`.
    2. Přidej round-trip test `Settings -> TOML -> Settings` pro obě varianty (`automatic`, `manual`).
    3. Přidej backward-compat test: TOML bez `save_mode` musí deserialize na `Manual`.
  </action>
  <verify>
    <automated>cargo test settings:: -- --nocapture</automated>
  </verify>
  <done>
    SaveMode persist vrstva je krytá testy a kompatibilní se starými settings.
  </done>
</task>

</tasks>

<verification>
cargo check
cargo test settings:: -- --nocapture
</verification>

<success_criteria>
- `Settings` obsahuje `save_mode` a default je `Manual`
- Starší settings bez `save_mode` se načtou bez chyby
- Persist save_mode přes TOML round-trip funguje
</success_criteria>
