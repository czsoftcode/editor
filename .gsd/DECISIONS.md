- "LightVariant enum with 3 variants (WarmIvory, CoolGray, Sepia) for Phase 3"
- "syntect_theme_name returns 'base16-ocean.dark' for dark, 'Solarized (light)' for light"
- "apply() delegates to to_egui_visuals() enabling future per-variant colors in Phase 3"
- "Task 1 a Task 2 byly už implementované v aktuálním HEAD; pro atomický workflow byly vytvořeny verifikační task commity."
- "Integrace highlighter.set_theme() byla provedena přímo v místech settings.apply() pro root i deferred viewport."
- "Použit `ctx.style().visuals.panel_fill` jako jednotný theme-aware fill pro floating terminálový frame."
- "Fix držet centralizovaný v `StandardTerminalWindow`, aby se změna automaticky propsala do AI i build floating terminálu."
- "Primary and secondary status text now follows ui.visuals() instead of fixed RGB values."
- "Diagnostics and save/LSP accents keep semantic colors but branch by dark_mode for readability."
- "Theme se aplikuje při každém renderu přes set_theme(terminal_theme_for_visuals(ui.visuals())) bez restartu backendu."
- "Scrollbar track/thumb jsou odvozené z ui.visuals() mixem, ne z hardcoded dark RGB hodnot."
- "Unfocused overlay/cursor barvy jsou napojené na aktivní visuals, aby light mode neztrácel čitelnost."
- "Git porcelain status se mapuje na GitVisualStatus a barva se řeší až v render vrstvě."
- "Light mode používá explicitní paletu pro M/A/D/?? místo globálního násobení barev."
- "Light varianty jsou mapovane explicitnim match self.light_variant primo v Settings::to_egui_visuals()."
- "Dark branch zustava Visuals::dark() beze zmeny kvuli kompatibilite s existujicim dark renderingem."
- "Light variant picker is rendered as clickable cards with swatch + label + clear selected state, and is hidden in dark mode."
- "Live preview runs only on theme control changed() events and only when theme fingerprint actually changes."
- "WorkspaceState now carries settings_original snapshot metadata for next plan's cancel-revert semantics."
- "Save persistuje na disk pouze při reálné změně theme fingerprintu (dark_theme, light_variant) — beze změny se settings.toml nepřepisuje."
- "Cancel/discard restoruje runtime settings ze snapshot a bumpne settings_version jen pokud se theme fingerprint liší."
- "Global confirm-discard flow volá discard_settings_draft(), která zároveň revertu snapshot i čistí draft — žádný leak preview stavu."
- "Canonical storage je settings.toml; settings.json je pouze legacy migrační vstup, který se po migraci smaže."
- "Testy persistence jsou izolovány přes TempConfigDir s unikátním tmp adresářem — nezasahují reálný user config."
- "Terminal variant-aware tón: blending panel_fill do celé ColorPalette (0.06–0.42 dle složky) bez restartu PTY backendu"
- "Git light variant tón: mix_color s panel_fill (0.16–0.22) + faint_bg_color (0.06) pro dvojitý tonální posun"
- "Dark mode beze změn: obě větve explicitně vracejí legacy barvy bez doteku dark path"
- "Kontrast zachován: žlutá a cyan >=2.2, fg/bg >=4.5 v light mode po tonálním posunu"
- "Checkmark v variant kartě je inline label s add_space(8.0) — ne vnořený layout; with_layout(right_to_left) uvnitř horizontal konzumuje veškerou šířku"
- "warm_ivory_bg() detekuje WarmIvory pomocí r-b > 10 a vrátí #f5f2e8 jako teplejší základ; CoolGray/Sepia dostávají #f3f5f7"
- "Background blend ratio zvýšeno z 0.42 na 0.55 pro silnější variantní tón; luminance WarmIvory bg ~0.94 stále splňuje test > 0.7"
- "Sandbox režim je stabilní flag ve WorkspaceState; změna v Settings se projeví až po reopen."
- "Session-only toast při prvním vypnutí sandboxu (nepersistuje se)."
- "Hover target tooltipu je navazany na cely radek sandbox prepinace."
- "Poznamka o restartu terminalu po reopen se renderuje bez small() potlaceni."
- "Sandbox apply se planuje pres pending_sandbox_apply a spousti se az po persistu settings."
- "Persist failure resi toast s volbou revert/keep a runtime apply bezi jen po explicitnim keep."
- "Label režimu terminálu odvozovat z reálného working dir, aby se změna ukázala až po restartu."
- "Při přepnutí sandbox režimu nabídnout remap otevřených tabů a chybějící soubory ponechat otevřené s varováním."
- "Blokace OFF se provádí bezprostředně po záznamu sandbox_mode_change v settings.rs — draft se vrátí na original a show_sandbox_staged = true"
- "Staged bar zůstává viditelná i po přepnutí do OFF, dokud sandbox_staged_files není prázdný"
- "Sync dialog při ON se zobrazuje přes sandbox_sync_confirmation: Option<SyncPlan> v process_pending_sandbox_apply"
- "Sync operace běží v background threadu přes spawn_task, výsledek přes sandbox_sync_rx"
- "pending_tab_remap se vymaže ihned po retain(), pokud žádný SandboxRemapTabs toast neexistuje — zombie request není možný"
- "Label terminálu se mění okamžitě při vytvoření nové instance (ne až po exitu starého PTY) — záměrné chování potvrzeno verifikací 05"
- "Static slice registry instead of HashMap for 7 commands"
- "Levenshtein threshold <= 2 for fuzzy suggestions, word length <= 10 to avoid false positives"
- "System messages use \\x00SYS\\x00 marker prefix stripped at render time"
- "Reuse existing run_build_check for /build instead of new implementation"
- "Generation counter (slash_conversation_gen) to detect stale async results after /clear"
- "Reverse iteration over conversation to find placeholder entries safely"
- "Conservative code-fence check: skip ALL path regex for blocks containing any code fence"
- "Enter in autocomplete selects and sends command immediately"
- "Tab completes command but does not send, keeps focus on input"
- "Click on popup item selects and executes command"
- "Popup renders below input, not above"
- "Dismissed flag prevents re-activation after Escape until text changes"
- "Custom YAML-like parser with no new dependencies — supports full subset needed by .planning/ files"
- "Raw source lines stored per FmNode for lossless round-trip on unmodified nodes"
- "Reject keys containing colons to handle malformed YAML-like lines tolerantly"
- "GSD dispatch uses match-based routing mirroring slash.rs pattern"
- "Two-level autocomplete: /gsd prefix triggers subcommand autocomplete in both input.rs and render.rs"
- "Config value parsing: try bool -> int -> float -> string fallback"
- "Combined TDD approach: tests and implementation in single file following project convention"
- "ISO timestamp generation without external crate using Howard Hinnant date algorithm"
- "Body section manipulation via string search (not AST) per research anti-pattern guidance"
- "M002 decomposed into 3 slices: S01 (pipeline+context menu), S02 (split view+diff), S03 (cleanup+edge cases) — S01 first because S02 needs real data, S02 second as highest risk, S03 last as polish"
- "Save hook signál vrací z save() pro odeslání z workspace kódu, ne přímý sender v Editor structu — nemíchat I/O concerns do editoru"
- "History split view nahradí normální rendering kompletně (ne overlay) — jednodušší než koexistence s markdown split"
- "Nezávislý scroll dvou history panelů — sync scroll by vyžadoval diff-aware mapování řádků, nezávislý je praktičtější"
- "Diff výsledek cachovat per history entry, ne počítat per-frame — similar::TextDiff je O(n*d)"
- "No REQUIREMENTS.md — M002 planned in legacy compatibility mode with requirements from PROJECT.md Active section"
- "S01 rozdělen na 2 tasky: T01 (backend pipeline + testy) a T02 (UI + i18n) — pipeline musí být funkční dřív než UI"
- "Tab context menu se renderuje přes egui .context_menu() na selectable_label response — nativní egui pattern"
- "History panel v S01 je jednoduchý list+preview (ne split view) — split view s diff je scope S02"
- "background_io_tx se drží ve WorkspaceState, ne v Editor structu — konzistentní s rozhodnutím o I/O concerns"
- "take_snapshot() mění signature z Option na Result — breaking change pro volající, ale nutné pro S-3 (I/O error propagace)"
- "send_snapshot_signal() helper jen pro manual save; autosave a unsaved-close-guard inline kvůli borrow checker konfliktu na pending_close_flow"
- "Timestamp formátování v history panelu bez chrono dependency — vlastní days_to_date() (Howard Hinnant) v UTC, chrono by přidalo zbytečnou dependency pro jednu funkci"
- "History panel renderován jako overlay v CentralPanel po editor renderingu — S02 nahradí split view renderem"
- "S02 plánován jako jeden task — scope je koherentní (stav, rendering, navigace, barvy, i18n) a 9 kroků / 7 souborů je v limitu pro jeden context window"
- "Split view layout: toolbar nahoře (šipky + info + ✕), pod ním dva panely (aktuální vlevo, historická vpravo) — konvenční diff UI"
- "Diff rendering: levý panel zobrazuje Equal+Insert řádky, pravý Equal+Delete — side-by-side diff bez duplicity společných řádků"
- "Light mode diff barvy: bg_added(200,240,200), bg_removed(255,210,210), fg_added(0,100,0), fg_removed(150,0,0) — kontrastnější než dark varianta"
- "preview_content a scroll_to_selected odstraněny z HistoryViewState — nahrazeny diff logikou a current_content"
- "Editor.ui() podmíněné na history_view.is_none() — editor se kompletně nekreslí v history mode, ne jen overlay přes"
- "Diff cache invalidace přes diff_for_index != selected_index — jednoduchý a efektivní pattern bez generace/verze countera"
- "on_hover_text() na navigačních šipkách voláno vždy (ne jen při hovered) — egui ho zobrazí jen při hoveru, zjednodušuje kód"
- "Výchozí větev je develop, ne main. Slice branche se vytvářejí z develop a mergují zpět do develop. Main se používá jen pro release tagy."
- "S03 jako jeden task — scope je malý (cleanup rozšíření, 2× edge case fix, i18n audit), žádná nová UI komponenta, žádný nový i18n klíč"
- "cleanup_history_dir() jako standalone funkce bez &self — LocalHistory není Send, pro thread::spawn nutné extrahovat logiku do volné funkce s PathBuf parametrem"
- "Cleanup I/O chyby ponechány jako let _ — background operace při startu, ne uživatelská akce, toastování by bylo rušivé"
- "M003 decomposed into 2 slices: S01 (editovatelný panel + syntax highlighting + sync scroll + diff) a S02 (obnovení + potvrzení + i18n) — S01 high risk kvůli TextEdit+diff+highlight kombinaci"
- "M003 supersedes M002 rozhodnutí o nezávislém scrollu — sync scroll nyní požadován uživatelem"
- "Levý panel v history view přechází z read-only LayoutJob na editovatelný TextEdit s layouterem — pattern z render/normal.rs"
- "Obnovení historické verze jako append (nový snapshot na konec fronty), ne replace — zachování kompletní historie"
- "Potvrzovací dialog před obnovením historické verze — prevence nechtěného přepsání"
- "Editace v levém panelu se průběžně propsávají do tab.content — ne až při zavření history view"
- "Výchozí stav: 1 verze → selected_index=None (prázdný pravý panel), >1 verze → selected_index=Some(0) (nejnovější historická)"
- "S01 rozdělen na 2 tasky: T01 (datový model + helpers + testy) a T02 (rendering + scroll sync + tab sync + volající) — T01 dodá testovatelné stavební bloky, T02 je složí do funkčního UI"
- "apply_diff_backgrounds() pracuje s LayoutJob sections přes binary search v line_offsets — mapování byte_range.start → řádek bez průchodu textu per-section"
- "Tab sync probíhá ve workspace/mod.rs po render_history_split_view(), ne uvnitř render funkce — render funkce nemá přístup k tab strukturám"
- "Scroll sync přes proportionální mapování s epsilon tolerancí (1.0px) — line-based mapování odloženo jako potenciální budoucí vylepšení"
- "Diff cache invalidace přes content_hash (xxh3_64) — ne per-frame, ale jen při reálné změně obsahu"
- "Borrow checker v workspace/mod.rs vyřešen extrakcí ext/fname/theme_name/font_size do lokálních proměnných; highlighter ref je disjoint borrow na jiném fieldu WorkspaceState — bez unsafe"
- "TextEdit+layouter+apply_diff_backgrounds pattern: syntax highlighting z Highlighter::highlight() klonovaný a modifikovaný diff overlay v layouter closure — funguje jako editovatelný widget s per-řádek background barvami"
- "Diff mapy (Vec<ChangeTag>) klonované před closure capture — clone je levný (Vec<u8-sized enum>), potřebný kvůli simultánnímu &mut history_view v TextEdit a immutable capture v layouteru"
- "ScrollSource flag + epsilon 1.0px pro scroll sync feedback loop prevenci — pokud levý scrolloval a source != Right, sync pravý a naopak; bez změny reset na None"
- "S02 jako jeden task — scope je koherentní (tlačítko, confirm dialog, restore logika, i18n), staví výhradně na existujících patternech (show_modal, HistorySplitResult), žádný nový UI pattern"
- "Restore signalizace přes HistorySplitResult.restore_confirmed, ne přímé volání — render funkce nemá &mut LocalHistory, restore musí proběhnout v workspace/mod.rs"
- "M004 decomposed into 3 slices: S01 (centrální dispatch + modifier fix), S02 (chybějící handlery + command palette), S03 (uživatelská konfigurace + dynamické labely) — S01 first jako high-risk jádro, S02 staví na dispatch, S03 jako low-risk konfigurace"
- "Command.shortcut se změní z Option<&'static str> na Option<KeyboardShortcut> — parsovaná forma pro dispatch i label rendering, string label odvozen z KeyboardShortcut::format()"
- "Keymap dispatch používá seřazený Vec<(KeyboardShortcut, CommandId)> od nejvíce modifikátorů po nejméně — egui consume_shortcut ignoruje extra modifikátory, řazení zajistí že trojkombinace matchne před dvoukombinací"
- "Všechny zkratky přes Modifiers::COMMAND (ne CTRL) — logický modifier: Ctrl na Linuxu, Cmd na macOS. Ruční cfg!(target_os) zbytečný."
- "Ctrl+A/C/V/X/Z/Y neregistrovány v centrálním keymapu — TextEdit je zpracuje sám, interceptování by rozbilo Copy/Paste/Undo"
- "Centrální dispatch napojený na existující execute_command → MenuActions → process_menu_actions pipeline — žádná duplikace logiky"
- "Uživatelská konfigurace keybindings přes HashMap<String, String> v [keybindings] sekci settings.toml — serde(default) pro backwards compatibility"
- "S01 rozdělen na 2 tasky: T01 (keymap modul + testy) a T02 (napojení na workspace + smazání ad-hoc handlerů) — T01 dodá testovatelné jádro bez UI závislostí, T02 provede wiring a ověří celý pipeline"
- "FocusEditor/FocusBuild/FocusClaude jako nové CommandId varianty — trojkombinace Ctrl+Alt+E/B/A procházejí stejným dispatch jako dvoukombinace, řazení zajistí správný match"
- "Focus panel commandy implementovány přes MenuActions flagy + process_menu_actions pipeline, ne přes přímý &mut WorkspaceState v execute_command — zachovává existující architektonický pattern a centralizuje side-effecty"
- "Keymap dispatch umístěn PŘED widget renderingem v render_workspace() — consume_shortcut konzumuje event, musí proběhnout dřív než widgety"
- "Clipboard (Ctrl+C/V/A) a editor-interní zkratky (Ctrl+F/H/G) ponechány s hardcoded menu labely — nejsou v command registry, TextEdit/editor je zpracovává sám"
- "S02 jako jeden task — scope je koherentní rozšíření jednoho pipeline (CommandId → registry → MenuActions → process_menu_actions), 9 kroků / 10 souborů (5 locale), žádná mezizávislost mezi kroky"
- "F1 jako alternativní binding pro CommandPalette — parse_shortcut rozšířen o whitelist standalone kláves (F1–F12, Escape, Delete, Insert) bez povinného modifikátoru"
- "Find/Replace/GotoLine přesunuty z editor-interních hardcoded handlerů do centrálního command registry — S01 decision o hardcoded labelech revidován, nově mají dynamické labely z keymapu"
- "Command palette toggle: Ctrl+Shift+P při otevřené palette ji zavře (VS Code chování), ne reopen"
- "Ctrl+F při otevřeném search baru refocusne input (VS Code chování), ne toggle off"
- "F1 alternativní binding registrován jako druhý Command záznam s id 'ui.command_palette_f1' a sdíleným i18n klíčem 'command-name-command-palette' — nejjednodušší přístup bez nového registračního mechanismu pro multi-binding"
- "is_standalone_key_allowed() jako privátní helper v keymap.rs — oddělení whitelistu od hlavní parse logiky, snadno rozšiřitelný pro budoucí standalone klávesy"
- "Menu položky s flagy pattern: kliknutí nastaví actions.* = true + ui.close_menu(), shortcut label přes shortcut_label(keymap, CommandId::*) — konzistentní s existujícím OpenFile/ProjectSearch/Build/Run vzorem"
