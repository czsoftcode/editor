---
phase: 11-file-operations-watcher-guard-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/ui/editor/files.rs
  - src/app/ui/editor/ui.rs
  - src/app/ui/terminal/mod.rs
  - src/app/ui/workspace/mod.rs
  - src/watcher.rs
  - src/settings.rs
  - src/app/local_history.rs
autonomous: true
requirements: [FILE-01, FILE-02, FILE-03, WATCH-01, WATCH-02]

must_haves:
  truths:
    - "Editor ukládá soubory přímo bez sandbox path kontroly"
    - "Editor nemá readonly režim vázaný na sandbox"
    - "Terminál vždy používá project_root bez sandbox switchingu"
    - "Watcher nefiltruje eventy podle sandbox cesty"
    - "Settings loading nevyžaduje sandbox migraci"
  artifacts:
    - path: "src/app/ui/editor/files.rs"
      provides: "File save/autosave without sandbox path checks"
      contains: "!.*\\.polycredo/sandbox"
    - path: "src/app/ui/editor/ui.rs"
      provides: "Editor UI without sandbox_mode_enabled param"
      contains: "!sandbox_mode_enabled"
    - path: "src/app/ui/terminal/mod.rs"
      provides: "Terminal without sandbox functions"
      contains: "!terminal_mode_label|!terminal_working_dir"
    - path: "src/watcher.rs"
      provides: "Watcher without sandbox event filter"
      contains: "!is_in_sandbox"
    - path: "src/settings.rs"
      provides: "Settings without migration function"
      contains: "!migrate_remove_sandbox_fields"
  key_links:
    - from: "src/app/ui/workspace/mod.rs"
      to: "src/app/ui/editor/ui.rs"
      via: "editor.ui() call without sandbox_mode_enabled param"
      pattern: "editor\\.ui\\("
---

<objective>
Odstranit sandbox logiku z editor file operations, terminal funkcí, watcher filtru a settings migrace.

Purpose: Editor pracuje přímo s projektovými soubory bez sandbox přesměrování a readonly guardu.
Output: Čisté editor/files.rs, editor/ui.rs, terminal/mod.rs, watcher.rs, settings.rs bez sandbox referencí.
</objective>

<execution_context>
@/home/stkremen/.claude/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-file-operations-watcher-guard-removal/11-CONTEXT.md

<interfaces>
<!-- Klíčové call chainy které se mění -->

From src/app/ui/workspace/mod.rs:300-306 (caller):
```rust
let editor_res = ws.editor.ui(
    ui,
    dialog_open_base,
    i18n,
    ws.lsp_client.as_ref(),
    &settings,
    false,  // sandbox_mode_enabled — REMOVE this arg
);
```

From src/app/ui/editor/ui.rs:12-16 (signature to change):
```rust
pub fn ui(
    &mut self, ui: &mut egui::Ui, dialog_open: bool,
    i18n: &crate::i18n::I18n,
    lsp_client: Option<&crate::app::lsp::LspClient>,
    settings: &crate::settings::Settings,
    sandbox_mode_enabled: bool,  // REMOVE
) -> EditorUiResult
```

From src/app/ui/editor/files.rs sandbox checks (lines 62-66, 96-99, 170):
```rust
// Line 62-66: autosave sandbox check
if tab.is_read_only
    && !tab.path.to_string_lossy().contains(".polycredo/sandbox") { ... }
// Line 96-99: save sandbox path check
if !path_str.contains(".polycredo/sandbox") { return; }
// Line 170: manual save sandbox path check
if !path_str.contains(".polycredo/sandbox") { return; }
```

From src/watcher.rs:114-119 (sandbox filter):
```rust
let is_in_sandbox =
    is_in_polycredo && p.as_path().to_string_lossy().contains("sandbox");
let skip = if is_in_polycredo && !is_in_sandbox {
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Odstranit sandbox logiku z editor files, editor UI a terminal</name>
  <files>src/app/ui/editor/files.rs, src/app/ui/editor/ui.rs, src/app/ui/terminal/mod.rs, src/app/ui/workspace/mod.rs</files>
  <action>
**editor/files.rs:**
- Řádek 62-66: Smazat celý readonly+sandbox autosave guard blok. Autosave by měl fungovat pro všechny taby bez omezení (readonly check ponechat jen pokud je `tab.is_read_only`, ale bez sandbox path podmínky).
- Řádek 96-99: Smazat celý blok "Safe Mode" sandbox path check — uživatel rozhodl smazat readonly logiku úplně.
- Řádek 170: Smazat analogický sandbox path check v manual save.
- Řádek 62 komentář: Smazat komentář "If strictly read-only and outside sandbox".
- Zkontrolovat zda po smazání zbývají prázdné if bloky nebo unreachable kód a vyčistit.

**editor/ui.rs:**
- Smazat parametr `sandbox_mode_enabled: bool` ze signatury `ui()` (řádek 15).
- Smazat celý `is_readonly` blok (řádky 88-94) — bez sandbox režimu není důvod mít readonly. Nastavit `is_readonly = false` přímo, nebo lépe smazat readonly logiku pokud ji nic jiného nepoužívá. Prozkoumat jak `is_readonly` ovlivňuje zbytek funkce a smazat odpovídající readonly branching.

**terminal/mod.rs:**
- Smazat celé 3 funkce: `terminal_mode_label()` (řádky 30-36), `terminal_mode_label_for_workdir()` (řádky 38-47), `terminal_working_dir()` (řádky 50-59).
- Smazat oba unit testy `test_terminal_mode_label_for_workdir_uses_sandbox_label` a `test_terminal_mode_label_for_workdir_uses_project_label` (řádky 64-79).
- Pokud po smazání zůstane prázdný `#[cfg(test)] mod tests`, smazat i ten.

**workspace/mod.rs:**
- Řádek 306: Odstranit argument `false` z volání `ws.editor.ui(...)` — nyní odpovídá nové signatuře bez sandbox_mode_enabled.

Spustit `cargo check` a opravit jakékoli kompilační chyby způsobené odstraněním.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>Editor ukládá soubory bez sandbox path kontroly, nemá sandbox_mode_enabled parametr, terminál nemá sandbox funkce. cargo check projde.</done>
</task>

<task type="auto">
  <name>Task 2: Odstranit sandbox filtr z watcheru, smazat settings migraci, vyčistit komentáře</name>
  <files>src/watcher.rs, src/settings.rs, src/app/local_history.rs</files>
  <action>
**watcher.rs (řádky 114-119):**
- Smazat sandbox filtr úplně. Aktuální logika: pokud je cesta v `.polycredo/` ale ne v `sandbox/`, přeskočí. Po sandbox removu chceme přeskakovat CELÝ `.polycredo/` adresář bez výjimky.
- Zjednodušit podmínku: `let skip = is_in_polycredo;` — celý .polycredo/ se ignoruje.
- Smazat proměnnou `is_in_sandbox` a komentář "We only want to watch the sandbox inside .polycredo".

**settings.rs:**
- Smazat funkci `migrate_remove_sandbox_fields()` (řádky 198-230 přibližně).
- Smazat oba volání `migrate_remove_sandbox_fields()` v loading kódu (řádky 238 a 247).
- Smazat test `test_settings_migration_strips_sandbox_fields` (řádky 615-662).
- Serde default values zvládnou neznámá pole v existujících settings souborech.

**local_history.rs (řádek 66):**
- Upravit komentář: `// Never take snapshots of the internal .polycredo directory (history, sandbox, etc.)` → `// Never take snapshots of the internal .polycredo directory (history, etc.)`

Spustit `cargo check` a `cargo test` pro ověření.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor && cargo check 2>&1 | tail -5 && cargo test 2>&1 | tail -10</automated>
  </verify>
  <done>Watcher ignoruje celý .polycredo/ adresář, settings nemá sandbox migraci, testy procházejí. Žádné sandbox reference ve watcher.rs a settings.rs.</done>
</task>

</tasks>

<verification>
```bash
# Ověřit že žádné sandbox reference nezůstaly v dotčených souborech
grep -n "sandbox" src/app/ui/editor/files.rs src/app/ui/editor/ui.rs src/app/ui/terminal/mod.rs src/watcher.rs src/settings.rs
# Očekávaný výstup: prázdný (žádné shody)

# Kompilace
cargo check

# Testy
cargo test
```
</verification>

<success_criteria>
- Žádné "sandbox" reference v editor/files.rs, editor/ui.rs, terminal/mod.rs, watcher.rs, settings.rs
- sandbox_mode_enabled parametr neexistuje v žádné signatuře
- Watcher ignoruje celý .polycredo/ adresář
- Settings loading funguje bez migrace
- cargo check a cargo test procházejí
</success_criteria>

<output>
After completion, create `.planning/phases/11-file-operations-watcher-guard-removal/11-01-SUMMARY.md`
</output>
