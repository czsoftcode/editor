---
phase: 25-unsaved-close-guard
plan: 10
type: execute
wave: 2
depends_on:
  - 25-08
files_modified:
  - src/app/ui/workspace/mod.rs
  - src/app/ui/workspace/state/mod.rs
  - src/app/ui/workspace/tests/unsaved_close_guard.rs
autonomous: true
gap_closure: true
requirements:
  - GUARD-01
  - GUARD-02
  - GUARD-03

must_haves:
  truths:
    - "SingleTab close (Ctrl+W i klik na X) řeší pouze cílový tab, ne všechny dirty taby."
    - "WorkspaceClose/Root close nadále iteruje přes všechny dirty taby v deterministickém pořadí."
    - "Klik na X řeší správný target tab bez závislosti na aktuální active_tab."
  artifacts:
    - path: "src/app/ui/workspace/state/mod.rs"
      provides: "Queue builder rozlišený podle close mode (SingleTab vs WorkspaceClose)"
      contains: "build_dirty_close_queue nebo ekvivalent"
    - path: "src/app/ui/workspace/mod.rs"
      provides: "Předání explicitního target tabu pro TabBarAction::Close(idx)"
      contains: "close flow start API"
    - path: "src/app/ui/workspace/tests/unsaved_close_guard.rs"
      provides: "Regresní testy pro target tab a režimové chování fronty"
      contains: "SingleTab vs WorkspaceClose coverage"
  key_links:
    - from: "TabBarAction::Close(idx)"
      to: "PendingCloseFlow"
      via: "explicit target path"
      pattern: "idx -> path"
---

<objective>
Opravit cílení close guardu tak, aby akce zavření jednoho tabu neprocházela všechny dirty
soubory a vždy řešila přesně ten tab, který uživatel zavírá.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/25-unsaved-close-guard/25-UAT.md
@.planning/debug/unsaved-queue-target-order.md
@src/app/ui/workspace/mod.rs
@src/app/ui/workspace/state/mod.rs
@src/app/ui/workspace/tests/unsaved_close_guard.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rozdělení queue builderu podle close mode</name>
  <files>src/app/ui/workspace/state/mod.rs</files>
  <action>
    1. Zaveď explicitní režimy fronty: `SingleTab(target)` a `WorkspaceClose(all dirty)`.
    2. Pro `SingleTab` vracej frontu s jedinou položkou cílového dirty tabu; clean target se zavře bez guardu.
    3. Pro `WorkspaceClose` zachovej stávající deterministic active-first/all-dirty chování.
  </action>
  <verify>
    <automated>cargo test unsaved_close_guard_queue_single_tab_target -- --nocapture</automated>
  </verify>
  <done>
    Queue builder vrací správné položky podle typu close akce.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Explicitní target tab pro klik na X i Ctrl+W</name>
  <files>src/app/ui/workspace/mod.rs</files>
  <action>
    1. U `TabBarAction::Close(idx)` předávej do close flow explicitní target identifikovaný indexem/path.
    2. U Ctrl+W zachovej target aktivního tabu, ale nepřepínej flow na globální dirty queue.
    3. Ošetři race scénář, kdy target mezitím zmizí (bez paniky, bezpečný no-op nebo fallback).
  </action>
  <verify>
    <automated>cargo test unsaved_close_guard_target_tab_from_tabbar_close -- --nocapture</automated>
  </verify>
  <done>
    Close guard řeší správný tab bez závislosti na pozdějších změnách active_tab.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Úprava a doplnění regresních testů pro SingleTab cílení</name>
  <files>src/app/ui/workspace/tests/unsaved_close_guard.rs</files>
  <action>
    1. Přepiš testy, které dosud u SingleTab očekávají iteraci přes všechny dirty taby.
    2. Doplň test: klik na X na neaktivním dirty tabu otevře guard právě pro tento tab.
    3. Doplň test: Ctrl+W nad aktivním dirty tabem neiteruje další dirty položky.
  </action>
  <verify>
    <automated>cargo test unsaved_close_guard_single_tab_regressions -- --nocapture</automated>
  </verify>
  <done>
    Test suite explicitně hlídá, že SingleTab flow není zaměněn za WorkspaceClose flow.
  </done>
</task>

</tasks>

<verification>
cargo check
cargo test unsaved_close_guard -- --nocapture
./check.sh
</verification>

<success_criteria>
- SingleTab close neiteruje přes všechny dirty taby
- X i Ctrl+W řeší správný target tab
- Workspace/root close stále prochází všechny dirty taby dle guard pravidel
</success_criteria>
