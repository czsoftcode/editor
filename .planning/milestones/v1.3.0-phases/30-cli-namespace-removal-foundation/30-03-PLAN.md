---
phase: 30-cli-namespace-removal-foundation
plan: 03
type: execute
wave: 4
depends_on:
  - 30-02
files_modified:
  - src/app/mod.rs
  - src/app/ui/mod.rs
  - src/app/ui/workspace/mod.rs
  - src/app/ui/workspace/state/mod.rs
  - src/app/ui/workspace/state/init.rs
  - src/app/ui/background.rs
  - src/app/types.rs
  - src/settings.rs
  - .planning/phases/30-cli-namespace-removal-foundation/30-VERIFICATION.md
autonomous: true
requirements:
  - CLI-03
  - CLI-02

must_haves:
  truths:
    - "Po cleanup passu nezůstávají mrtvé `mod`/`pub use` exporty vázané na původní CLI vrstvu."
    - "Finální grep audit nevrací žádný `app::cli` ani `crate::app::cli` odkaz ve `src/`."
    - "Povinná quality gate `./check.sh` je zelená v konfiguraci bez legacy namespace."
  artifacts:
    - path: "src/app/mod.rs"
      provides: "Čistý modulový strom bez CLI leaků"
      contains: "pub mod ai_core"
    - path: ".planning/phases/30-cli-namespace-removal-foundation/30-VERIFICATION.md"
      provides: "Důkazy grep/build gate pro CLI-02/CLI-03"
      contains: "CLI-02"
  key_links:
    - from: "Export cleanup"
      to: "CLI-03 acceptance"
      via: "Odstranění mrtvých modulů/re-exportů + compile clean"
      pattern: "pub mod"
---

<objective>
Dokončit strukturální cleanup po odstranění CLI vrstvy, odstranit mrtvé exporty a potvrdit
fázový done-state přes finální audit (`rg`) a quality gate (`cargo check`, `./check.sh`).
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-cli-namespace-removal-foundation/30-CONTEXT.md
@.planning/phases/30-cli-namespace-removal-foundation/30-RESEARCH.md
@.planning/phases/30-cli-namespace-removal-foundation/30-VALIDATION.md
@./CLAUDE.md
@.planning/phases/30-cli-namespace-removal-foundation/30-01-PLAN.md
@.planning/phases/30-cli-namespace-removal-foundation/30-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dead export/modul cleanup pass</name>
  <files>src/app/mod.rs, src/app/ui/mod.rs, src/app/ui/workspace/mod.rs, src/app/ui/workspace/state/mod.rs, src/app/ui/workspace/state/init.rs, src/app/ui/background.rs, src/app/types.rs, src/settings.rs</files>
  <action>
    1. Projdi `mod.rs` a re-export body a smaž vše, co existovalo jen kvůli `app::cli` vrstvě.
    2. Odstraň nepoužité typy/aliasy/importy navázané na legacy CLI API.
    3. Udrž minimální public API `app::ai_core` pouze pro runtime-critical call sites.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>
    Modulový strom je očištěný od mrtvých CLI exportů a kompiluje.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Final namespace and export audit</name>
  <files>src/</files>
  <action>
    1. Spusť audit: `rg -n "crate::app::cli|app::cli" src` s očekáváním prázdného výsledku.
    2. Spusť export audit: `rg -n "pub mod cli|mod cli" src/app` s očekáváním prázdného výsledku.
    3. Pokud audit najde zbytky, proveď okamžitý cleanup v tom samém commitu.
  </action>
  <verify>
    <automated>rg -n "crate::app::cli|app::cli" src</automated>
  </verify>
  <done>
    Namespace i export audit potvrzuje splnění CLI-02 a CLI-03.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mandatory quality gate + verification artifact</name>
  <files>.planning/phases/30-cli-namespace-removal-foundation/30-VERIFICATION.md</files>
  <action>
    1. Spusť finální quality gate: `cargo check` a `./check.sh`.
    2. Zapiš do `30-VERIFICATION.md` konkrétní výstupy auditů pro CLI-02/CLI-03.
    3. Pokud `./check.sh` selže, řeš blocker in-phase (stop-and-fix policy), neodkládej do další fáze.
  </action>
  <verify>
    <automated>./check.sh</automated>
  </verify>
  <done>
    Fáze 30 má finální důkazy o čistém namespace i quality gate stavu.
  </done>
</task>

</tasks>

<verification>
cargo check
./check.sh
rg -n "crate::app::cli|app::cli" src
rg -n "pub mod cli|mod cli" src/app
</verification>

<success_criteria>
- V `src/` nejsou žádné odkazy na `app::cli`
- Neexistují mrtvé `mod cli`/re-export vazby
- `cargo check` a `./check.sh` jsou zelené
- CLI-03 (a finální CLI-02) jsou pokryty auditními důkazy
</success_criteria>
