---
phase: 24-save-mode-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 24-01
  - 24-02
files_modified:
  - src/app/ui/workspace/mod.rs
  - src/app/ui/workspace/menubar/mod.rs
  - src/app/ui/background.rs
  - src/app/ui/editor/files.rs
  - locales/cs/errors.ftl
  - locales/en/errors.ftl
  - locales/de/errors.ftl
  - locales/ru/errors.ftl
  - locales/sk/errors.ftl
autonomous: true
requirements:
  - SAVE-01
  - SAVE-02
  - SAVE-03
  - MODE-03

must_haves:
  truths:
    - "Ctrl+S i menu Save používají stejný ruční save handler pro aktivní tab."
    - "Ruční save okamžitě přepíná modified -> saved bez nutnosti změny fokusu."
    - "Save chyby se vždy propagují do UI toastu a tab zůstává Modified."
    - "Při opakovaném stejném save selhání v krátkém intervalu uživatel nedostává toast spam."
    - "V Manual režimu se background autosave nevykonává, v Automatic režimu zůstává aktivní."
  artifacts:
    - path: "src/app/ui/workspace/mod.rs"
      provides: "Unified manual save shortcut flow"
      contains: "Ctrl+S"
    - path: "src/app/ui/workspace/menubar/mod.rs"
      provides: "Menu Save napojené na stejný handler"
      contains: "actions.save"
    - path: "src/app/ui/background.rs"
      provides: "Autosave gate podle save_mode"
      contains: "try_autosave"
  key_links:
    - from: "workspace Ctrl+S"
      to: "editor save pipeline"
      via: "shared helper volaný i z menubar Save"
      pattern: "save"
    - from: "background tick"
      to: "Settings.save_mode"
      via: "gate before try_autosave"
      pattern: "SaveMode::Automatic"
---

<objective>
Sjednotit save pipeline pro ruční ukládání (`Ctrl+S` + menu Save), doplnit explicitní user feedback
(včetně no-op případu) a zavést runtime gate autosave podle zvoleného save režimu.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-save-mode-foundation/24-CONTEXT.md
@.planning/phases/24-save-mode-foundation/24-RESEARCH.md
@.planning/phases/24-save-mode-foundation/24-VALIDATION.md
@./CLAUDE.md
@src/app/ui/workspace/mod.rs
@src/app/ui/workspace/menubar/mod.rs
@src/app/ui/background.rs
@src/app/ui/editor/files.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unified manual save handler pro Ctrl+S + menu Save</name>
  <files>src/app/ui/workspace/mod.rs, src/app/ui/workspace/menubar/mod.rs, src/app/ui/editor/files.rs</files>
  <action>
    1. Vyextrahuj jednotný helper pro ruční save aktivního tabu a používej ho jak pro `Ctrl+S`, tak pro menu `Save`.
    2. Pokud aktivní tab není modified, vrať info toast „soubor už je uložen“ (no-op feedback).
    3. Pokud save selže, zobraz error toast; status tabu musí zůstat `Modified`.
    4. Ošetři modal kontext: při otevřeném Settings modalu nesmí `Ctrl+S` ukládat editor soubor na pozadí.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>
    Ruční save je konzistentní napříč zkratkou a menu, se správným feedbackem.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Autosave gate podle SaveMode + i18n save feedbacku</name>
  <files>src/app/ui/background.rs, locales/cs/errors.ftl, locales/en/errors.ftl, locales/de/errors.ftl, locales/ru/errors.ftl, locales/sk/errors.ftl</files>
  <action>
    1. V background event loopu volej `try_autosave()` pouze při `SaveMode::Automatic`.
    2. V `SaveMode::Manual` autosave path přeskoč bez side-effectů a bez nové polling logiky.
    3. Doplň i18n klíč pro no-op save info (soubor už je uložen) do všech jazyků.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>
    Autosave respektuje režim a save feedback je lokalizovaný.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deduplikace opakovaných save error toastů</name>
  <files>src/app/ui/workspace/mod.rs, src/app/ui/background.rs, src/app/types.rs</files>
  <action>
    1. Zaveď jednotnou dedupe policy pro opakované save chyby (stejný klíč chyby + krátké časové okno) pro ruční save i autosave.
    2. Ujisti se, že první výskyt chyby je vždy zobrazen, ale opakované identické chyby v okně jsou potlačené.
    3. Po uplynutí okna nebo změně chyby se toast opět zobrazí.
    4. Doplň cílený test/helper test pro dedupe rozhodování, aby byla politika binárně ověřitelná.
  </action>
  <verify>
    <automated>cargo check && cargo test save_error_dedupe -- --nocapture</automated>
  </verify>
  <done>
    Save error feedback je robustní a nezahlcuje uživatele opakovanými identickými toasty.
  </done>
</task>

</tasks>

<verification>
cargo check
./check.sh
</verification>

<success_criteria>
- `Ctrl+S` i menu Save spouští stejnou save logiku
- `Modified -> Saved` přechod proběhne hned po ručním save
- Save chyba je viditelná jako toast a neprobíhá tiché selhání
- Opakované stejné save chyby jsou deduplikované v krátkém intervalu
- Autosave běží jen v `Automatic` režimu a je vypnutý v `Manual`
</success_criteria>
