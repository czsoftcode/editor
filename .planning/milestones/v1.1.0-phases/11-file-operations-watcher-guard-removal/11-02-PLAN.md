---
phase: 11-file-operations-watcher-guard-removal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/registry/mod.rs
  - src/app/registry/plugins/mod.rs
  - src/app/registry/plugins/security.rs
  - src/app/registry/plugins/host/fs.rs
  - src/app/registry/plugins/host/sys.rs
  - src/app/registry/plugins/host/search.rs
  - src/app/ai/tools.rs
  - src/app/ai/types.rs
  - src/app/ui/ai_panel.rs
  - src/plugins/gemini/src/lib.rs
  - src/plugins/ollama/src/lib.rs
autonomous: true
requirements: [GIT-01, GIT-02]

must_haves:
  truths:
    - "Plugin registry používá project_root místo sandbox_root"
    - "AI tool se jmenuje 'exec' místo 'exec_in_sandbox'"
    - "System prompty neobsahují 'exec_in_sandbox'"
    - "WASM pluginy (gemini, ollama) volají 'exec' místo 'exec_in_sandbox'"
  artifacts:
    - path: "src/app/registry/plugins/mod.rs"
      provides: "PluginManager with project_root field"
      contains: "project_root"
    - path: "src/app/registry/plugins/security.rs"
      provides: "PluginHostState with project_root"
      contains: "project_root"
    - path: "src/app/ai/tools.rs"
      provides: "exec tool definition"
      contains: "name: \"exec\""
    - path: "src/app/ai/types.rs"
      provides: "System prompts without exec_in_sandbox"
      contains: "!exec_in_sandbox"
  key_links:
    - from: "src/app/registry/mod.rs"
      to: "src/app/registry/plugins/mod.rs"
      via: "PluginManager::new(project_root)"
      pattern: "PluginManager::new"
    - from: "src/app/registry/plugins/mod.rs"
      to: "src/app/registry/plugins/host/sys.rs"
      via: "host_exec function registration"
      pattern: "host_exec"
    - from: "src/plugins/gemini/src/lib.rs"
      to: "src/app/registry/plugins/host/sys.rs"
      via: "WASM extern fn exec"
      pattern: "fn exec"
---

<objective>
Přejmenovat sandbox_root na project_root v celém plugin systému a přejmenovat AI tool exec_in_sandbox na exec.

Purpose: Plugin registry a AI tools neobsahují sandbox terminologii — vše pracuje přímo s project root.
Output: Čisté registry/plugins/*, ai/tools.rs, ai/types.rs a WASM pluginy bez sandbox referencí.
</objective>

<execution_context>
@/home/stkremen/.claude/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-file-operations-watcher-guard-removal/11-CONTEXT.md

<interfaces>
<!-- Klíčové struktury a funkce k přejmenování -->

From src/app/registry/plugins/mod.rs:
```rust
pub struct PluginManager {
    pub sandbox_root: PathBuf,   // → project_root
    // ...
}
impl PluginManager {
    pub fn new(sandbox_root: PathBuf) -> Self { ... }  // → project_root
}
```

From src/app/registry/plugins/security.rs:
```rust
pub struct PluginHostState {
    pub sandbox_root: PathBuf,   // → project_root
    // ...
}
```

From src/app/registry/mod.rs:159:
```rust
pub fn new(sandbox_root: std::path::PathBuf) -> Self {
    // ...
    plugins: Arc::new(PluginManager::new(sandbox_root)),
}
```

From src/app/mod.rs:126 (caller — already uses project_root_for_registry):
```rust
let mut registry = crate::app::registry::Registry::new(project_root_for_registry);
```

From src/app/registry/plugins/host/sys.rs:53:
```rust
pub fn host_exec_in_sandbox(...)  // → host_exec
```

From src/app/ai/tools.rs:106-107:
```rust
name: "exec_in_sandbox".to_string(),
description: "Executes a shell command within the project sandbox and returns output.".to_string(),
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Přejmenovat sandbox_root na project_root v plugin registry</name>
  <files>src/app/registry/mod.rs, src/app/registry/plugins/mod.rs, src/app/registry/plugins/security.rs, src/app/registry/plugins/host/fs.rs, src/app/registry/plugins/host/sys.rs, src/app/registry/plugins/host/search.rs</files>
  <action>
**registry/plugins/mod.rs:**
- Řádek 18: `pub sandbox_root: PathBuf` → `pub project_root: PathBuf`
- Řádek 26: `pub fn new(sandbox_root: PathBuf)` → `pub fn new(project_root: PathBuf)`
- Řádek 33: `sandbox_root,` → `project_root,`
- Řádek 48: `.sandbox_root` → `.project_root`
- Řádek 220: `sandbox_root: self.sandbox_root.clone()` → `project_root: self.project_root.clone()`
- Řádek 315: `"exec_in_sandbox"` → `"exec"`
- Řádek 319: `host_exec_in_sandbox` → `host_exec`

**registry/plugins/security.rs:**
- Řádek 39: `pub sandbox_root: PathBuf` → `pub project_root: PathBuf`

**registry/plugins/host/fs.rs:**
- Řádky 37, 134, 185: `state.sandbox_root.join(rel_path)` → `state.project_root.join(rel_path)`
- Řádek 463: `let root = &state.sandbox_root` → `let root = &state.project_root`

**registry/plugins/host/sys.rs:**
- Řádek 53: Přejmenovat funkci `host_exec_in_sandbox` → `host_exec`
- Řádek 164: `"exec_in_sandbox"` → `"exec"` v help textu
- Řádek 184: `.current_dir(&state.sandbox_root)` → `.current_dir(&state.project_root)`

**registry/plugins/host/search.rs:**
- Řádek 20: Aktualizovat komentář — nahradit `'exec_in_sandbox'` za `'exec'`

**registry/mod.rs:**
- Řádek 159: `pub fn new(sandbox_root: std::path::PathBuf)` → `pub fn new(project_root: std::path::PathBuf)`
- Řádek 164: `PluginManager::new(sandbox_root)` → `PluginManager::new(project_root)`

Spustit `cargo check` a opravit jakékoli kompilační chyby.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>Plugin registry používá project_root všude, exec funkce přejmenována. cargo check projde.</done>
</task>

<task type="auto">
  <name>Task 2: Přejmenovat exec_in_sandbox na exec v AI tools, system promptech a WASM pluginech</name>
  <files>src/app/ai/tools.rs, src/app/ai/types.rs, src/app/ui/ai_panel.rs, src/plugins/gemini/src/lib.rs, src/plugins/ollama/src/lib.rs</files>
  <action>
**ai/tools.rs:**
- Řádek 106: `name: "exec_in_sandbox".to_string()` → `name: "exec".to_string()`
- Řádek 107: `description: "Executes a shell command within the project sandbox and returns output."` → `description: "Executes a shell command within the project directory and returns output."`

**ai/types.rs (system prompty):**
- Řádek 30: Nahradit `'exec_in_sandbox'` za `'exec'` v system prompt textu
- Řádek 32: Nahradit `'exec_in_sandbox: cargo check'` za `'exec: cargo check'`
- Řádek 41: Nahradit `'exec_in_sandbox: cargo check'` za `'exec: cargo check'`
- Řádek 50: Nahradit `'exec_in_sandbox: cargo check'` za `'exec: cargo check'`

**ai_panel.rs:**
- Řádek 161: Smazat komentář `// Start agent immediately (no sandbox sync needed)` — je to zbytečný komentář po sandbox removu, nebo ho přeformulovat na neutrální.

**plugins/gemini/src/lib.rs:**
- Řádek 124: `fn exec_in_sandbox(command: String) -> String;` → `fn exec(command: String) -> String;`
- Řádek 260: `"exec_in_sandbox"` → `"exec"` v match větvi
- Řádek 261: `exec_in_sandbox(...)` → `exec(...)` ve volání

**plugins/ollama/src/lib.rs:**
- Řádek 142: `fn exec_in_sandbox(command: String) -> String;` → `fn exec(command: String) -> String;`
- Řádek 421: `"exec_in_sandbox"` → `"exec"` v match větvi
- Řádek 422: `exec_in_sandbox(...)` → `exec(...)` ve volání

Spustit `cargo check` a `cargo test` pro ověření.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor && cargo check 2>&1 | tail -5 && cargo test 2>&1 | tail -10</automated>
  </verify>
  <done>AI tool se jmenuje "exec", system prompty používají "exec", WASM pluginy volají "exec". Žádné "exec_in_sandbox" reference v codebase. cargo check a cargo test procházejí.</done>
</task>

</tasks>

<verification>
```bash
# Ověřit že žádné sandbox reference nezůstaly v dotčených souborech
grep -rn "sandbox" src/app/registry/ src/app/ai/ src/plugins/
# Očekávaný výstup: prázdný (žádné shody)

# Kompilace
cargo check

# Testy
cargo test
```
</verification>

<success_criteria>
- Žádné "sandbox_root" reference v registry/* souborech
- Žádné "exec_in_sandbox" reference v ai/* a plugins/* souborech
- PluginManager a PluginHostState používají "project_root"
- AI tool se jmenuje "exec"
- System prompty neobsahují "exec_in_sandbox"
- cargo check a cargo test procházejí
</success_criteria>

<output>
After completion, create `.planning/phases/11-file-operations-watcher-guard-removal/11-02-SUMMARY.md`
</output>
