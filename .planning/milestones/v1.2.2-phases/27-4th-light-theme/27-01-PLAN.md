---
phase: 27-4th-light-theme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/settings.rs
  - src/app/ui/workspace/modal_dialogs/settings.rs
  - locales/cs/ui.ftl
  - locales/en/ui.ftl
  - locales/sk/ui.ftl
  - locales/de/ui.ftl
  - locales/ru/ui.ftl
autonomous: true
requirements:
  - THEME-01
  - THEME-02
  - THEME-03
  - THEME-04

must_haves:
  truths:
    - "User can see WarmTan option in Settings → Theme picker UI"
    - "User can select WarmTan and see immediate visual change"
    - "User can restart app and WarmTan is still selected (persisted in settings.toml)"
    - "WarmTan shows visual swatch in theme picker"
    - "WarmTan has localized label in all 5 languages (cs, en, de, ru, sk)"
  artifacts:
    - path: "src/settings.rs"
      provides: "LightVariant enum + to_egui_visuals() colors"
      contains: "WarmTan"
    - path: "src/app/ui/workspace/modal_dialogs/settings.rs"
      provides: "Theme picker UI with swatch"
      contains: "light_variant_label_key"
    - path: "locales/*/ui.ftl"
      provides: "i18n translations for 5 languages"
      contains: "settings-light-variant-warm-tan"
---

<objective>
Přidat 4. světlé téma "WarmTan" do aplikace.

Purpose: Rozšířit výběr světlých témat o variantu mezi Sepia a hnědou, příjemnou pro oči.
Output: WarmTan jako 4. LightVariant v settings, picker UI, i18n lokalizace
</objective>

<execution_context>
@/home/stkremen/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/27-4th-light-theme/27-CONTEXT.md

From src/settings.rs (LightVariant enum - lines 63-68):
```rust
pub enum LightVariant {
    #[default]
    WarmIvory,
    CoolGray,
    Sepia,
}
```

From src/settings.rs (to_egui_visuals() - lines 308-324):
```rust
match self.light_variant {
    LightVariant::WarmIvory => {
        visuals.panel_fill = eframe::egui::Color32::from_rgb(255, 252, 240);
        visuals.window_fill = eframe::egui::Color32::from_rgb(250, 246, 235);
        visuals.faint_bg_color = eframe::egui::Color32::from_rgb(247, 241, 226);
    }
    LightVariant::CoolGray => { ... }
    LightVariant::Sepia => { ... }
}
```

From src/app/ui/workspace/modal_dialogs/settings.rs (label keys - lines 16-21):
```rust
fn light_variant_label_key(variant: &LightVariant) -> &'static str {
    match variant {
        LightVariant::WarmIvory => "settings-light-variant-warm-ivory",
        LightVariant::CoolGray => "settings-light-variant-cool-gray",
        LightVariant::Sepia => "settings-light-variant-sepia",
    }
}
```

From locales/en/ui.ftl (existing i18n keys):
```
settings-light-variant-warm-ivory = Warm Ivory
settings-light-variant-cool-gray = Cool Gray
settings-light-variant-sepia = Sepia
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WarmTan to LightVariant enum in settings.rs</name>
  <files>src/settings.rs</files>
  <action>
Přidat `WarmTan` variantu do `LightVariant` enum na řádku 63-68:

```rust
pub enum LightVariant {
    #[default]
    WarmIvory,
    CoolGray,
    Sepia,
    WarmTan,
}
```

Přidat match větev do `to_egui_visuals()` na řádcích 308-324 (před závěrečné `visuals`):

```rust
LightVariant::WarmTan => {
    visuals.panel_fill = eframe::egui::Color32::from_rgb(215, 200, 185);
    visuals.window_fill = eframe::egui::Color32::from_rgb(205, 190, 175);
    visuals.faint_bg_color = eframe::egui::Color32::from_rgb(195, 180, 165);
}
```
  </action>
  <verify>cargo check --lib 2>&1 | grep -E "(error|warning: unused)" | head -20</verify>
  <done>WarmTan je v enum a má barvy v to_egui_visuals()</done>
</task>

<task type="auto">
  <name>Task 2: Add WarmTan to settings UI picker</name>
  <files>src/app/ui/workspace/modal_dialogs/settings.rs</files>
  <action>
Přidat `WarmTan` do `light_variant_label_key()` (řádky 16-22):

```rust
fn light_variant_label_key(variant: &LightVariant) -> &'static str {
    match variant {
        LightVariant::WarmIvory => "settings-light-variant-warm-ivory",
        LightVariant::CoolGray => "settings-light-variant-cool-gray",
        LightVariant::Sepia => "settings-light-variant-sepia",
        LightVariant::WarmTan => "settings-light-variant-warm-tan",
    }
}
```

Přidat swatch do `light_variant_swatch()` (řádky 24-30):

```rust
fn light_variant_swatch(variant: &LightVariant) -> egui::Color32 {
    match variant {
        LightVariant::WarmIvory => egui::Color32::from_rgb(250, 246, 235),
        LightVariant::CoolGray => egui::Color32::from_rgb(236, 236, 236),
        LightVariant::Sepia => egui::Color32::from_rgb(234, 223, 202),
        LightVariant::WarmTan => egui::Color32::from_rgb(215, 200, 185),
    }
}
```
  </action>
  <verify>cargo check --lib 2>&1 | grep -E "(error|warning: unused)" | head -20</verify>
  <done>WarmTan se zobrazuje v UI pickeru jako swatch s labelem</done>
</task>

<task type="auto">
  <name>Task 3: Add i18n translations for WarmTan in all 5 languages</name>
  <files>locales/cs/ui.ftl, locales/en/ui.ftl, locales/sk/ui.ftl, locales/de/ui.ftl, locales/ru/ui.ftl</files>
  <action>
Přidat překladový klíč `settings-light-variant-warm-tan` do každého souboru:

**locales/cs/ui.ftl** (po řádku 222):
```
settings-light-variant-warm-tan = Teplý tan
```

**locales/en/ui.ftl** (po řádku 217):
```
settings-light-variant-warm-tan = Warm Tan
```

**locales/sk/ui.ftl** (po řádku 221):
```
settings-light-variant-warm-tan = Teplý tan
```

**locales/de/ui.ftl** (po řádku 217):
```
settings-light-variant-warm-tan = Warme Tan
```

**locales/ru/ui.ftl** (po řádku 236):
```
settings-light-variant-warm-tan = Тёплый тан
```
  </action>
  <verify>cargo check --lib 2>&1 | grep -E "(error|warning: unused)" | head -20</verify>
  <done>WarmTan má lokalizovaný název ve všech 5 jazycích</done>
</task>

</tasks>

<verification>
- [ ] cargo check --lib úspěšný
- [ ] Nová varianta WarmTan se zobrazuje v Settings > Theme picker
- [ ] Po restartu aplikace je WarmTan stále vybrán (persistence)
- [ ] Swatch barva odpovídá definovaným RGB hodnotám
</verification>

<success_criteria>
User can see WarmTan option in Settings → Theme picker UI, can select it, and it persists across restart.
</success_criteria>

<output>
After completion, create `.planning/phases/27-4th-light-theme/27-01-SUMMARY.md`
</output>
