---
phase: 01-zaklad
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/ui/editor/ui.rs
autonomous: true
gap_closure: true
requirements: [UI-03]

must_haves:
  truths:
    - "Status bar text barvy nejsou hardcoded světlé RGB konstanty; odvozují se z aktivního tématu (`ui.visuals()`)."
    - "V light mode je text status baru (path, line/col, encoding, file type, save/LSP stavy) čitelný vůči backgroundu."
    - "Při runtime přepnutí dark/light se barvy status baru aktualizují okamžitě bez restartu."
  artifacts:
    - path: "src/app/ui/editor/ui.rs"
      provides: "Theme-aware status bar palette použitá v `Editor::status_bar()`."
      exports: ["Editor::status_bar"]
      contains: "ui\\.visuals\\(\\)\\.text_color\\(\\)|ui\\.visuals\\(\\)\\.weak_text_color\\(\\)"
  key_links:
    - from: "Editor::status_bar()"
      to: "egui::Visuals"
      via: "theme-aware výběr primární/sekundární text barvy"
      pattern: "status_bar\\(|ui\\.visuals\\(\\)"
---

<objective>
Uzavřít UAT gap #6: status bar text je v light mode příliš světlý a špatně čitelný.

Purpose: Nahradit fixní status bar text paletu za theme-aware barvy, aby byl kontrast stabilní v dark i light režimu.
Output: `src/app/ui/editor/ui.rs` s theme-aware status bar barvami bez hardcoded světle-modrých konstant.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-zaklad/01-UAT.md
@.planning/debug/status-bar-text-too-light-in-light-mode.md
@src/app/ui/editor/ui.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Převést status bar text paletu na theme-aware barvy</name>
  <files>src/app/ui/editor/ui.rs</files>
  <action>
    V `Editor::status_bar()` nahraď fixní barvy:
    - `primary_color = Color32::from_rgb(235, 240, 248)`
    - `secondary_color = Color32::from_rgb(195, 205, 220)`

    theme-aware variantou z `ui.visuals()` (`text_color()`, `weak_text_color()` a případně theme-branching pro warning/ok text), aby light mode měl dostatečný kontrast.

    Zachovej existující status bar layout a pořadí segmentů; cílem je pouze kontrastní korekce barev.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor/.polycredo/sandbox && cargo check 2>&1 | tail -30</automated>
  </verify>
  <done>
    `cargo check` prochází a status bar už není založený na fixních světlých RGB konstantách pro primární/sekundární text.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sjednotit kontrast pro diagnostiky a save/LSP stavy</name>
  <files>src/app/ui/editor/ui.rs</files>
  <action>
    Zkontroluj a případně dolaď barvy diagnostik (`✕`, `⚠`) a save/LSP stavů tak, aby v light mode nezanikaly a zároveň zůstaly konzistentní v dark mode.

    Přidej krátký komentář v místě výběru palety, že barvy jsou theme-aware kvůli čitelnosti ve light režimu (UAT gap closure).
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor/.polycredo/sandbox && rg -n \"from_rgb\\(235, 240, 248\\)|from_rgb\\(195, 205, 220\\)|status_bar\" src/app/ui/editor/ui.rs</automated>
  </verify>
  <done>
    Status bar používá theme-aware palette pro hlavní text i stavové segmenty, bez návratu k problematickým fixním světlým barvám.
  </done>
</task>

</tasks>

<verification>
- `cargo check 2>&1` bez chyb
- `rg -n "from_rgb\\(235, 240, 248\\)|from_rgb\\(195, 205, 220\\)" src/app/ui/editor/ui.rs` nevrátí původní fixní textové konstanty
- Manuální smoke: v light mode je status bar text čitelný (path, line/col, encoding, save/LSP stavy)
</verification>

<success_criteria>
- Status bar text je čitelný v light mode (UI-03)
- Textové barvy status baru se odvozují z aktivního tématu
- Runtime přepnutí dark/light okamžitě mění status bar barvy
- `cargo check` prochází
</success_criteria>

<output>
Po dokončení vytvoř `.planning/phases/01-zaklad/01-04-SUMMARY.md`
</output>
