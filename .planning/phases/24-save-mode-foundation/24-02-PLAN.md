---
phase: 24-save-mode-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - src/app/ui/workspace/modal_dialogs/settings.rs
  - src/app/ui/workspace/mod.rs
  - locales/cs/ui.ftl
  - locales/en/ui.ftl
  - locales/de/ui.ftl
  - locales/ru/ui.ftl
  - locales/sk/ui.ftl
autonomous: true
requirements:
  - MODE-01
  - MODE-03

must_haves:
  truths:
    - "Uživatel může v Settings přepnout Automatic Save/Manual Save přes draft flow Save/Cancel."
    - "Změna režimu se aplikuje až po Save v Settings (ne při pouhém klikání v draftu)."
    - "Když je otevřený Settings modal, `Ctrl+S` uloží settings draft místo editor souboru."
    - "Aktivní režim je minimálně viditelný za běhu (status bar indikace)."
  artifacts:
    - path: "src/app/ui/workspace/modal_dialogs/settings.rs"
      provides: "UI toggle save režimu + apply po Save"
      contains: "settings-save-mode"
    - path: "src/app/ui/workspace/mod.rs"
      provides: "Runtime indikace aktivního save režimu ve status baru"
      contains: "status_bar"
  key_links:
    - from: "Settings modal Save"
      to: "AppShared.settings"
      via: "draft.try_save -> shared.settings update -> settings_version"
      pattern: "settings_version"
    - from: "workspace Ctrl+S"
      to: "Settings modal Save action"
      via: "modal-specific shortcut branch pro save draftu"
      pattern: "Ctrl+S"
---

<objective>
Doručit UI přepínač save režimu v Settings a okamžitý runtime apply po kliknutí `Save`,
včetně základní viditelnosti aktivního režimu v hlavním UI.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-save-mode-foundation/24-CONTEXT.md
@.planning/phases/24-save-mode-foundation/24-RESEARCH.md
@.planning/phases/24-save-mode-foundation/24-VALIDATION.md
@./CLAUDE.md
@src/app/ui/workspace/modal_dialogs/settings.rs
@src/app/ui/workspace/mod.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Settings UI toggle pro Automatic/Manual save</name>
  <files>src/app/ui/workspace/modal_dialogs/settings.rs</files>
  <action>
    1. Přidej do Settings (Editor sekce) radio volby `Automatic Save` / `Manual Save` navázané na `draft.save_mode`.
    2. Zachovej existující draft semantiku: změna se runtime persistuje/aplikuje až při `Save`.
    3. Po úspěšném `try_save` přidej informační toast o změně režimu pouze pokud se režim skutečně změnil.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>
    Přepínač save režimu je v Settings funkční a respektuje Save/Cancel lifecycle.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Modal-specific Ctrl+S ukládá settings draft</name>
  <files>src/app/ui/workspace/mod.rs, src/app/ui/workspace/modal_dialogs/settings.rs</files>
  <action>
    1. Přidej explicitní větev pro `Ctrl+S`, když je otevřený Settings modal: shortcut musí uložit settings draft (ekvivalent kliknutí na modal `Save`).
    2. Zajisti, že v tomto modal kontextu se nespouští file-save aktivního editor tabu.
    3. Zachovej existující chování mimo modal (Ctrl+S dál ukládá aktivní tab).
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>
    Při otevřeném Settings modalu `Ctrl+S` uloží draft settings a nezasáhne editor file-save flow.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lokalizace a runtime indikace aktivního režimu</name>
  <files>src/app/ui/workspace/mod.rs, locales/cs/ui.ftl, locales/en/ui.ftl, locales/de/ui.ftl, locales/ru/ui.ftl, locales/sk/ui.ftl</files>
  <action>
    1. Přidej i18n klíče pro labely save režimu a krátkou status indikaci.
    2. V renderu status baru zobraz aktivní save režim (`Automatic`/`Manual`) načtený z `shared.settings`.
    3. Ověř, že nové i18n klíče existují ve všech podporovaných jazycích.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>
    Aktivní save režim je v UI viditelný a i18n klíče jsou konzistentní napříč jazyky.
  </done>
</task>

</tasks>

<verification>
cargo check
./check.sh
</verification>

<success_criteria>
- Settings obsahuje přepínač `Automatic Save` / `Manual Save`
- Režim se mění až po `Save` v modalu
- `Ctrl+S` v otevřeném Settings modalu ukládá settings draft
- Aktivní režim je runtime viditelný ve status baru
- Nové i18n klíče jsou ve všech jazycích
</success_criteria>
