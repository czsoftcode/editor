---
phase: 09-core-sandbox-logic-settings-removal
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/app/mod.rs
  - src/app/types.rs
  - src/app/ui/workspace/state/mod.rs
  - src/app/ui/workspace/state/init.rs
  - src/app/ui/workspace/mod.rs
  - src/app/ui/workspace/modal_dialogs.rs
  - src/app/ui/workspace/modal_dialogs/settings.rs
  - src/app/ui/background.rs
  - src/app/ui/panels.rs
  - src/app/ui/terminal/right/mod.rs
autonomous: true
requirements: [CORE-02]

must_haves:
  truths:
    - "Struktury Sandbox, SyncPlan, PendingSettingsSave, SandboxApplyRequest, SandboxPersistFailure, TabRemapRequest neexistuji"
    - "WorkspaceState neobsahuje zadne sandbox-related fieldy (~18 poli odstraneno)"
    - "AppShared neobsahuje sandbox_off_toast_shown"
    - "ToastActionKind enum a ToastAction struct jsou odstraneny (vsech 6 variant bylo sandbox-only)"
    - "Projekt se kompiluje pres cargo check (warnings povoleny)"
  artifacts:
    - path: "src/app/types.rs"
      provides: "Toast bez ToastActionKind/ToastAction"
    - path: "src/app/ui/workspace/state/mod.rs"
      provides: "WorkspaceState bez sandbox fieldu a sandbox metod"
    - path: "src/app/mod.rs"
      provides: "EditorApp bez sandbox_root a sandbox mode change detection"
  key_links:
    - from: "src/app/ui/workspace/state/init.rs"
      to: "src/app/ui/workspace/state/mod.rs"
      via: "WorkspaceState constructor bez sandbox fieldu"
      pattern: "WorkspaceState \\{"
    - from: "src/app/mod.rs"
      to: "src/app/ui/workspace/mod.rs"
      via: "render_workspace bez sandbox parametru"
      pattern: "render_workspace"
---

<objective>
Odstranit vsechny sandbox struktury, fieldy a metody z celeho codebase (krome modal_dialogs/sandbox.rs ktery se ponechava pro Phase 10). Opravit vsechny kaskadove kompilacni chyby. Projekt se musi zkompilovat pres `cargo check`.

Purpose: Dokoncit CORE-02 — zadne sandbox datove struktury v kodu. Editor se kompiluje a je pripraven pro Phase 10 (UI cleanup).
Output: Cista kompilace, vsechny sandbox struktury/fieldy/metody odstraneny.
</objective>

<execution_context>
@/home/stkremen/.claude/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-core-sandbox-logic-settings-removal/09-CONTEXT.md
@.planning/phases/09-core-sandbox-logic-settings-removal/09-RESEARCH.md
@.planning/phases/09-core-sandbox-logic-settings-removal/09-01-SUMMARY.md

@src/app/mod.rs
@src/app/types.rs
@src/app/ui/workspace/state/mod.rs
@src/app/ui/workspace/state/init.rs
@src/app/ui/workspace/mod.rs
@src/app/ui/workspace/modal_dialogs.rs
@src/app/ui/workspace/modal_dialogs/settings.rs
@src/app/ui/background.rs
@src/app/ui/panels.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Odstranit sandbox struktury z types.rs a state/mod.rs</name>
  <files>src/app/types.rs, src/app/ui/workspace/state/mod.rs</files>
  <action>
1. **src/app/types.rs** — odstranit:
   - `ToastActionKind` enum (řádky 200-209) — všech 6 variant je sandbox-only
   - `ToastAction` struct (řádky 211-221) — závisí na ToastActionKind
   - `Toast::info_with_actions()` metodu (řádky 242-249) — používá ToastAction
   - Z `Toast` struct: odstranit field `actions: Vec<ToastAction>` (řádek 197)
   - V `Toast::error()` a `Toast::info()`: odstranit `actions: Vec::new()` inicializaci
   - V `Toast::is_expired()`: zjednodušit na fixní 4s lifetime (odstranit actions.is_empty check)
   - Odstranit `sandbox_off_toast_shown: bool` z `AppShared` (řádek 182)
   - Odstranit `#[allow(clippy::enum_variant_names)]` nad smazaným enumem

2. **src/app/ui/workspace/state/mod.rs** — odstranit:
   - Struktury: `PendingSettingsSave`, `SandboxApplyRequest`, `SandboxPersistFailure`, `TabRemapRequest`
   - Vsech ~18 sandbox fieldu z `WorkspaceState` struct (viz RESEARCH.md Example 2 pro kompletni seznam):
     pending_settings_save, pending_sandbox_apply, sandbox_persist_failure, sandbox_persist_decision, pending_tab_remap, sandbox_deletion_sync, promotion_success, show_sandbox_staged, sync_confirmation, sandbox_sync_confirmation, sandbox_sync_rx, sandbox_mode_enabled, build_in_sandbox, file_tree_in_sandbox, sandbox, sandbox_staged_files, sandbox_staged_rx, sandbox_staged_dirty, sandbox_staged_last_dirty, sandbox_staged_last_refresh
   - Metody: `apply_sandbox_mode_change()`, `should_apply_sandbox_request()`
   - Sandbox-related testy v state/mod.rs (should_apply_sandbox_request testy)
   - Nepotřebné importy (mpsc, PathBuf pokud nikde jinde nepoužité — ověřit)

POZOR: Nemazat fieldy které NEJSOU sandbox-related. WorkspaceState má 80+ fieldů — pečlivě odstranit jen ty se sandbox v názvu nebo typu.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor && ! grep -q 'ToastActionKind' src/app/types.rs && echo "ToastActionKind removed OK" && ! grep -q 'sandbox_mode_enabled' src/app/ui/workspace/state/mod.rs && echo "sandbox fields removed OK" && ! grep -q 'PendingSettingsSave' src/app/ui/workspace/state/mod.rs && echo "sandbox structs removed OK"</automated>
  </verify>
  <done>
    - ToastActionKind, ToastAction, info_with_actions odstraněny z types.rs
    - Toast zjednodušen (bez actions field)
    - sandbox_off_toast_shown odstraněno z AppShared
    - Všechny sandbox struktury a ~18 fieldů odstraněny z state/mod.rs
    - Sandbox metody odstraněny ze state/mod.rs
  </done>
</task>

<task type="auto">
  <name>Task 2: Odstranit sandbox logiku z app/mod.rs, workspace, background a opravit kompilaci</name>
  <files>src/app/mod.rs, src/app/ui/workspace/mod.rs, src/app/ui/workspace/state/init.rs, src/app/ui/workspace/modal_dialogs.rs, src/app/ui/workspace/modal_dialogs/settings.rs, src/app/ui/background.rs, src/app/ui/panels.rs, src/app/ui/terminal/right/mod.rs</files>
  <action>
1. **src/app/mod.rs** — odstranit:
   - sandbox_root inicializaci (řádky 122-127): `let sandbox_root = ...` a předání do Registry/WorkspaceState
   - Registry::new() — pokud přijímá sandbox_root parametr, změnit na project root nebo odstranit parametr (prozkoumat src/app/registry/mod.rs)
   - Sandbox mode change detection bloky (řádky 566-590, 757-780): celé if-bloky pro `sandbox_mode_enabled != settings_snapshot.sandbox_mode`
   - Všechny reference na `sandbox` v update() a jiných metodách
   - Nepoužívané importy po odstranění

2. **src/app/ui/workspace/state/init.rs** — odstranit:
   - Sandbox instance vytváření (řádky 22-34)
   - staged files inicializaci
   - sandbox_mode_enabled init
   - Všech sandbox fieldů z WorkspaceState konstruktoru (musí odpovídat fieldům smazaným v Task 1)
   - spawn_semantic_indexer: změnit `thread_sandbox_root` na `thread_root` (project root) per RESEARCH.md Pitfall 6

3. **src/app/ui/workspace/mod.rs** — odstranit:
   - `spawn_get_staged_files()` funkci
   - `process_pending_sandbox_apply()` funkci
   - `trigger_sandbox_staged_refresh`, `render_sandbox_staged_bar`, `render_sandbox_deletion_sync_dialog` a další sandbox funkce
   - Veškeré sandbox reference v render_workspace a dalších public funkcích
   - Toast action handling pro sandbox varianty (match arms na ToastActionKind)

4. **src/app/ui/workspace/modal_dialogs.rs** — deaktivovat sandbox modul:
   - Řádek 12: `mod sandbox;` — zakomentovat nebo obalit `#[cfg(never)]`
   - Přidat `// TODO: Phase 10 — remove modal_dialogs/sandbox.rs entirely`
   - Soubor `modal_dialogs/sandbox.rs` samotný NEMAZAT (per user decision: ponechat pro Phase 10)

5. **src/app/ui/workspace/modal_dialogs/settings.rs** — odstranit:
   - Sandbox helper funkce pokud jsou zde: `sandbox_mode_change()`, `requires_sandbox_off_confirm()`, `should_block_sandbox_off_due_to_staged()`, `should_block_sandbox_apply()`, `show_sandbox_off_confirm()`, `SandboxModeChange` enum
   - Sandbox-related testy (6 testů, řádky 732-826)
   - Reference na smazané ToastActionKind/ToastAction — všechny toast.actions a info_with_actions volání nahradit prostým Toast::info()
   - Reference na smazané sandbox fieldy z WorkspaceState

6. **src/app/ui/background.rs** — odstranit:
   - Staged files refresh logiku
   - Sandbox sync background tasks
   - Všechny reference na sandbox fieldy z WorkspaceState

7. **src/app/ui/panels.rs** — odstranit:
   - Sandbox reference v panel layout logice
   - Toast action rendering pro ToastActionKind (match na action.kind pro sandbox varianty)

8. **src/app/ui/terminal/right/mod.rs** — odstranit:
   - Claude panel sandbox references (6 výskytů)

9. **Iterativní oprava kompilace:**
   - Po každé větší skupině změn spustit `cargo check 2>&1 | head -50`
   - Opravovat chyby shora dolů — kompilátor ukazuje na další removal target
   - Cíl: `cargo check` projde (warnings OK, errors NE)
   - Poté: `cargo test --lib 2>&1` — ověřit že existující testy procházejí

DŮLEŽITÉ:
- modal_dialogs/sandbox.rs NEMAZAT — pouze deaktivovat mod deklaraci
- UI prvky v build_bar.rs, file_tree/render.rs NEMAZAT — patří do Phase 10
- Kde UI kód referencuje smazané sandbox fieldy, nahradit hardcoded `false` nebo odstranit celý blok pokud je čistě sandbox-only
- Pokud ToastAction/ToastActionKind je referencován v panels.rs pro rendering toast actions, zjednodušit toast rendering (Toast už nemá actions field)
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor && cargo check 2>&1 | tail -5 && ! grep -rq 'struct Sandbox' src/ && echo "No Sandbox struct OK" && ! grep -rq 'struct SyncPlan' src/ && echo "No SyncPlan struct OK" && cargo test --lib 2>&1 | tail -10</automated>
  </verify>
  <done>
    - cargo check projde bez errors (warnings povoleny)
    - Žádné struct Sandbox/SyncPlan v src/
    - sandbox_root odstraněn z app/mod.rs
    - modal_dialogs/sandbox.rs deaktivován (mod zakomentován), soubor ponechán
    - Všechny sandbox metody odstraněny z workspace/mod.rs
    - background.rs a panels.rs očištěny od sandbox logiky
    - cargo test --lib projde
  </done>
</task>

</tasks>

<verification>
- `cargo check` — kompilace bez errors
- `cargo test --lib` — všechny testy procházejí
- `! grep -r 'struct Sandbox' src/` — žádná Sandbox struktura
- `! grep -r 'struct SyncPlan' src/` — žádná SyncPlan struktura
- `! grep -r 'SandboxApplyRequest\|SandboxPersistFailure\|PendingSettingsSave\|TabRemapRequest' src/app/ui/workspace/state/mod.rs` — žádné sandbox struktury ve state
- `! grep -r 'ToastActionKind' src/` — enum odstraněn
- `test ! -f src/app/sandbox.rs` — soubor neexistuje (z Plan 01)
</verification>

<success_criteria>
Projekt se kompiluje (cargo check OK), všechny sandbox struktury/fieldy/metody odstraněny, existující testy procházejí, modal_dialogs/sandbox.rs ponechán ale deaktivován.
</success_criteria>

<output>
After completion, create `.planning/phases/09-core-sandbox-logic-settings-removal/09-02-SUMMARY.md`
</output>
