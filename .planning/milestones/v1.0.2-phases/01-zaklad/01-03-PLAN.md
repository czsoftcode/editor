---
phase: 01-zaklad
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/ui/terminal/window.rs
autonomous: true
gap_closure: true
requirements: [UI-01]

must_haves:
  truths:
    - "Floating terminal window frame nepoužívá hardcoded tmavou výplň; fill je odvozený z aktivních `egui::Visuals`."
    - "Při runtime přepnutí dark/light se frame floating terminálu přebarví konzistentně s tématem bez restartu."
    - "Build i AI floating terminál (přes `StandardTerminalWindow`) sdílí stejný theme-aware frame behavior."
  artifacts:
    - path: "src/app/ui/terminal/window.rs"
      provides: "Theme-aware frame fill pro StandardTerminalWindow místo fixního `Color32::from_rgb(20, 20, 25)`."
      exports: ["StandardTerminalWindow::show"]
      contains: "ui\\.visuals\\(\\)|ctx\\.style\\(\\)\\.visuals"
  key_links:
    - from: "StandardTerminalWindow::show()"
      to: "egui::Frame::window(...)"
      via: "fill barva odvozená z aktivních visuals"
      pattern: "Frame::window\\(&ctx\\.style\\(\\)\\)\\.fill\\("
---

<objective>
Uzavřít UAT gap #2: floating terminal rám zůstává čistě černý v light mode.

Purpose: Odstranit hardcoded tmavý frame u standardního terminálového okna a navázat barvu rámu na aktivní téma.
Output: `src/app/ui/terminal/window.rs` s theme-aware frame fill, který funguje pro oba floating terminály.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-zaklad/01-UAT.md
@.planning/debug/terminal-frame-remains-pure-black-in-light-mode.md
@src/app/ui/terminal/window.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nahradit hardcoded frame fill theme-aware variantou</name>
  <files>src/app/ui/terminal/window.rs</files>
  <action>
    Ve `StandardTerminalWindow::show()` odstraň fixní `viewer_bg = Color32::from_rgb(20, 20, 25)` a odvoď frame fill z aktivních visuals (`ctx.style()` / `ui.visuals()`), aby light mode nepřebíral tmavý hardcode.

    Implementace má být minimální:
    - zachovej stávající `egui::Window` konfiguraci (size/min/max/resizable/open),
    - změň pouze způsob výběru fill barvy pro `.frame(...)`,
    - nepřidávej žádný restart terminálu ani další side efekty.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor/.polycredo/sandbox && cargo check 2>&1 | tail -30</automated>
  </verify>
  <done>
    `cargo check` je zelený a `src/app/ui/terminal/window.rs` už neobsahuje `Color32::from_rgb(20, 20, 25)` pro frame fill.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ověřit wiring pro oba floating terminály</name>
  <files>src/app/ui/terminal/window.rs</files>
  <action>
    Zkontroluj, že změna zůstává v `StandardTerminalWindow`, aby fix platil jak pro AI floating terminál, tak pro build floating terminál bez duplikace.

    Přidej krátký komentář u nové theme-aware fill logiky, proč nesmí být hardcoded tmavá barva.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor/.polycredo/sandbox && rg -n \"Color32::from_rgb\\(20, 20, 25\\)|Frame::window\\(&ctx\\.style\\(\\)\\)\\.fill\" src/app/ui/terminal/window.rs</automated>
  </verify>
  <done>
    Frame fill je theme-aware, fix je centralizovaný v `StandardTerminalWindow` a bez regresí v kompilaci.
  </done>
</task>

</tasks>

<verification>
- `cargo check 2>&1` bez chyb
- `rg -n "Color32::from_rgb\\(20, 20, 25\\)" src/app/ui/terminal/window.rs` nevrátí hardcoded frame fill
- `rg -n "Frame::window\\(&ctx\\.style\\(\\)\\)\\.fill" src/app/ui/terminal/window.rs` vrátí theme-aware frame fill
</verification>

<success_criteria>
- Floating terminál frame v light mode není čistě černý
- Přepnutí dark/light za běhu mění frame barvu konzistentně s tématem
- Změna je omezena na `src/app/ui/terminal/window.rs`
- `cargo check` prochází
</success_criteria>

<output>
Po dokončení vytvoř `.planning/phases/01-zaklad/01-03-SUMMARY.md`
</output>
