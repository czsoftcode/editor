---
phase: 28-dark-variant-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/settings.rs
  - src/app/ui/workspace/modal_dialogs/settings.rs
  - locales/cs/ui.ftl
  - locales/en/ui.ftl
  - locales/sk/ui.ftl
  - locales/de/ui.ftl
  - locales/ru/ui.ftl
  - src/app/ui/git_status.rs
  - src/app/ui/terminal/instance/theme.rs
autonomous: true
requirements: [THEME-05, THEME-06, THEME-07]

must_haves:
  truths:
    - "User can see 2nd dark variant (Midnight) option in Settings → Theme picker UI"
    - "User can select Midnight variant and see immediate visual change (different panel colors)"
    - "Settings persist across app restart (dark_variant stored in settings.toml)"
    - "Midnight variant shows visual swatch in theme picker"
    - "All i18n keys exist in all 5 languages"
  artifacts:
    - path: "src/settings.rs"
      provides: "DarkVariant enum with Default and Midnight variants"
      contains: "pub enum DarkVariant"
    - path: "src/app/ui/workspace/modal_dialogs/settings.rs"
      provides: "Dark variant picker UI in settings modal"
      contains: "show_dark_variant_card"
    - path: "locales/{cs,en,sk,de,ru}/ui.ftl"
      provides: "i18n translations for dark variants"
      contains: "settings-dark-variant"
  key_links:
    - from: "Settings.dark_variant"
      to: "to_egui_visuals()"
      via: "match self.dark_variant"
      pattern: "DarkVariant::Midnight"
    - from: "Settings.dark_variant"
      to: "settings.toml"
      via: "serde serialization"
      pattern: "serde.*DarkVariant"
---

<objective>
Přidat 2. dark theme variantu (Midnight) do aplikace — podobný pattern jako LightVariant z Phase 27.

Purpose: Uživatel může vybrat mezi dvěma dark variantami (Default a Midnight).
Output: DarkVariant enum, UI picker, i18n překlady, persistované nastavení.
</objective>

<execution_context>
@/home/stkremen/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-4th-light-theme/27-01-SUMMARY.md
@src/settings.rs (LightVariant pattern lines 61-69, to_egui_visuals lines 303-334)
@src/app/ui/workspace/modal_dialogs/settings.rs (light variant picker lines 373-389)
@locales/en/ui.ftl (i18n key pattern lines 214-218)

Key patterns from Phase 27:
- LightVariant enum: lines 61-69 in settings.rs
- to_egui_visuals() match: lines 309-330
- UI picker: show_light_variant_card function pattern
- i18n keys: settings-light-variant-{name}
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DarkVariant enum and update Settings struct</name>
  <files>src/settings.rs</files>
  <action>
    1. Add DarkVariant enum BEFORE SaveMode (after LightVariant):
       ```
       #[derive(serde::Serialize, serde::Deserialize, Clone, PartialEq, Debug, Default)]
       #[serde(rename_all = "snake_case")]
       pub enum DarkVariant {
           #[default]
           Default,
           Midnight,
       }
       ```

    2. Replace `dark_theme: bool` field (line 90-91) with `dark_variant: DarkVariant`:
       ```
       /// Dark mode color variant.
       #[serde(default)]
       pub dark_variant: DarkVariant,
       ```

    3. Update default_dark_theme() to return DarkVariant::Default instead of bool (line 15-17).

    4. Add to_egui_visuals() match arm for dark variants (modify lines 304-305):
       Replace:
       ```
       if self.dark_theme {
           eframe::egui::Visuals::dark()
       ```
       With:
       ```
       match self.dark_variant {
           DarkVariant::Default => eframe::egui::Visuals::dark(),
           DarkVariant::Midnight => {
               let mut::egui::Vis visuals = eframeuals::dark();
               // Dark blue-gray tones for Midnight
               visuals.panel_fill = eframe::egui::Color32::from_rgb(30, 35, 45);
               visuals.window_fill = eframe::egui::Color32::from_rgb(25, 28, 38);
               visuals.faint_bg_color = eframe::egui::Color32::from_rgb(20, 22, 32);
               visuals
           }
       }
       ```

    5. Update syntect_theme_name() to check dark_variant (lines 293-299).

    6. Update any other places using `self.dark_theme` bool to use `self.dark_variant != DarkVariant::Default`.
  </action>
  <verify>
    <automated>cargo check 2>&1 | head -30</automated>
  </verify>
  <done>
    DarkVariant enum exists with Default and Midnight variants.
    Settings.dark_variant field replaces dark_theme bool.
    to_egui_visuals() returns different colors for Midnight variant.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add dark variant picker to settings UI</name>
  <files>src/app/ui/workspace/modal_dialogs/settings.rs</files>
  <action>
    1. Import DarkVariant at top of file (if not already imported via crate::settings).

    2. Update theme_fingerprint function (line 87-89) to return DarkVariant instead of bool:
       ```
       fn theme_fingerprint(settings: &crate::settings::Settings) -> (DarkVariant, LightVariant) {
           (settings.dark_variant.clone(), settings.light_variant.clone())
       }
       ```

    3. Replace dark_theme radio buttons (lines 356-370) with dark variant picker:
       - When dark is selected, show dark variant options (Default, Midnight)
       - Use similar card UI pattern as light variant picker

       Replace the dark_theme radio section with:
       ```
       ui.strong(i18n.get("settings-theme"));
       let theme_before = theme_fingerprint(draft);
       let mut theme_controls_changed = false;
       ui.horizontal(|ui| {
           // Dark/Light toggle (radio buttons)
           let is_dark = draft.dark_variant != DarkVariant::Default;
           theme_controls_changed |= ui
               .radio_value(&mut draft.dark_variant, DarkVariant::Default, i18n.get("settings-theme-dark"))
               .changed();
           theme_controls_changed |= ui
               .radio_value(&mut draft.dark_variant, LightVariant::WarmIvory.try_into().unwrap_or(DarkVariant::Default), i18n.get("settings-theme-light"))
               .changed();
       });
       ui.add_space(16.0);

       // Show variant picker based on mode
       let is_dark = matches!(draft.dark_variant, DarkVariant::Default | DarkVariant::Midnight);
       if is_dark {
           ui.strong(i18n.get("settings-dark-variant"));
           ui.add_space(6.0);
           ui.horizontal_wrapped(|ui| {
               for variant in [DarkVariant::Default, DarkVariant::Midnight] {
                   theme_controls_changed |= show_dark_variant_card(ui, draft, i18n, variant);
                   ui.add_space(8.0);
               }
           });
           ui.add_space(16.0);
       } else {
           // Light variant picker (existing code)
           ...
       }
       ```

    4. Add show_dark_variant_card function similar to show_light_variant_card:
       - Shows swatch (dark colors)
       - Uses i18n key: settings-dark-variant-{name}
       - Returns true if changed
  </action>
  <verify>
    <automated>cargo check 2>&1 | head -30</automated>
  </verify>
  <done>
    Settings UI shows dark variant picker when dark mode is selected.
    Both Default and Midnight options are visible and selectable.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add i18n translations for dark variants</name>
  <files>locales/cs/ui.ftl, locales/en/ui.ftl, locales/sk/ui.ftl, locales/de/ui.ftl, locales/ru/ui.ftl</files>
  <action>
    Add to all 5 locale files after settings-light-variant-warm-tan:

    For cs/ui.ftl:
    ```
    settings-dark-variant = Tmavá varianta
    settings-dark-variant-default = Výchozí
    settings-dark-variant-midnight = Půlnoc
    ```

    For en/ui.ftl:
    ```
    settings-dark-variant = Dark variant
    settings-dark-variant-default = Default
    settings-dark-variant-midnight = Midnight
    ```

    For sk/ui.ftl:
    ```
    settings-dark-variant = Tmavá varianta
    settings-dark-variant-default = Predvolené
    settings-dark-variant-midnight = Polnočná
    ```

    For de/ui.ftl:
    ```
    settings-dark-variant = Dunkle Variante
    settings-dark-variant-default = Standard
    settings-dark-variant-midnight = Mitternacht
    ```

    For ru/ui.ftl:
    ```
    settings-dark-variant = Тёмный вариант
    settings-dark-variant-default = По умолчанию
    settings-dark-variant-midnight = Полночь
    ```
  </action>
  <verify>
    <automated>cargo check 2>&1 | head -30</automated>
  </verify>
  <done>
    All 5 locales contain dark variant translations.
    UI displays localized dark variant names.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update test arrays</name>
  <files>src/app/ui/git_status.rs, src/app/ui/terminal/instance/theme.rs</files>
  <action>
    Update test arrays that iterate over theme variants to include the new dark variant:

    In git_status.rs (line ~75):
    - Update any test that uses LightVariant::iter_all() or similar to include 4 light + 2 dark variants

    In terminal/instance/theme.rs (line ~186):
    - Update test array to include 2 dark variants instead of just checking bool
  </action>
  <verify>
    <automated>cargo test 2>&1 | tail -20</automated>
  </verify>
  <done>
    All tests pass with new dark variant.
  </done>
</task>

</tasks>

<verification>
1. Run `cargo check` to verify no compilation errors
2. Run `cargo test` to verify tests pass
3. Run `./check.sh` if available
</verification>

<success_criteria>
- User can see 2nd dark variant (Midnight) option in Settings → Theme picker UI
- User can select Midnight variant and see immediate visual change (different panel colors)
- Settings persist across app restart (dark_variant stored in settings.toml)
- Midnight variant shows visual swatch in theme picker
- All i18n keys exist in all 5 languages

Measurable:
- THEME-05: ✓ Settings UI contains Midnight option
- THEME-06: ✓ dark_variant persisted in settings.toml
- THEME-07: ✓ Swatch renders in picker UI
</success_criteria>

<output>
After completion, create `.planning/phases/28-dark-variant-support/28-01-SUMMARY.md`
</output>
