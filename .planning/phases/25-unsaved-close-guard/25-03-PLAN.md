---
phase: 25-unsaved-close-guard
plan: 03
type: execute
wave: 3
depends_on:
  - 25-01
  - 25-02
files_modified:
  - src/app/ui/workspace/mod.rs
  - src/app/ui/dialogs/confirm.rs
  - src/app/ui/widgets/modal.rs
autonomous: true
requirements:
  - GUARD-03
  - GUARD-04

must_haves:
  truths:
    - "Guard dialog vždy nabízí akce `Save`, `Discard`, `Cancel` a default/focus je `Cancel`."
    - "`Esc` i zavření dialogu přes `X` mají stejnou semantiku jako `Cancel`."
    - "Po selhání `Save` se zobrazí inline chyba i toast a flow zůstane na stejném tabu."
    - "Po save failu je stále dostupné `Discard` i `Cancel` bez opuštění close flow."
  artifacts:
    - path: "src/app/ui/workspace/mod.rs"
      provides: "Reducer rozhodnutí guard dialogu + retry save větve"
      contains: "apply_unsaved_close_decision"
  key_links:
    - from: "Guard dialog Save"
      to: "Editor save_path"
      via: "save pro current_path + handling resultu"
      pattern: "save_path"
---

<objective>
Dodat rozhodovací dialog s bezpečným defaultem `Cancel` a robustní větví selhání ukládání,
která nikdy nedovolí tiché zavření bez informovaného rozhodnutí uživatele.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-unsaved-close-guard/25-CONTEXT.md
@.planning/phases/25-unsaved-close-guard/25-RESEARCH.md
@.planning/phases/25-unsaved-close-guard/25-VALIDATION.md
@./CLAUDE.md
@src/app/ui/dialogs/confirm.rs
@src/app/ui/widgets/modal.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard modal UI + rozhodovací větve</name>
  <files>src/app/ui/workspace/mod.rs, src/app/ui/dialogs/confirm.rs, src/app/ui/widgets/modal.rs</files>
  <action>
    1. Přidej reusable unsaved-close guard dialog s informacemi o souboru (název + cesta + dirty stav).
    2. Zajisti akce `Save`, `Discard`, `Cancel` a safe default fokus na `Cancel`.
    3. Implementuj `Esc` a `X` jako `Cancel` a potvrď, že `Cancel` ruší celý aktuální close flow.
  </action>
  <verify>
    <automated>cargo test unsaved_close_guard_modal_actions -- --nocapture</automated>
  </verify>
  <done>
    Dialog a rozhodovací logika jsou konzistentní s rozhodnutími fáze.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Save fail handling v close flow</name>
  <files>src/app/ui/workspace/mod.rs</files>
  <action>
    1. Při selhání `Save` v close flow nastav inline error v dialogu a současně emituj error toast.
    2. Neuzavírej tab ani aplikaci po save failu; ponech stejné `current_path` a umožni retry.
    3. Připrav jasné integrační body pro následné doplnění i18n bez změny chování flow.
  </action>
  <verify>
    <automated>cargo test unsaved_close_guard_save_fail -- --nocapture</automated>
  </verify>
  <done>
    Save fail větev je transparentní, bezpečná a připravená na následné i18n doplnění.
  </done>
</task>

</tasks>

<verification>
cargo check
cargo test unsaved_close_guard_modal_actions -- --nocapture
cargo test unsaved_close_guard_save_fail -- --nocapture
</verification>

<success_criteria>
- Dialog obsahuje `Save`/`Discard`/`Cancel` a `Cancel` je safe default
- `Esc` a `X` jsou ekvivalent `Cancel`
- Save fail během close zobrazí inline chybu + toast
- Po save failu flow zůstává otevřený na stejném tabu
</success_criteria>
