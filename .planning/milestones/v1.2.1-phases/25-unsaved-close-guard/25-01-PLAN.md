---
phase: 25-unsaved-close-guard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/ui/workspace/state/mod.rs
  - src/app/ui/workspace/state/init.rs
  - src/app/ui/workspace/mod.rs
autonomous: true
requirements:
  - GUARD-01
  - GUARD-03

must_haves:
  truths:
    - "Opakované kliknutí na zavření během otevřeného guard dialogu nespustí další paralelní dialog ani duplicitní close akci."
    - "Při více neuložených tabech se vždy nejdřív řeší aktuálně aktivní tab a zbytek pokračuje ve stabilním pořadí."
    - "Zavírání správně cílí na původně vybraný soubor i pokud se mezitím změní pořadí tabů v UI."
  artifacts:
    - path: "src/app/ui/workspace/state/mod.rs"
      provides: "Datový model close guard flow (mode, queue, current path, inline error)"
      contains: "PendingCloseFlow"
    - path: "src/app/ui/workspace/mod.rs"
      provides: "Guard-aware vstupní API pro start tab close flow"
      contains: "request_close_active_tab"
  key_links:
    - from: "workspace close trigger"
      to: "PendingCloseFlow queue"
      via: "build_dirty_close_queue(active_first)"
      pattern: "PathBuf"
---

<objective>
Zavést minimální a bezpečný stavový základ unsaved close guardu na úrovni workspace,
aby všechny další close triggery používaly jednu orchestraci bez přímého obcházení guardu.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-unsaved-close-guard/25-CONTEXT.md
@.planning/phases/25-unsaved-close-guard/25-RESEARCH.md
@.planning/phases/25-unsaved-close-guard/25-VALIDATION.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PendingCloseFlow model + queue builder</name>
  <files>src/app/ui/workspace/state/mod.rs, src/app/ui/workspace/state/init.rs</files>
  <action>
    1. Přidej stavovou strukturu pro unsaved close flow (`SingleTab`, `WorkspaceClose`) se snapshot queue nad `PathBuf`.
    2. Přidej helper pro sestavení fronty dirty tabů s pravidlem active-first + deterministický zbytek.
    3. Inicializuj nový stav v `WorkspaceState::new` bez změny existující architektury.
  </action>
  <verify>
    <automated>cargo test unsaved_close_guard_queue -- --nocapture</automated>
  </verify>
  <done>
    Guard flow má stabilní interní model a binárně ověřitelný queue order.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Workspace guard-aware close request API</name>
  <files>src/app/ui/workspace/mod.rs</files>
  <action>
    1. Přidej jednotné API pro start close flow aktivního tabu a explicitní guard proti re-entrancy.
    2. Pokud není žádný dirty tab, close proběhne okamžitě bez dialogu.
    3. Pokud je guard aktivní, nové close requesty ignoruj do dokončení aktuálního flow.
  </action>
  <verify>
    <automated>cargo test unsaved_close_guard_queue -- --nocapture</automated>
  </verify>
  <done>
    Workspace má jednotný, guard-aware vstupní bod pro zavírání tabu.
  </done>
</task>

</tasks>

<verification>
cargo check
cargo test unsaved_close_guard_queue -- --nocapture
</verification>

<success_criteria>
- Existuje `PendingCloseFlow` stav s queue nad `PathBuf`
- Active-first pořadí dirty tabů je deterministické a testované
- Workspace close request nepovolí re-entrantní spuštění guardu
</success_criteria>
