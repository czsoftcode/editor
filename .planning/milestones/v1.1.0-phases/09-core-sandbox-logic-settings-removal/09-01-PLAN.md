---
phase: 09-core-sandbox-logic-settings-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/sandbox.rs
  - src/app/mod.rs
  - src/settings.rs
autonomous: true
requirements: [CORE-01, SET-01, SET-02]

must_haves:
  truths:
    - "Soubor src/app/sandbox.rs neexistuje"
    - "mod sandbox deklarace neexistuje v app/mod.rs"
    - "Settings.sandbox_mode field neexistuje v settings.rs"
    - "Settings loading funguje korektne i se starymi settings soubory obsahujicimi sandbox_mode/project_read_only"
    - "Migrace automaticky odstrani sandbox_mode a project_read_only z TOML i JSON souboru"
  artifacts:
    - path: "src/settings.rs"
      provides: "Settings bez sandbox_mode field + migracni funkce"
      contains: "migrate_remove_sandbox_fields"
  key_links:
    - from: "src/settings.rs"
      to: "settings.toml / settings.json"
      via: "migrate_remove_sandbox_fields called during load"
      pattern: "migrate_remove_sandbox_fields"
---

<objective>
Smazat sandbox modul (sandbox.rs) a odstranit sandbox_mode field ze Settings vcetne legacy project_read_only aliasu. Pridat migracni logiku ktera pri loadu automaticky odstrani sandbox pole z existujicich settings souboru.

Purpose: Zaklad pro celou fazi 9 — bez sandbox.rs a sandbox_mode v Settings muze Plan 02 cistit zavisle struktury.
Output: sandbox.rs smazan, Settings ocisteny, migrace funguje, testy aktualizovany.
</objective>

<execution_context>
@/home/stkremen/.claude/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-core-sandbox-logic-settings-removal/09-CONTEXT.md
@.planning/phases/09-core-sandbox-logic-settings-removal/09-RESEARCH.md

@src/app/sandbox.rs
@src/app/mod.rs
@src/settings.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smazat sandbox.rs a odstranit sandbox_mode ze Settings + pridat migraci</name>
  <files>src/app/sandbox.rs, src/app/mod.rs, src/settings.rs</files>
  <action>
1. **Smazat src/app/sandbox.rs** — celý soubor.

2. **src/app/mod.rs** — odstranit řádek `pub mod sandbox;` (řádek 14). Ostatní sandbox reference v mod.rs ZATÍM NEŘEŠIT (sandbox_root inicializace, mode change detection) — to řeší Plan 02.

3. **src/settings.rs** — Settings struct:
   - Odstranit field `sandbox_mode: bool` (řádek 128-129) včetně `#[serde(alias = "project_read_only")]` atributu
   - Odstranit `sandbox_mode: false` z `impl Default for Settings` (řádek 155)
   - Přidat `#[serde(default)]` dekoraci na Settings struct pokud ještě nemá, aby deserializace starých souborů se sandbox_mode nevyhazovala chybu (serde silently ignores unknown fields s `#[serde(deny_unknown_fields)]` NENI použito — ověřit)

4. **src/settings.rs** — přidat migrační funkci (per user decision: explicitní migrace při loadu):
   ```rust
   /// One-time migration: strip sandbox_mode and project_read_only from settings files.
   fn migrate_remove_sandbox_fields(path: &std::path::Path) {
       let Ok(content) = std::fs::read_to_string(path) else { return };
       // TOML
       if path.extension().map_or(false, |e| e == "toml") {
           if let Ok(mut value) = content.parse::<toml::Value>() {
               if let Some(table) = value.as_table_mut() {
                   let a = table.remove("sandbox_mode").is_some();
                   let b = table.remove("project_read_only").is_some();
                   if a || b {
                       if let Ok(new_content) = toml::to_string_pretty(&value) {
                           let _ = std::fs::write(path, new_content);
                       }
                   }
               }
           }
       }
       // JSON (legacy)
       if path.extension().map_or(false, |e| e == "json") {
           if let Ok(mut value) = serde_json::from_str::<serde_json::Value>(&content) {
               if let Some(obj) = value.as_object_mut() {
                   let a = obj.remove("sandbox_mode").is_some();
                   let b = obj.remove("project_read_only").is_some();
                   if a || b {
                       if let Ok(new_content) = serde_json::to_string_pretty(&value) {
                           let _ = std::fs::write(path, new_content);
                       }
                   }
               }
           }
       }
   }
   ```
   Volat `migrate_remove_sandbox_fields(&path)` v `Settings::load_from_config_dir` IHNED PO úspěšném načtení souboru (před return). Migrace pro oba formáty (TOML i JSON) v jednom kroku per user decision.

5. **src/settings.rs testy** — aktualizovat:
   - SMAZAT test `test_sett02_canonical_toml_persists_sandbox_mode` (testuje odstraněnou feature)
   - PŘEPSAT test `test_sett05_legacy_project_read_only_maps_to_sandbox_mode` na nový test `test_settings_migration_strips_sandbox_fields` — ověří, že migrace odstraní sandbox_mode z TOML souboru
   - PŘIDAT test `test_settings_migration_strips_project_read_only_from_json` — ověří migraci JSON legacy souboru
   - Smazat sandbox helper funkce pokud existují: `sandbox_mode_change()`, `requires_sandbox_off_confirm()`, `should_block_sandbox_off_due_to_staged()`, `should_block_sandbox_apply()`, `show_sandbox_off_confirm()`, `SandboxModeChange` enum — POZOR: tyto mohou být v `modal_dialogs/settings.rs`, ne v `settings.rs`. Pokud jsou v `modal_dialogs/settings.rs`, NEMAZAT (Phase 10). Pokud jsou v `settings.rs`, smazat.
   - Ověřit že ostatní testy v settings.rs stále procházejí

DŮLEŽITÉ: Po tomto tasku projekt NEBUDE kompilovat — to je očekávané. Sandbox reference v dalších souborech (state, types, workspace) řeší Plan 02. Tento task pouze zajistí, že sandbox.rs neexistuje, Settings jsou čisté a migrace funguje.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor && test ! -f src/app/sandbox.rs && echo "sandbox.rs deleted OK" && ! grep -q 'pub mod sandbox' src/app/mod.rs && echo "mod declaration removed OK" && ! grep -q 'sandbox_mode' src/settings.rs && echo "sandbox_mode field removed OK" && grep -q 'migrate_remove_sandbox_fields' src/settings.rs && echo "migration function exists OK" && cargo test settings::tests -- 2>&1 | tail -20</automated>
  </verify>
  <done>
    - src/app/sandbox.rs neexistuje
    - `pub mod sandbox;` odstraněno z app/mod.rs
    - Settings struct nemá sandbox_mode field
    - migrate_remove_sandbox_fields funkce existuje a je volána při loadu
    - Migrační testy procházejí (TOML i JSON)
    - Staré sandbox testy v settings.rs smazány
  </done>
</task>

</tasks>

<verification>
- `test ! -f src/app/sandbox.rs` — soubor neexistuje
- `! grep -q 'pub mod sandbox' src/app/mod.rs` — mod deklarace odstraněna
- `! grep -q 'sandbox_mode.*bool' src/settings.rs` — field odstraněn
- `grep -q 'migrate_remove_sandbox_fields' src/settings.rs` — migrace přítomna
- `cargo test settings::tests` — settings testy procházejí
</verification>

<success_criteria>
sandbox.rs smazán, Settings bez sandbox_mode, migrace TOML/JSON funguje, settings testy zelené.
</success_criteria>

<output>
After completion, create `.planning/phases/09-core-sandbox-logic-settings-removal/09-01-SUMMARY.md`
</output>
