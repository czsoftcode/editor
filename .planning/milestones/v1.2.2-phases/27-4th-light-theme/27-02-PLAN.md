---
phase: 27-4th-light-theme
plan: 02
type: execute
wave: 2
depends_on:
  - 27-01
files_modified:
  - src/app/ui/workspace/modal_dialogs/settings.rs
  - src/settings.rs
  - src/i18n.rs
autonomous: true
requirements:
  - THEME-01
  - THEME-02
  - THEME-03
  - THEME-04

must_haves:
  truths:
    - "WarmTan je viditelný v Settings pickeru mezi light variantami a je přepínatelný bez skrytých podmínek."
    - "Přepnutí na WarmTan se projeví okamžitě a po restartu zůstane persistováno."
    - "WarmTan má funkční label key + swatch + i18n ve všech 5 jazycích."
  artifacts:
    - path: "src/app/ui/workspace/modal_dialogs/settings.rs"
      provides: "Deterministická iterace light variant v pickeru včetně WarmTan"
      contains: "light variant render loop + label/swatch mapping"
    - path: "src/settings.rs"
      provides: "Persistenční a runtime kontrakt pro WarmTan"
      contains: "LightVariant + serialization/default behavior"
    - path: "src/i18n.rs"
      provides: "Existence a načítání i18n klíče pro WarmTan"
      contains: "lookup/coverage guard"
  key_links:
    - from: "LightVariant::WarmTan"
      to: "Settings picker options"
      via: "render iteration"
      pattern: "settings modal"
    - from: "Settings.light_variant"
      to: "runtime visuals + persisted settings.toml"
      via: "save/apply path"
      pattern: "settings.rs"
---

<objective>
Opravit regresi Phase 27: nové light téma WarmTan není v Settings vidět, takže nejde zvolit.

Purpose: Vrátit THEME-01..04 do funkčního stavu bez redesignu témat.
Output: minimální patch pickeru + persistence guard + regresní testy.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
@/home/stkremen/.codex/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/27-4th-light-theme/27-CONTEXT.md
@.planning/phases/27-4th-light-theme/27-RESEARCH.md
@.planning/phases/27-4th-light-theme/27-VALIDATION.md
@.planning/phases/27-4th-light-theme/27-01-PLAN.md
@src/app/ui/workspace/modal_dialogs/settings.rs
@src/settings.rs

Boundary:
- Scope: regresní oprava viditelnosti/přepnutí WarmTan + test guard.
- Out of scope: nové theme varianty, redesign pickeru, širší refaktor Settings architektury.
</context>

<tasks>

<task id="27-02-01" type="auto">
  <name>Reprodukovat a zafixovat viditelnost WarmTan v Settings pickeru</name>
  <files>src/app/ui/workspace/modal_dialogs/settings.rs</files>
  <action>
    1. Ověřit, že render path pickeru opravdu iteruje přes všechny 4 light varianty včetně `WarmTan`.
    2. Pokud je seznam variant explicitní a nekompletní, doplnit `WarmTan` do stejného seznamu/order jako ostatní light varianty.
    3. Zkontrolovat mapování `light_variant_label_key` a `light_variant_swatch` pro `WarmTan`.
    4. Přidat regresní test, který failne, pokud picker nezahrnuje `WarmTan`.
  </action>
  <verify>
    <automated>cargo test -q settings_light_variant_picker_includes_warmtan</automated>
  </verify>
  <done>
    Settings picker deterministicky obsahuje WarmTan a test guard brání opětovnému vypadnutí volby z UI.
  </done>
</task>

<task id="27-02-02" type="auto">
  <name>Opravit přepnutí a persistence WarmTan end-to-end</name>
  <files>src/settings.rs, src/app/ui/workspace/modal_dialogs/settings.rs</files>
  <action>
    1. Ověřit save/apply path pro `light_variant = warm_tan` a odstranit případný fallback, který hodnotu přepisuje.
    2. Doplnit nebo upravit regresní test, že přepnutí na WarmTan mění runtime visuals ihned po apply.
    3. Doplnit/udržet roundtrip test persistence pro `warm_tan` v `settings.toml`.
  </action>
  <verify>
    <automated>cargo test -q settings_light_variant_switch_to_warmtan</automated>
    <automated>cargo test -q settings_light_variant_warmtan_roundtrip_persistence</automated>
  </verify>
  <done>
    WarmTan je nejen vidět, ale i funkčně přepínatelný a persistovaný napříč restartem.
  </done>
</task>

<task id="27-02-03" type="auto">
  <name>Uzavřít i18n coverage a anti-regression gate pro THEME-04</name>
  <files>src/i18n.rs, src/app/ui/workspace/modal_dialogs/settings.rs</files>
  <action>
    1. Ověřit, že klíč `settings-light-variant-warm-tan` existuje a je načitatelný v cs/en/de/ru/sk.
    2. Přidat lehký regresní test pro label key mapping + locale coverage.
    3. Dodat krátký manuální checklist do summary: viditelnost volby, přepnutí, persistence, lokalizace.
  </action>
  <verify>
    <automated>cargo test -q settings_light_variant_label_warmtan_localized</automated>
    <automated>cargo check</automated>
    <manual>Otevři Settings v light režimu, potvrď že WarmTan je vidět, přepínatelný a s lokalizovaným labelem.</manual>
  </verify>
  <done>
    THEME-04 je ověřený testem i manuální kontrolou, bez regrese v ostatních variantách.
  </done>
</task>

</tasks>

<verification>
- [ ] `cargo test -q settings_light_variant_picker_includes_warmtan` pass
- [ ] `cargo test -q settings_light_variant_switch_to_warmtan` pass
- [ ] `cargo test -q settings_light_variant_warmtan_roundtrip_persistence` pass
- [ ] `cargo test -q settings_light_variant_label_warmtan_localized` pass
- [ ] `cargo check` pass
- [ ] `./check.sh` informational gate (known repo-wide fmt drift is out-of-scope)
</verification>

<success_criteria>
- WarmTan je viditelný v Settings pickeru.
- Uživatel může na WarmTan přepnout a změna se projeví okamžitě.
- WarmTan zůstane po restartu aplikace.
- Lokalizovaný label funguje ve všech 5 jazycích.
</success_criteria>

<output>
After completion, create `.planning/phases/27-4th-light-theme/27-02-SUMMARY.md`.
</output>
