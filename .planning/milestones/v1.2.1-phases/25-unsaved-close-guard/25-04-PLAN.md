---
phase: 25-unsaved-close-guard
plan: 04
type: execute
wave: 4
depends_on:
  - 25-01
  - 25-02
  - 25-03
files_modified:
  - src/app/mod.rs
  - src/app/types.rs
  - src/app/ui/workspace/mod.rs
autonomous: false
requirements:
  - GUARD-02

must_haves:
  truths:
    - "Close aplikace/projektu se při dirty tabs nepokračuje bez dokončení guard flow."
    - "Při více workspacích běží řešení dirty tabů sekvenčně a `Cancel` stopne celou close operaci."
    - "Po úspěšném vyřešení všech queue položek se spustí původní close akce přes existující flow."
  artifacts:
    - path: "src/app/mod.rs"
      provides: "Root orchestrátor global close guard flow pro Quit/Close Project/window close"
      contains: "pending_global_close"
  key_links:
    - from: "Root close triggers (Quit/Close Project/window close)"
      to: "workspace guard queues"
      via: "snapshot dirty tabs napříč workspacy + sekvenční průchod"
      pattern: "GUARD-02"
---

<objective>
Napojit unsaved guard na globální close scénáře (`Quit`, `Close Project`, close viewportu)
a zajistit sekvenční zpracování více workspace front bez obcházení guard pravidel.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-unsaved-close-guard/25-CONTEXT.md
@.planning/phases/25-unsaved-close-guard/25-RESEARCH.md
@.planning/phases/25-unsaved-close-guard/25-VALIDATION.md
@./CLAUDE.md
@src/app/mod.rs
@src/app/ui/workspace/mod.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Root close orchestrace pro GUARD-02</name>
  <files>src/app/mod.rs, src/app/types.rs, src/app/ui/workspace/mod.rs</files>
  <action>
    1. Vlož guard fázi před existující quit/close-project/viewport close potvrzení.
    2. Udělej snapshot dirty tab queue napříč workspacy a řeš je sekvenčně bez držení locků během render/modal kroků.
    3. `Cancel` ukončí celou globální close operaci; při clean stavu pokračuje okamžitě původní flow.
  </action>
  <verify>
    <automated>cargo test unsaved_close_guard_root_flow -- --nocapture</automated>
  </verify>
  <done>
    Globální close flow respektuje guard pravidla a je konzistentní napříč triggery.
  </done>
</task>

</tasks>

<verification>
cargo check
cargo test unsaved_close_guard_root_flow -- --nocapture
</verification>

<success_criteria>
- `Quit`, `Close Project` a close viewportu používají guard flow při dirty tabs
- Multi-workspace close je sekvenční a `Cancel` stopne celou operaci
- Root close orchestrace nemá vedlejší bypass mimo guard flow
</success_criteria>
