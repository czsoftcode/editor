---
phase: 02-terminal-git-barvy
plan: 02
type: tdd
wave: 2
depends_on: [02-01]
files_modified:
  - src/app/ui/background.rs
  - src/app/ui/file_tree/mod.rs
  - src/app/ui/file_tree/render.rs
  - src/app/ui/git_status.rs
  - src/app/ui/workspace/state/mod.rs
autonomous: true
requirements: [TREE-01, TREE-02]

must_haves:
  truths:
    - "Git stavy M/A/??/D mají explicitní dark i light paletu; light mode nepoužívá globální `* 0.55` násobení"
    - "`??` (untracked) je v light mode samostatná modro-azurová barva s dostatečným kontrastem"
    - "Mapování git statusu na semantický typ je testovatelné a sdílené, aby barvy byly stabilní i při rename/copy statu"
    - "Soubory bez git statusu dál používají standardní `ui.visuals().text_color()`"
  artifacts:
    - path: "src/app/ui/git_status.rs"
      provides: "Sdílený status enum + parser + dark/light palette resolver"
      exports: ["GitVisualStatus", "parse_porcelain_status", "git_color_for_mode"]
      contains: "pub enum GitVisualStatus"
    - path: "src/app/ui/background.rs"
      provides: "Napojení `git status --porcelain` na semantické statusy místo ad-hoc barev"
      contains: "parse_porcelain_status"
    - path: "src/app/ui/file_tree/render.rs"
      provides: "Render git barev z explicitní light/dark palety"
      contains: "git_color_for_mode"
  key_links:
    - from: "git porcelain XY"
      to: "GitVisualStatus"
      via: "parse_porcelain_status"
      pattern: "parse_porcelain_status"
    - from: "FileTree::show_node"
      to: "ui.visuals().dark_mode"
      via: "git_color_for_mode(status, dark_mode)"
      pattern: "git_color_for_mode"
    - from: "WorkspaceState git status receiver"
      to: "FileTree::set_git_statuses"
      via: "shared status map"
      pattern: "set_git_statuses"
---

<objective>
Nahradit heuristické darkening git barev explicitní semantickou light/dark paletou a garantovat čitelnost `??` na světlém pozadí.

Purpose: Splnit `TREE-01` a `TREE-02` s testovatelným mapováním statusů, které nebude závislé na náhodném odstínu vstupní barvy.
Output: Sdílený git status resolver + upravený file tree render s explicitní light paletou.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-terminal-git-barvy/02-CONTEXT.md
@.planning/phases/02-terminal-git-barvy/02-RESEARCH.md
@.planning/phases/02-terminal-git-barvy/02-VALIDATION.md
@.planning/phases/02-terminal-git-barvy/02-01-PLAN.md

<interfaces>
```rust
// src/app/ui/file_tree/render.rs (aktuálně)
let adapt_git_color = |c: egui::Color32| -> egui::Color32 {
    if dark_mode { c } else { c * 0.55 }
};

// cíl
let file_color = git_statuses
    .get(&node.path)
    .map(|status| git_color_for_mode(*status, dark_mode))
    .unwrap_or(text_color);
```

```rust
// src/app/ui/background.rs (aktuálně)
fn parse_git_status(...) -> HashMap<PathBuf, egui::Color32>

// cíl
fn parse_git_status(...) -> HashMap<PathBuf, GitVisualStatus>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — sdílený GitVisualStatus model + testy parseru/palet</name>
  <files>src/app/ui/git_status.rs</files>
  <behavior>
    - existuje `GitVisualStatus` enum (`Modified`, `Added`, `Deleted`, `Untracked`)
    - parser z porcelain `XY` vrací semantický status konzistentně (včetně rename/copy edge-case)
    - paletový resolver vrací explicitní dark a explicitní light barvu pro každý status
    - `Untracked` má v light režimu vlastní dobře čitelnou modro-azurovou barvu
  </behavior>
  <action>
    1. Přidej nový modul `src/app/ui/git_status.rs`:
       - `pub enum GitVisualStatus`
       - `pub fn parse_porcelain_status(x: char, y: char) -> GitVisualStatus`
       - `pub fn git_color_for_mode(status: GitVisualStatus, dark_mode: bool) -> egui::Color32`

    2. Přidej unit testy:
       - mapování `??`, `A `, ` D`, `M `, `RM` na očekávané semantické statusy
       - light palette pro `Untracked` je odlišná od `Modified` i `Added`
       - dark/light barvy nejsou identické pro všechny statusy
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor/.polycredo/sandbox && RUSTC_WRAPPER= cargo test file_tree_git -- --nocapture</automated>
  </verify>
  <done>
    Git status model je sdílený, parser i palette resolver mají zelené unit testy.
  </done>
</task>

<task type="auto">
  <name>Task 2: Background fetch + state typy přepnout na semantické statusy</name>
  <files>src/app/ui/background.rs, src/app/ui/workspace/state/mod.rs, src/app/ui/file_tree/mod.rs</files>
  <action>
    1. V `background.rs` nahraď interní mapování přímo na `Color32`:
       - `parse_git_status(...)` vrací `HashMap<PathBuf, GitVisualStatus>`
       - použij `parse_porcelain_status(...)` z nového modulu

    2. Propaguj typovou změnu:
       - `WorkspaceState.git_status_rx` -> `Receiver<HashMap<PathBuf, GitVisualStatus>>`
       - `FileTree.git_colors` přejmenuj na `git_statuses`
       - `set_git_colors(...)` přejmenuj na `set_git_statuses(...)`

    3. Oprav call sites v background loopu (`ws.file_tree.set_git_statuses(statuses)`), bez funkčních změn mimo typy.
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor/.polycredo/sandbox && RUSTC_WRAPPER= cargo check</automated>
  </verify>
  <done>
    Typová linka od git fetch po file tree drží semantický status místo barvy a projekt se kompiluje.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: File tree render přepnout na explicitní light/dark git paletu</name>
  <files>src/app/ui/file_tree/render.rs</files>
  <action>
    1. Odstraň lambda `adapt_git_color` s násobením `0.55`.
    2. Při renderu souboru:
       - přečti `dark_mode = ui.visuals().dark_mode`
       - pokud status existuje, použij `git_color_for_mode(status, dark_mode)`
       - jinak fallback na `text_color`
    3. Doplň testovatelný helper v modulu (např. `resolve_file_tree_git_color`) a unit testy:
       - light `Untracked` je viditelný a ne splývavý
       - všechny 4 statusy mají v light mode vzájemně odlišitelné barvy
  </action>
  <verify>
    <automated>cd /home/stkremen/MyProject/Rust/polycredo_editor/.polycredo/sandbox && RUSTC_WRAPPER= cargo test file_tree_git</automated>
  </verify>
  <done>
    File tree používá explicitní light/dark git palety bez globálního darkeningu.
  </done>
</task>

</tasks>

<verification>
- `RUSTC_WRAPPER= cargo test file_tree_git` — parser + palette + render helper testy zelené
- `RUSTC_WRAPPER= cargo check` — bez compile chyb po typových změnách
- `rg -n \"0\\.55\" src/app/ui/file_tree/render.rs` — starý darkening odstraněn
- `rg -n \"GitVisualStatus|git_color_for_mode|set_git_statuses\" src/app/ui` — semantické mapování je zapojené end-to-end
- Manuální smoke: repozitář s M/A/??/D, přepnout na light mode, ověřit čitelnost a odlišitelnost všech stavů
</verification>

<success_criteria>
- `TREE-01`: M/A/??/D jsou v light mode čitelné přes explicitní light paletu
- `TREE-02`: `??` je v light mode vizuálně jasně viditelný a odlišený
- File tree render nepoužívá globální `* 0.55` transformaci git barev
- Typový tok git statusu je semantický a testovatelný (parser + palette helpery)
- `RUSTC_WRAPPER= cargo check` i relevantní testy jsou zelené
</success_criteria>

<output>
Po dokončení vytvoř `.planning/phases/02-terminal-git-barvy/02-02-SUMMARY.md`.
</output>
