---
phase: 38-watcher-stability-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/watcher.rs
  - tests/phase38_watcher_stability.rs
autonomous: true
requirements:
  - RELIAB-03

must_haves:
  truths:
    - "Watcher ingest vrstva deduplikuje eventy podle path+kind v batch okne bez replay stormu."
    - "Kolize eventu na stejne ceste maji deterministickou prioritu finalniho stavu (remove > create/modify)."
    - "Pri event spike/overflow je aktivovan fail-safe signal pro jeden full reload misto granularniho replaye."
    - "Implementace zustava non-blocking pro UI vlakno a neprovadi I/O v UI ceste."
  artifacts:
    - path: "src/watcher.rs"
      provides: "Stabilizovany batch kontrakt watcheru (dedupe/merge/overflow signal)"
      contains: "ProjectWatcherBatch|merge_event|overflowed"
    - path: "tests/phase38_watcher_stability.rs"
      provides: "Unit dukazy pro dedupe, precedence a overflow kontrakt"
      contains: "phase38_dedupe_path_kind|phase38_batch_window_locked|phase38_remove_precedence|phase38_overflow_sets_fallback"
  key_links:
    - from: "Batch dedupe + merge policy"
      to: "RELIAB-03"
      via: "Delete/restore burst je znormalizovan na deterministicky vystup bez event storm patternu"
      pattern: "phase38_dedupe_path_kind|phase38_batch_window_locked|phase38_remove_precedence"
    - from: "Overflow fallback signal"
      to: "RELIAB-03"
      via: "Pri spicce eventu se pipeline degraduje do jednoho reload triggeru"
      pattern: "phase38_overflow_sets_fallback"
---

<objective>
Uzavrit watcher stabilizacni jadro tak, aby event ingest vracel deterministicky a omezeny batch
vhodny pro dalsi zpracovani bez event stormu.
</objective>

<execution_context>
@/home/stkremen/.codex/get-shit-done/workflows/execute-plan.md
@/home/stkremen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-watcher-stability-verification/38-CONTEXT.md
@.planning/phases/38-watcher-stability-verification/38-RESEARCH.md
@.planning/phases/38-watcher-stability-verification/38-VALIDATION.md
@./AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zavedeni stabilizovaneho batch kontraktu watcheru</name>
  <files>src/watcher.rs, tests/phase38_watcher_stability.rs</files>
  <action>
    1. Zaved kontrakt batch vystupu pro `ProjectWatcher` s explicitnim signalem `overflowed`.
    2. Zamkni batch window v rozsahu `100-150 ms` (preferovane fixne 120 ms) jako explicitni ingest pravidlo.
    3. Implementuj deduplikaci v batch okne podle klice `path + kind`.
    4. Udrz cap eventu tak, aby pipeline pri prekroceni prechazela do overflow vetve.
    5. Doplnenim testu over, ze redundantni eventy neeskaluji velikost finalniho batch outputu.
    6. Pridanim explicitni aserce v testu over, ze batch window pravidlo zustava v intervalu `100-150 ms`.
  </action>
  <verify>
    <automated>cargo test phase38_dedupe_path_kind -- --nocapture</automated>
    <automated>cargo test phase38_batch_window_locked -- --nocapture</automated>
  </verify>
  <done>
    Watcher vrstva produkuje stabilizovany batch se zakladnim dedupe kontraktem.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deterministicka merge precedence remove&gt;create/modify</name>
  <files>src/watcher.rs, tests/phase38_watcher_stability.rs</files>
  <action>
    1. Definuj merge pravidla pro kolize eventu na stejne ceste s prioritou `Removed`.
    2. Udrz finalni vystup deterministicky i pri mixu Created/Modified/Removed v jednom burstu.
    3. Doplnenim focused testu over precedence tabulku pro kriticke kombinace.
  </action>
  <verify>
    <automated>cargo test phase38_remove_precedence -- --nocapture</automated>
  </verify>
  <done>
    Kolizni eventy maji jednotny finalni stav bez nondeterministickych replayu.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Overflow signal a fallback kontrakt</name>
  <files>src/watcher.rs, tests/phase38_watcher_stability.rs</files>
  <action>
    1. Pri event spicce nastav explicitni `overflowed` signal v batchi.
    2. Zajisti, ze overflow path neposila granularni replay vsech syrovych eventu.
    3. Doplnenim testu over, ze burst nad limitem aktivuje fallback kontrakt.
  </action>
  <verify>
    <automated>cargo test phase38_overflow_sets_fallback -- --nocapture</automated>
  </verify>
  <done>
    Overflow je first-class stav pripraveny pro jeden full reload handoff.
  </done>
</task>

</tasks>

<verification>
cargo test phase38_watcher_stability -- --nocapture
cargo check
rg -n "ProjectWatcherBatch|overflowed|Removed|dedupe|merge" src/watcher.rs -S
</verification>

<verification_criteria>
- RELIAB-03: watcher ingest deduplikuje a mergeuje burst eventy bez event storm patternu.
- Batch window je explicitne zamcene v intervalu 100-150 ms a kryte asercnim testem.
- Remove precedence je explicitne testovana a deterministicka.
- Overflow fallback signal je testove dokazany a pripraveny pro wave 2 orchestrace.
</verification_criteria>

<success_criteria>
- Wave 1 doda minimalni stabilizacni jadro v `src/watcher.rs` s auditovatelnymi test hooky.
- Plan scope zustane omezen na RELIAB-03 bez driftu do novych feature.
</success_criteria>

<output>
Po dokonceni vytvor `.planning/phases/38-watcher-stability-verification/38-01-SUMMARY.md`.
</output>
